CashLedgerTransactionDetail is a KeyField

    Representation
        type is Numeric size 12

    Ontology
        stereotype is Document
        business class is CashLedgerTransactionDetail
        Context
            CashManagementGroup
                delete cascades

    Field Rules
        CashLedgerTransactionDetail
            autosequence
                minimize contention
