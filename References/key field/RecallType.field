RecallType is a KeyField
	default label is "Type"
	
    Representation
        type is AlphaUpper size 50

    Ontology
        stereotype is BusinessPolicy
        business class is RecallType
        Context
        	RecallGroup
    
