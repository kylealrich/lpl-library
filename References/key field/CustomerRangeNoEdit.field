CustomerRangeNoEdit is a Field

    Representation
        Group Fields
            FromCustomer is like Customer
            ToCustomer   is like Customer

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromCustomer
            to is   ToCustomer
