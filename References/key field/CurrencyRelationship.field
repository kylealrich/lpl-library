CurrencyRelationship is a KeyField

	Representation
		Group Fields
			FromCurrency
			ToCurrency

    Ontology
    	stereotype is BusinessPolicy     
        business class is CurrencyRelationship
        Context
        	FinanceEnterpriseGroup
     	
    	     	
	Field Rules
		FromCurrency
			required

		ToCurrency
			constraint (ToCurrency != FromCurrency)
				"ToCurrencyCannotBeTheSameAsFromCurrency"			
			required  				
