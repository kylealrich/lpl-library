ContractTermAndCondition is a KeyField
	default label is "ContractTerm_andCondition"

	Representation
		type is Numeric size 8

	Ontology
		stereotype is DocumentFulfillment
		business class is ContractTermAndCondition
		Context
			ContractArticle
				delete cascades

	Patterns
		implements ParentHierarchy
			parent field name is ParentTermAndCondition

	Field Rules
		ContractTermAndCondition
			if (ContractTermAndCondition !entered)
				autosequence
