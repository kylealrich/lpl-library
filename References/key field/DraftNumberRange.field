DraftNumberRange is a Field
	
    Representation
        Group Fields
            FromDraftNumber		is a DraftNumber
            	default label is "DraftNumber"
            ToDraftNumber		is a DraftNumber
            	default label is "_"

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromDraftNumber
            to is ToDraftNumber
