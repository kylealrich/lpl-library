SearchAmountRange is a Field

    Representation
        Group Fields
            Begin is an InternationalAmount
            End   is an InternationalAmount

	Patterns
		implements CompactFormat
			delimiter is "-"		
		implements FieldRange
			from is Begin
			to is 	End

    Field Rules

        End
            constraint (End >= Begin)
                "EndingAmountMustBeGreaterThanBeginningAmount"
