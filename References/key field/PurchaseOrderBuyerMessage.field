PurchaseOrderBuyerMessage is a KeyField

	Representation
		Group Fields
			MessageDate		is TimeStamp
			MessageSequence	is a Sequence

	Ontology
		stereotype is DocumentFulfillment
		business class is PurchaseOrderBuyerMessage
		Context
			PurchasingCompany
			Buyer

	Patterns
		implements CompactFormat

	Field Rules
		MessageDate
			initial value is current timestamp
			default to current timestamp
