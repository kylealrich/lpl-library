PostingDateRange is a Field

    Representation
        Group Fields
            BeginDate is Date
            EndDate   is Date

    Patterns
        implements CompactFormat
        implements FieldRange
            from is BeginDate
            to is   EndDate
