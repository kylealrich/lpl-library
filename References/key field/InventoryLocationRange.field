InventoryLocationRange is a Field

    Representation
        Group Fields
            FromInventoryLocation is like InventoryLocation
            ToInventoryLocation   is like InventoryLocation

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromInventoryLocation
            to is   ToInventoryLocation

	Field Rules
				
    Relations
