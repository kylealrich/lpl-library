ParLevelUpdateLineUpload is a KeyField

    Representation
        type is Numeric size 20

    Ontology
        business class is ParLevelUpdateLineUpload
        Context
            ParLevelUpdateUpload

	Field Rules
		ParLevelUpdateLineUpload
			autosequence
				minimize contention and gaps
