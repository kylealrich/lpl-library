LocalCheckContractMilestone is a Field

    Representation
        Group Fields
            ContractGroup
            CheckContract				is a Contract
            CheckContractMilestone		is a ContractMilestone
