PostalCodeRange is a Field

    Representation
        Group Fields
            FromPostalCode		is an PostalCode
            	default label is "PostalCode"
            ToPostalCode		is an PostalCode
            	default label is "-"

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromPostalCode
            to is ToPostalCode

