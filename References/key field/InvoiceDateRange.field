InvoiceDateRange is a Field

    Representation
        Group Fields
            FromDate is Date
            ToDate   is Date

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromDate
            to is   ToDate
