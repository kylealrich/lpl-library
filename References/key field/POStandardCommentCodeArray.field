POStandardCommentCodeArray is a Field

    Representation 
        StandardCommentCode
            occurs 4 times

    Field Rules
        POStandardCommentCodeArray
            no duplicates on StandardCommentCode
            for each POStandardCommentCodeArray.StandardCommentCode
                if (each entered)
                    constraint (each.IsValidPurchasingCompanyComment) 
                        "CommentCode<each>IsNot_Print_On_Purchase_OrderOr_Print_On_Purchase_Order_Trailer"
