ContractRange is a Field

    Representation
        Group Fields
            FromContract	 	is a Contract
            ToContract			is a Contract

	Patterns
		implements CompactFormat
		implements FieldRange
			from is FromContract
			to is 	ToContract

    Field Rules

        ToContract
            constraint (ToContract >= FromContract)
                "ToContractCannotPrecedeFromContract"
