ParLocationGroupMember is a KeyField

	Ontology
		business class is ParLocationGroupMember
		Context
			ParLocationGroup
				delete cascades
			InventoryLocation

    Field Rules
        InventoryLocation
            constraint (InventoryLocation.IsParLocation)
                "LocationIsNotAParLocation"

            constraint (InventoryLocation.IsActive)
                "ParLocationIsInactive"
