AlternatePhone is a Field
	extends Telephone

	Field Rules
		InternationalPrefix
			if (SubscriberNumber entered)
			    default to TelephoneCountry
				
		SubscriberNumber
			if (Extension not entered)
				required
				
		Extension
			if (SubscriberNumber not entered)
				required
