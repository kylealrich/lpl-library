ARToAPApplicationSelect is a KeyField

    Representation
        Group Fields
            BatchNumber
            TransactionType is a TransType
            Invoice
            PaymentSeq      is a Sequence

    Ontology
        business class is ARToAPApplicationSelect
        Context
            ReceivableCompany

    Patterns
        implements CompactFormat
