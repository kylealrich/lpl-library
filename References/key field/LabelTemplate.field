LabelTemplate is a KeyField
	
    Representation
        type is AlphaUpper 100
        
    Ontology
        business class is LabelTemplate
        Context
            MobileSupplyChainConfiguration
                enable surrogate context
