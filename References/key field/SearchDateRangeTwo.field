SearchDateRangeTwo is a Field

    Representation
        Group Fields
            Begin is Date
            End   is Date

	Patterns
		implements CompactFormat
			delimiter is "-"
		implements FieldRange
			from is Begin
			to is 	End

    Field Rules

        End
            constraint (End >= Begin)
                "EndDateCannotPrecedeBeginDate"



