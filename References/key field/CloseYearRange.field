CloseYearRange is a Field

    Representation
        Group Fields
            Begin			is a GeneralLedgerCloseYear
            End				is a GeneralLedgerCloseYear

	Patterns
		implements CompactFormat
		implements FieldRange
			from is Begin
			to is 	End

	Context Fields
		GeneralLedgerCloseConfiguration

    Field Rules

        End
            constraint (End >= Begin)
                "EndCloseYearCannotPrecedeBeginCloseYear"



