DunningLevel is a Field

    Representation
        type is Numeric size 2

    Field Rules

        DunningLevel
            constraint (DunningLevel >= 1
            and   DunningLevel <= 99)
                "ValueMustBeBetween1And99"

