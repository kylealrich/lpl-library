CashTransactionCategory is a KeyField
	default label is "Category"

	Representation
		type is AlphaUpper 30
		
    Ontology
    	stereotype is BusinessPolicy
        business class is CashTransactionCategory
        Context
        	CashManagementGroup
    
    Patterns
    	implements ParentHierarchy
    		parent field name is ParentTransactionCategory
