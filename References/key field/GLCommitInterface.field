GLCommitInterface is a KeyField
	default label is "GlobalCommitmentInterface"

    Representation
        Group Fields
			RunGroup				is AlphaUpper 30
	        SequenceNumber			is Numeric 12

    Ontology
    	stereotype is AccountingTransaction
        business class is GLCommitInterface
		Context
        	FinanceEnterpriseGroup
        	
    Field Rules
    	RunGroup
    		required
    	SequenceNumber
    		required
        
