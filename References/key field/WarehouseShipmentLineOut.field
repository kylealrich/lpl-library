WarehouseShipmentLineOut is a KeyField

    Representation
        Group Fields
            CreationDate is TimeStamp

    Ontology
		stereotype is DocumentFulfillment
        business class is WarehouseShipmentLineOut
        Context
            WarehouseShipmentLine
            	delete cascades

    Patterns
        implements CompactFormat
