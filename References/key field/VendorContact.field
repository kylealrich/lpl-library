VendorContact is a KeyField

    Representation
        Group Fields
            VendorContactCode
            ContactLevel

    Ontology
        business class is VendorContact
        Context
            Vendor
				delete cascades
            VendorLocation
                optional
				delete cascades

    Patterns
        implements CompactFormat
		
	Field Rules
		VendorContactCode
			required

		ContactLevel
			if (VendorContactCode = "PRIME")
				cannot be entered
