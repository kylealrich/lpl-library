PDFPageOptions is a Field

    Representation
        Group Fields
			PDFPageSize
    		CustomPageDimensions is a PDFPageDimensions
    		UnitOfMeasure is Numeric size 1
    			States
    				Inches 		value is 0
    				Centimeters value is 1
    				Points 		value is 2
			

	Field Rules
		PDFPageSize
			required
			initial value is "Letter"
			
    	CustomPageDimensions
    		if (PDFPageSize.IsCustomPageSize)
    			required
    		else
    			initialize CustomPageDimensions
        
