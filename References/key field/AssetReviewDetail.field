AssetReviewDetail is a KeyField

    Representation
        type is like Asset

    Ontology
    	business class is AssetReviewDetail
		Context
            AssetReview
                delete cascades

    Field Rules
		AssetReviewDetail
			autosequence
                minimize contention
