GeneralLedgerJournalDocument is a KeyField
	default label is "GlobalLedgerJournalDocument"

	Representation
        type is Numeric 12

    Ontology
        business class is GeneralLedgerJournalDocument
        Context 
         	GeneralLedgerJournalControl
    			delete cascades

 	Field Rules
	   	GeneralLedgerJournalDocument
	   		autosequence
	   			minimize contention
