PfiDateRange is a Field
    default label is "DateRange"

    Representation
        Group Fields
            StartDate is Date
            StopDate  is Date

	Patterns
		implements FieldRange
			from is StartDate
			to is 	StopDate

    Field Rules

        StopDate
            constraint (StopDate >= StartDate)
                "StopDateCannotPrecedeStartDate"



