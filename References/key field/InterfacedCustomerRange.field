InterfacedCustomerRange is a Field

    Representation
        Group Fields
            FromCustomer 	 is an InterfacedCustomer
            ToCustomer       is an InterfacedCustomer

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromCustomer
            to is   ToCustomer
