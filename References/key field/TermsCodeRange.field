TermsCodeRange is a Field

    Representation
        Group Fields
            BegTermsCode is a TermsCode
            EndTermsCode is a TermsCode

    Patterns
        implements CompactFormat
        implements FieldRange
            from is BegTermsCode
            to is   EndTermsCode
