GrantFederalReportingTemplate is a Field

    Representation
        Group Fields
			Title			is DocumentTitle size 50
			File			is BinaryDocument
				document template for GrantReportingSettings
			MimeType		is MimeType

	Patterns
		implements CompactFormat
		
	Field Rules
		Title
			required
		File
			required
		MimeType
			required
			constraint (MimeType = "application/rtf" or MimeType = "application/pdf")
				"GrantFederalReportingTemplateMustBeA(.rtf)FileOrA(.pdf)File"			
