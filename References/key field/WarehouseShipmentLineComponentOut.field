WarehouseShipmentLineComponentOut is a KeyField

    Representation
        Group Fields
            CreationDate is TimeStamp

    Ontology
		stereotype is DocumentFulfillment
        business class is WarehouseShipmentLineComponentOut
        Context
            WarehouseShipmentLineComponent

    Patterns
        implements CompactFormat
