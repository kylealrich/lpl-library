WarehouseShipmentOut is a KeyField

    Representation
        Group Fields
            CreationDate is TimeStamp

    Ontology
		stereotype is DocumentFulfillment
        business class is WarehouseShipmentOut
        Context
            WarehouseShipment
            	delete cascades

    Patterns
        implements CompactFormat
