PaymentPriority is a Field

    Representation
        type is Numeric size 2

    Field Rules

        PaymentPriority
            constraint (PaymentPriority >= 1
            and   PaymentPriority <= 99)
                "ValueMustBeBetween1And99"


