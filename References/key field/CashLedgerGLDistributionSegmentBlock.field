CashLedgerGLDistributionSegmentBlock is a KeyField
	default label is "CashLedgerDistributionSegmentBlock"

    Representation
        Group Fields
            BankTransactionCode
            TransactionIDNumber
            DistributionType    is a DistType
            SequenceNumber      is a SeqNbr

    Ontology
        business class is CashLedgerGLDistributionSegmentBlock
        Context
            CashCode

    Patterns
        implements CompactFormat
