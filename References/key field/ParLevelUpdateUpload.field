ParLevelUpdateUpload is a KeyField

    Representation
        type is Numeric size 20

    Ontology
        business class is ParLevelUpdateUpload
        Context
            InventoryLocation

	Field Rules
		ParLevelUpdateUpload
			autosequence
				minimize contention and gaps
