BankStatementLine is a KeyField
	default label is "Line"

	Representation
        Group Fields
			LineNumber		is Numeric 8
			SequenceNumber

	Ontology
		stereotype is DocumentFulfillment
		business class is BankStatementLine
	    Context
			BankStatement
				delete ignored

    Field Rules
		BankStatementLine.LineNumber
        	default to BankStatement.LastBankStatementLine + 1

	Patterns
        implements CompactFormat
			delimiter is "-"
