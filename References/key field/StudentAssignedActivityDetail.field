StudentAssignedActivityDetail is a KeyField

	Representation
		Group Fields
			PostDate		is TimeStamp

    Ontology
        business class is StudentAssignedActivityDetail
        Context
        	StudentAssignedActivity
        		delete cascades

	Field Rules
		PostDate
			default to current timestamp
