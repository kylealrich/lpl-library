AssetItemDisposal is a KeyField

    Representation
        Group Fields
            ProcessSequence is a Sequence

    Ontology
        business class is AssetItemDisposal
        Context
        	AssetDisposal
                delete cascades
            AssetItem
                delete ignored

    Patterns
        implements CompactFormat
