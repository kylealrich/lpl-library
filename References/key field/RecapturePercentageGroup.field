RecapturePercentageGroup is a Field

	Representation
		Group Fields
			RecapturePercentage is a BusinessPercent

	Patterns
		implements CompactFormat

	Field Rules
		RecapturePercentage
			constraint (RecapturePercentage <= 100)
				"RecapturePercentageCannotBeGreaterThan100%"
