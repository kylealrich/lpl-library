InventoryLocationGroup is a Field

    Representation
    	Group Fields
        	InventoryCompany
    		InventoryLocation
    		
	Field Rules
	
		InventoryCompany
			initial value is actor.agent(Employee).RecallUser.DefaultCompany
		InventoryLocation
			initial value is actor.agent(Employee).RecallUser.DefaultLocation
    		
