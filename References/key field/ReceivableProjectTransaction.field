ReceivableProjectTransaction is a KeyField

    Representation
        Group Fields
            TransType
            BatchNumber
            Transaction	is like TransactionNumber
            PaymentSeq  is a Sequence

    Ontology
        business class is ReceivableProjectTransaction
        Context
            ReceivableCompany

    Patterns
        implements CompactFormat
