BatchRange is a Field

    Representation
        Group Fields
            BegBatch is a BatchNumber
            EndBatch is a BatchNumber

    Patterns
        implements CompactFormat
        implements FieldRange
            from is BegBatch
            to is   EndBatch
