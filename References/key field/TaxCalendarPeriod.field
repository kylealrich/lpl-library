TaxCalendarPeriod is a KeyField

    Representation
        type is Numeric size 2		

    Ontology
        business class is TaxCalendarPeriod
        Context
        	TaxCalendar
        		delete cascades

    Field Rules

        TaxCalendarPeriod
            constraint (TaxCalendarPeriod >= 1
            and   TaxCalendarPeriod <= 13)
                "ValueMustBeBetween1And13"

