Step is a Field

    Representation
        type is Numeric size 3
        States
            DefinedOnDetail value is 0
                default label is "Defined on Detail"

    Field Rules

        Step
            constraint (Step >= 1
            and   Step <= 999)
                "ValueMustBeBetween1And999"

