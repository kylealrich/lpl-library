CurrencyExchangeRate is a Field

    Representation        
		type is Decimal size 31.15
		
	Context Fields
		BypassNegativeRateEdit
		
	Field Rules
	
		CurrencyExchangeRate
			if (!BypassNegativeRateEdit)
				constraint (CurrencyExchangeRate >= 0)
					"RateCannotBeNegative"
		
