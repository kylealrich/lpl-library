DateRange3 is a Field

    Representation
        Group Fields
            Begin is Date
            End   is Date

	Patterns
		implements CompactFormat
		implements FieldRange
			from is Begin
			to is 	End

    Field Rules

        End
            constraint (End >= Begin)
                "EndDateCannotPrecedeBeginDate"
