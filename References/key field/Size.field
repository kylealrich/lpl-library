Size is a Field

    Representation
        type is Numeric size 2

    Field Rules

        Size
            constraint (Size >= 1
            and   Size <= 24)
                "ValueMustBeBetween1And24"
            constraint (Size >= 1
            and   Size <= 99)
                "ValueMustBeBetween1And99"
            constraint (Size >= 1
            and   Size <= 32)
                "ValueMustBeBetween1And32"

