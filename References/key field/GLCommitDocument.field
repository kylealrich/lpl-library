GLCommitDocument is a KeyField
	default label is "GlobalLedgerCommitDocument"

    Representation
        type is UniqueID

    Ontology
    	stereotype is AccountingTransaction
        business class is GLCommitDocument
