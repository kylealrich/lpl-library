NumberRange is a Field

    Representation
        Group Fields
            From is Numeric size 4
            To   is Numeric size 4

	Patterns
		implements CompactFormat
			delimiter is "-"
		implements FieldRange
			from is From
			to is 	To

    Field Rules

        To
            constraint (To >= From)
                "ToNumberCannotPrecedeFromNumber"
