IntrastatHeader is a KeyField

    Representation
        Group Fields
            FiscalYear
            IntrastatNumber

    Ontology
        business class is IntrastatHeader
        Context
            TaxEntity

    Patterns
        implements CompactFormat

    Field Rules
		FiscalYear
			default to TaxEntity.FiscalYear
