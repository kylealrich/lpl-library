RecallDomain is a KeyField
	default label is "Domain"
	
    Representation
        type is AlphaUpper size 50

    Ontology
        business class is RecallDomain
        stereotype is BusinessPolicy
        Context
        	RecallGroup
    
    Patterns
    	implements UserDefinedStates
