VendorRange is a Field

    Representation
        Group Fields
            FromVendor is like Vendor
            ToVendor   is like Vendor

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromVendor
            to is   ToVendor

	Field Rules
				
    Relations
