DunLevel is a Field

    Representation
        type is Numeric size 2

    Field Rules

        DunLevel
            constraint (DunLevel >= 1
            and   DunLevel <= 99)
                "ValueMustBeBetween1And99"

