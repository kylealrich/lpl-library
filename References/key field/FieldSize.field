FieldSize is a Field

    Representation
        type is Numeric size 2

    Field Rules

        FieldSize
            constraint (FieldSize >= 1
            and   FieldSize <= 32)
                "ValueMustBeBetween1And32"

