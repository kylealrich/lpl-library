CustomerOrderReturn is a KeyField
    default label is "ReturnAuthorizationNumber"

    Representation
        type is Numeric size 10

    Ontology
    	stereotype is Document
        business class is CustomerOrderReturn
        Context
            BillingCompany
	
	Field Rules
		CustomerOrderReturn
			if (not Company.ManualReturnNumbering)
				autosequence
