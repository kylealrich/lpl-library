ProjectRange is a Field

    Representation
        Group Fields
            FromProject is a Project
            ToProject   is a Project

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromProject
            to is   ToProject
