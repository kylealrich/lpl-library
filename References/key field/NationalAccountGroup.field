NationalAccountGroup is a Field

    Representation
        Group Fields
            NatCompany  is a ReceivableCompany
            NatCustomer is a Customer

    Patterns
        implements CompactFormat
        
	Field Rules
   		NatCompany
    		if(NatCustomer entered)
    			required
    				"NationalAccountCompanyIsRequired!"
   			
    	NatCustomer
   			if(NatCompany entered)
    			required
   					"NationalAccountCustomerIsRequired!"



