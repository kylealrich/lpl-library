CashLedgerTransaction is a KeyField

    Representation
        type is Numeric size 10

    Ontology
    	stereotype is Document
        business class is CashLedgerTransaction
        Context
            BankTransactionCode
            CashLedgerSourceRecord

    Field Rules

        CashLedgerTransaction
        	if (CashLedgerTransaction not entered)
	            autosequence using BankTransactionCode.TransactionIDNumber 

