HandlingTimeInMinutes is a Field

    Representation
        type is Numeric size 3 

    Conditions
        NotNegative
            when (HandlingTimeInMinutes >=0)

    Field Rules
        HandlingTimeInMinutes
            constraint (NotNegative)
                "HandlingTimeInMinutesShouldBeAPositiveValue"
