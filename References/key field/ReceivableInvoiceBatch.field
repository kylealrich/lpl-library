ReceivableInvoiceBatch is a KeyField

    Representation
        type is Numeric size 12

    Ontology
        business class is ReceivableInvoiceBatch
        Context
            ReceivableCompany

	Field Rules
		ReceivableInvoiceBatch
			if (Company.TransBatchNumbering)
				autosequence using Company.LastAutoTransBatchNumber

			if (ReceivableInvoiceBatch not entered)
				constraint (Company.TransBatchNumbering)
					"BatchNumberRequired"                                     
	
