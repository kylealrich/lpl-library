NumberOfFields is a Field

    Representation
        type is Numeric size 2

    Field Rules

        NumberOfFields
            constraint (NumberOfFields >= blank
            and   NumberOfFields <= 15)
                "ValueMustBeBetweenAnd15"

