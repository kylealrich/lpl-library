CashLedgerBankUpdate is a KeyField

    Representation
        Group Fields
            RunGroup
            CashManagementAccount
            SequenceNumber
		
    Ontology
        business class is CashLedgerBankUpdate
        Context
        	CashManagementGroup
        	
    Patterns
        implements CompactFormat

    Field Rules
    	CashLedgerBankUpdate.RunGroup
    		required
		CashLedgerBankUpdate.SequenceNumber	
			autosequence
    			minimize contention   	
