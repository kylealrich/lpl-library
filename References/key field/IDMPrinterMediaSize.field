IDMPrinterMediaSize is a Field

    Representation
        Group Fields
        	Height				is an IDMMediaSize 
				default label is "Height_(in)"
        	Width				is an IDMMediaSize
				default label is "Width_(in)"
        	MarginFromLeft		is an IDMMediaSize
        		default label is "LeftMargin"
			MarginFromTop		is an IDMMediaSize
				default label is "TopMargin"
	
	Context Fields
		IDMPrinter

	Field Rules
		Width
			required
		
		Height
			required
