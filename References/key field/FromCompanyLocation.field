FromCompanyLocation is a Field

    Representation
        Group Fields
            FromCompany  is an InventoryCompany
            FromLocation is an InventoryLocation

    Patterns
        implements CompactFormat

	Context Fields
		Requisition

	Field Rules
	
		FromLocation
			if  (FromCompany entered
			and  Requisition entered)
				required
					"From_\LocationIsRequired"
