YearPeriodStatus is a Field

    Representation
        Group Fields
            YearPeriod is Date
            Status     is AlphaUpper size 1
                States
                    Closed value is "C"
                    Open   value is "O"

    Patterns
        implements CompactFormat

	Field Rules
		YearPeriod
			required
				"MustEnterPeriodEndDateForAllPeriods"

		Status
			if (YearPeriod entered)
				default to Status.Open
        
