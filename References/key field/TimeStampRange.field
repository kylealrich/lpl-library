TimeStampRange is a Field

    Representation
        Group Fields
            Begin			is TimeStamp
            End				is TimeStamp

	Patterns
		implements CompactFormat
		implements FieldRange
			from is Begin
			to is 	End

    Field Rules

        End
            constraint (End >= Begin)
                "EndDateCannotPrecedeBeginDate"



