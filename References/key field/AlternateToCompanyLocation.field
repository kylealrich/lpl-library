AlternateToCompanyLocation is a Field
    Representation
        Group Fields
            ToCompany    is like InventoryCompany
            ToLocation   is like InventoryLocation

    Patterns
        implements CompactFormat
