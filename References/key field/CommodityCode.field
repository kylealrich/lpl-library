CommodityCode is a KeyField

	Representation
		Segment is AlphaUpper size 10
			occurs 6 times

	Ontology
		stereotype is BusinessPolicy
		business class is CommodityCode
		Context
			ItemGroup

	Patterns
		implements CompactFormat
			delimiter is "-"
		implements ArrayHierarchy

	Display Fields
		Description

	Field Rules
		CommodityCode
			array size is ItemGroup.CommodityCodeStructure.MaxLevels
			field size is ItemGroup.CommodityCodeStructure.Segments.Segment.Size
