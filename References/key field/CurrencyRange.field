CurrencyRange is a Field

    Representation
        Group Fields
            FromCurrency is a Currency
            ToCurrency   is a Currency

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromCurrency
            to is   ToCurrency
