BudgetNbr is a Field

    Representation
        type is Numeric size 3

    Field Rules

        BudgetNbr
            constraint (BudgetNbr >= blank
            and   BudgetNbr <= 999)
                "ValueMustBeBetweenAnd999"

