CashCodeConsolidatedUpdate is a KeyField

    Representation
        Group Fields
            Company             is a GeneralLedgerCompany
            BankTransactionCode
            SequenceNumber		is a SeqNbr

    Ontology
    	stereotype is DocumentFulfillment
        business class is CashCodeConsolidatedUpdate
        Context
            CashCode

    Patterns
        implements CompactFormat

	Field Rules
		SequenceNumber
			autosequence
				minimize contention
