BankDateRange is a Field

    Representation
        Group Fields
            StartDate is Date
            EndDate   is Date

    Patterns
        implements CompactFormat
        implements FieldRange
            from is StartDate
            to is   EndDate

    Field Rules
        EndDate
            constraint (EndDate >= StartDate)
                "EndDateCannotPrecedeStartDate"
