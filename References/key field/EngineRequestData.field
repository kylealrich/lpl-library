EngineRequestData is a Field

    Representation
        Group Fields
            GroupId				is an AsyncActionRequest
            	delete ignored
            BackgroundGroupName	is AlphaUpper up to 200
            ClassName			is Alpha up to 200
            ActionName			is Alpha up to 200
            
	Relations
		AsyncActionRequestRel
			one-to-one relation to AsyncActionRequest
			Field Mapping uses symbolic key
				related.AsyncActionRequest = GroupId
