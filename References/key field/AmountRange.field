AmountRange is a Field

    Representation
        Group Fields
            FromAmount is an InternationalAmount
            ToAmount   is an InternationalAmount

    Patterns
        implements CompactFormat
        implements FieldRange
            from is FromAmount
            to is   ToAmount
