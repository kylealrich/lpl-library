AssetBookPeriodHistory is a KeyField

    Representation
        Group Fields
            Period is Numeric size 2

    Ontology
        business class is AssetBookPeriodHistory
        Context
            AssetBookHistory
            	delete cascades

    Patterns
        implements CompactFormat
