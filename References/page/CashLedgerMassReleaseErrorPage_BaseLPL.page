CashLedgerMassReleaseErrorPage is a Page
    title is "CashLedgerMassReleaseErrors"
    show panel control on left

    TransactionErrors is a Panel
        business class is CashLedgerTransactionError
            list is TransactionErrorsForMassReleaseList

    PaymentErrors is a Panel
        business class is CashLedgerTransactionError
            list is PaymentErrorsForMassReleaseList

    CashReceiptErrors is a Panel
        business class is CashLedgerTransactionError
            list is CashReceiptErrorsForMassReleaseList

    ReturnedPaymentErrors is a Panel
        business class is CashLedgerTransactionError
            list is ReturnedPaymentErrorsForMassReleaseList

    EFTExtractErrors is a Panel
        business class is CashLedgerTransactionError
            list is EFTExtractErrorsForMassReleaseList

    BankFundTransferErrors is a Panel
        business class is CashLedgerTransactionError
            list is BankFundTransferErrorsForMassReleaseList

