ReconciliationManagementPageUX1 is a Page
    title is "Reconciliations"
    implements MenuSearch

    PeriodReconciliations is a Panel
        business class is ReconciliationAccountStructure
            list is ReconciliationAccountStructureCardPeriodList

    AllOpenReconciliations is a Panel
        business class is ReconciliationAccountStructure
            list is ReconciliationAccountStructureCardOpenList

    ReconciliationsByReportGroup is a Panel
        business class is ReconciliationAccountStructure
            list is ReportGroupsByReconciliationsList

    ClosedReconciliationsOutOfBalanceWithLedger is a Panel
        business class is ReconciliationAccountStructure
            list is ReconciliationAccountStructureCardClosedList

