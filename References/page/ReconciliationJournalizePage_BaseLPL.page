ReconciliationJournalizePage is a Page
    title is "JournalizeReconciliationDistributions"
    show panel control on left

    ReconciliationDistributions is a Panel
        title is "Preview"
        business class is PeriodEndDistribution
            list is ReleasedPeriodEndDistributionList
                title is "Preview"

    ViewJournalizedGLDistributions is a Panel
        title is "Results"
        business class is JournalizeGroupResult
            list is JournalizeGroupResults
                title is "Results"

