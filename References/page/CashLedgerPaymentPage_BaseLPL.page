CashLedgerPaymentPage is a Page
    title is "CashLedgerPayment"
    show panel control on left

    AllCashLedgerPayments is a Panel
        business class is CashLedgerPayment
            list is AllCashLedgerPayments

    ReleasedCashLedgerPayments is a Panel
        business class is CashLedgerPayment
            list is ReleasedCashLedgerPayments

    ReportableIncomeAdjustments is a Panel
        business class is CashLedgerPayment
            list is ReportableIncomeAdjustments

