RequestingLocationPage is a Page
    show panel control on left

    RequestingLocations is a Panel
        business class is RequestingLocation
            list is RequestingLocationList

    RequestingLocationAuditHistory is a Panel
        business class is RequestingLocation
            list is RequestingLocationAuditList

    UsingResponsiblityMatrixApproval is a Panel
        visible when (actor.context.FinanceEnterpriseGroup.EnableResponsibilityMatrix)
        business class is RequestingLocation
            list is RequestingLocationsUsingMatix

    InvalidIssueAccountForMatrixApproval is a Panel
        visible when (actor.context.FinanceEnterpriseGroup.EnableResponsibilityMatrix)
        business class is RequestingLocation
            list is RequestingLocationsWithInvalidIssueAccountForMatix

