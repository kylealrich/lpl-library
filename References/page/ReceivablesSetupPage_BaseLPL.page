ReceivablesSetupPage is a Page
    title is "ReceivableSetup"

    ReceivableProcessLevel is a Panel
        business class is ReceivableProcessLevel
            list is ReceivableProcessLevelList

    CustomerContacts is a Panel
        business class is CompanyCustomerContact
            list is CustomerContacts

    NationalAccount is a Panel
        business class is NationalAccountBalance
            list is ForNationalAccountList

    CustomerClass is a Panel
        business class is CustomerMajorClass
            list is CustomerMajorClassList

    BankAccounts is a Panel
        business class is CompanyCustomerBankAccount
            list is MICRCustomers

    ReasonCodes is a Panel
        business class is ReceivableReason
            list is ReceivableReasonCodeList

    ReceivableGeneralLedgerCode is a Panel
        title is "ReceivableGlobalLedgerCode"
        business class is ReceivableGeneralLedgerCode
            list is ListReceivableGeneralLedgerCode
                title is "ReceivableGlobalLedgerCode"

    DisputeCodes is a Panel
        business class is ReceivableInvoiceDisputeCode
            list is ReceivableInvoiceDisputeCodeInline

    ReceivableOperators is a Panel
        business class is ReceivableOperator
            list is ReceivableOperatorInline

    DistributionCategory is a Panel
        business class is ReceivableDistributionCategory
            list is Categories

    AgingCodes is a Panel
        business class is AgingCode
            list is AgingCodesInline

    ProcessLevelGroups is a Panel
        business class is ReceivableProcessLevelReportHeader
            list is ProcessLevelGroups

    ReportGroups is a Panel
        business class is ReceivableProcessLevelReportGroup
            list is ProcessLevelReportGroups

    DunningLetterCodes is a Panel
        business class is DunningLetterTextCode
            list is DunningLetterTextCodes

    DunningLetter is a Panel
        business class is DunningLetter
            list is DunningLetterList

    DunningTextCodes is a Panel
        business class is DunningTextCode
            list is DunningTextCodes

    DunningProcessCodes is a Panel
        business class is DunningProcessCode
            list is DunningProcessCodeMaintenanceList

    FinanceChargeLanguageText is a Panel
        business class is FinanceChargeLanguageText
            list is LatePaymentNoticeTextCodeMaintenance

    ChargebackTextCode is a Panel
        business class is ChargebackTextCode
            list is ChargebackTextCodeMaintenance

    ReturnToMakerText is a Panel
        business class is RTMPaymentLanguageText
            list is ReturntoMakerTextCodeMaintenance

    StatementText is a Panel
        business class is ReceivableStatementLanguageText
            list is StatementTextCodeMaintenance

    ReceivableCycle is a Panel
        business class is ReceivableCycle
            list is ReceivableCycleCodeList

    CreditAnalyst is a Panel
        business class is CreditAnalyst
            list is CreditAnalystList

    Territory is a Panel
        business class is Territory
            list is TerritoryList

    SalesRepresentative is a Panel
        business class is SalesRepresentative
            list is SalesRepresentativeList

    FinanceChargeCode is a Panel
        business class is FinanceChargeCode
            list is FinanceChargeCodeList

    ReceivableLockbox is a Panel
        business class is ReceivableLockBox
            list is ReceivableLockBoxPrimaryList

    ReceivableHoldCode is a Panel
        business class is ReceivableHoldCode
            list is HoldCodeLists

