ProjectAccountingSetupPage is a Page
    title is "<actor.context.FinanceEnterpriseGroup.ProjectLabel>LedgerSetup"
    show panel control on left

    Structure is a Panel
        business class is ProjectStructure
            list is ProjectStructures

    Status is a Panel
        business class is ProjectStatus
            list is ProjectStatusList

    EffortSchedules is a Panel
        business class is ProjectEffortSchedule
            list is ProjectEffortScheduleList

    LaborSchedules is a Panel
        business class is ProjectLaborSchedule
            list is ProjectLaborScheduleList

    Roles is a Panel
        business class is ProjectRole
            list is ProjectRoles

    PeriodOfPerformance is a MultiPanePanel
        pane 1
            business class is PeriodOfPerformance
                list is PeriodOfPerformanceList
        pane 3
            business class is pane 1.PeriodOfPerformanceSystem set
                list is PeriodOfPerformanceSystemList

    SalaryCaps is a Panel
        business class is ProjectSalaryCap
            list is ProjectSalaryCapList

    FringeBurdenCodes is a Panel
        business class is ProjectBurdenCode
            list is ProjectBurdenCodeList

    IndirectBurdenCodes is a Panel
        business class is ProjectIndirectBurdenCode
            list is ProjectIndirectBurdenCodes

    FeeCodes is a Panel
        business class is ProjectFeeCode
            list is ProjectFeeCodes

    BillingGroups is a Panel
        business class is ProjectBillingGroup
            list is ProjectBillingGroupList

    BillingRateTables is a Panel
        business class is ProjectBillingRateTable
            list is ProjectBillingRateTableList

    JurisdictionCodes is a Panel
        business class is ProjectJurisdictionCode
            list is ProjectJurisdictionCodes

    TaxLocations is a Panel
        business class is ProjectServiceLocation
            list is ProjectServiceLocationList

    ProjectFundingSourceTemplate is a Panel
        title is "ProjectFundingSourceTemplates"
        business class is ProjectFundingSourceTemplate
            list is ProjectFundingSourceTemplate
                title is "ProjectFundingSourceTemplates"

    ProjectBillingOverrideTemplate is a Panel
        title is "ProjectBillingOverrideTemplates"
        business class is ProjectBillingOverrideTemplate
            list is ProjectBillingOverrideTemplate
                title is "ProjectBillingOverrideTemplates"

    ReportingSettingsByActor is a Panel
        business class is ProjectReportingSettings
            list is ProjectReportingSettingsList

    GlobalReportingSettings is a Panel
        business class is ProjectReportingSettingsAll
            list is ProjectReportingSettingsList

    CustomGroups is a Panel
        business class is BusinessClassGroup
            list is BusinessClassGroups

    MajorCategories is a Panel
        business class is ProjectMajorCategory
            list is ProjectMajorCategoryList

    ContractTypes is a Panel
        business class is ProjectContractType
            list is ProjectContractTypeList

    CustomerTypes is a Panel
        business class is ProjectCustomerType
            list is ProjectCustomerTypeList

    WorkTypes is a Panel
        business class is ProjectWorkType
            list is ProjectWorkTypeList

    ServiceTypes is a Panel
        business class is ProjectServiceType
            list is ProjectServiceTypeList

    UsageTypes is a Panel
        business class is ProjectUsageType
            list is ProjectUsageTypeList

    Departments is a Panel
        business class is FinanceDimension2Department
            list is FinanceDimension2Department

    Divisions is a Panel
        business class is FinanceDimension2Division
            list is FinanceDimension2Division

    ActionReasons is a Panel
        business class is ActionReason
            list is ActionReasonList

    Deliverables is a Panel
        business class is ProjectDeliverable
            list is ProjectDeliverableList

    ALN is a Panel
        business class is FinanceDimension2ALNCode
            list is ALNCodesList

    ProgramTitle is a Panel
        business class is FinanceDimension2ProgramTitle
            list is ProgramTitleList

