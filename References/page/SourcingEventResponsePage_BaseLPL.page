SourcingEventResponsePage is a Page
    title is "EventResponses"
    implements MenuSearch

    AnalyzeResponses is a Panel
        business class is SourcingEvent
            list is EventsToBeAwarded

    ManageResponses is a Panel
        business class is SourcingEventResponse
            list is EventResponses

    SearchResponseLines is a Panel
        business class is SourcingEventLineResponse
            list is EventLineResponseSearch

