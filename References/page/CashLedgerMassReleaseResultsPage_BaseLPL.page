CashLedgerMassReleaseResultsPage is a Page
    title is "CashLedgerMassReleaseResults"
    show panel control on left

    TransactionResults is a Panel
        business class is CashLedgerMassReleaseResult
            list is TransactionsResultList

    PaymentResults is a Panel
        business class is CashLedgerMassReleaseResult
            list is PaymentsResultList

    CashReceiptResults is a Panel
        business class is CashLedgerMassReleaseResult
            list is CashReceiptsResultList

    ReturnedPaymentResults is a Panel
        business class is CashLedgerMassReleaseResult
            list is ReturnedPaymentsResultList

    EFTExtractResults is a Panel
        business class is CashLedgerMassReleaseResult
            list is EFTExtractsResultList

    BankFundTransferResults is a Panel
        business class is CashLedgerMassReleaseResult
            list is BankFundTransfersResultList

