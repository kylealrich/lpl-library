MyRequisitionsPage is a Page
    title is "MyRequisitions"

    UnreleasedRequisitions is a Panel
        title is "Unreleased"
        business class is Requisition
            list is MyUnreleasedRequisitions
                title is "Unreleased"

    OpenRequisitions is a Panel
        title is "Open"
        business class is Requisition
            list is MyOpenRequisitions
                title is "Open"

    NeedsApprovalRequisitions is a Panel
        title is "NeedsApproval"
        business class is Requisition
            list is MyNeedsApprovalRequisitions
                title is "NeedsApproval"

    AllRequisitions is a Panel
        title is "All"
        business class is Requisition
            list is MyAllRequisitions
                title is "All"

    RushOrderRequisitionLines is a Panel
        title is "RushOrders"
        business class is RequisitionLine
            list is MyRushOrderRequisitionLinesList
                title is "RushOrders"

    ClosedRejectedRequisitions is a Panel
        title is "Closed_/Rejected"
        business class is Requisition
            list is MyClosedRejectedRequisitions
                title is "Closed_/Rejected"

    SearchRequisitions is a Panel
        title is "Search"
        business class is Requisition
            list is MyRequisitionsSearchList
                title is "Search"

    LineSearchRequisitions is a Panel
        title is "LineSearch"
        business class is RequisitionLine
            list is MyRequisitionsLineSearchList
                title is "LineSearch"

    SearchPOLines is a Panel
        business class is PurchaseOrderLineSource
            list is PurchaseOrdersFromMyRequisitions

    ContractCosts is a Panel
        business class is ContractLine
            list is ContractCostsForAnItem

    AuditHistory is a Panel
        title is "AuditHistory"
        visible when (not actor.context.FinanceEnterpriseGroup.ColumnarRequisitionAuditInUse)
        business class is Requisition
            list is MyRequisitionAuditList
                title is "AuditHistory"

    AuditHistoryColumnar is a Panel
        title is "AuditHistory"
        visible when (actor.context.FinanceEnterpriseGroup.ColumnarRequisitionAuditInUse)
        business class is Requisition
            list is ColumnarMyRequisitionAuditList
                title is "AuditHistory"

    LineAuditHistory is a Panel
        title is "LineAuditHistory"
        visible when (not actor.context.FinanceEnterpriseGroup.ColumnarRequisitionLineAuditInUse)
        business class is RequisitionLine
            list is MyRequisitionLineAuditList
                title is "LineAuditHistory"

    LineAuditHistoryColumnar is a Panel
        title is "LineAuditHistory"
        visible when (actor.context.FinanceEnterpriseGroup.ColumnarRequisitionLineAuditInUse)
        business class is RequisitionLine
            list is ColumnarMyRequisitionLineAuditList
                title is "LineAuditHistory"

