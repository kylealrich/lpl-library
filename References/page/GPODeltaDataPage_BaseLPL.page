GPODeltaDataPage is a Page
    title is "GPODeltaData"

    Contracts is a Panel
        business class is ContractGPOHeaderDelta
            list is ContractGPOHeaderDeltaList

    Distributors is a Panel
        business class is ContractGPOSupplierDelta
            list is ContractGPOSupplierList

    Items is a Panel
        business class is ContractGPOItemDelta
            list is ContractGPOItemDeltaList

    Participants is a Panel
        business class is ContractGPOParticipantDelta
            list is ContractGPOParticipantDeltaList

    DistributorMarkups is a Panel
        business class is ContractGPODistributorDelta
            list is ContractGPODistributorDeltaList

    ContractCategories is a Panel
        business class is ContractGPOCategoryDelta
            list is ContractGPOCategoryDeltaList

