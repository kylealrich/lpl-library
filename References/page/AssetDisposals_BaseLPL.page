AssetDisposals is a Page
    title is "AssetDisposals"
    implements MenuSearch
    is report for AssetManager,AssetAnalyst,ApplicationAdministrator,FinancialsViewer,Assets,FinancialsProcessor
        category is "History"

    AssetDisposals is a Panel
        title is "AssetDisposals"
        business class is AssetDisposal
            list is AssetDisposals
                title is "AssetDisposals"

    AssetDisposalList is a Panel
        business class is AssetDisposal
            list is AssetDisposal

    DisposedAssetBooks is a Panel
        business class is AssetBookDisposal
            list is AssetBookDisposalListView

