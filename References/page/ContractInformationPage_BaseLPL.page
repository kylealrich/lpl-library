ContractInformationPage is a Page
    title is "AdditionalContractInformation"

    ContractAlerts is a Panel
        business class is Contract
            list is ContractAlerts

    Commitments is a MultiPanePanel
        title is "HeaderAndLineCommitments"
        pane 1
            business class is ContractLineDistribution
                list is LineCommitments
        pane 2
            business class is Contract
                list is ContractCommitments

    MoreCommitments is a MultiPanePanel
        title is "SpreadCommitments"
        pane 1
            business class is ContractSpread
                list is Commitments
        pane 2
            business class is ContractLineDistributionSpread
                list is Commitments

    LineErrorsToFix is a Panel
        business class is ContractLine
            list is ErrorsToFix

    TierReportingContracts is a Panel
        business class is Contract
            list is TierReportingList

    TierQualifiers is a Panel
        business class is ContractTierQualifier
            list is TierQualifiersForCalculations

    TierQualifierCalculations is a Panel
        business class is TierQualifierCalculation
            list is TierQualifierCalculations

    ContractCosts is a Panel
        business class is ContractLine
            list is ContractCostsForAnItem

    TaxDefaults is a Panel
        business class is Contract
            list is ContractsWithTax

    ToleranceErrors is a Panel
        business class is Contract
            list is ToleranceErrors

    CostComparisons is a Panel
        business class is Contract
            list is CostComparisons

    ProcurementTemplatesToUpdate is a Panel
        business class is Contract
            list is ContractsWithProcurementTemplates

    ProcessingTimes is a Panel
        business class is Contract
            list is DaysToProcess

    LinesWithGMDNTerms is a Panel
        business class is ContractLine
            list is LinesWithGMDNTerms

    ContractEvaluations is a Panel
        business class is SupplierPerformanceEvaluation
            list is ContractEvaluations

