CBOpenCommitmentsAndEncumbrances is a Page
    title is "OpenCommitmentsAndEncumbrances"
    show panel control on left

    TransactionCommitments is a Panel
        business class is CashLedgerTransaction
            list is CashLedgerTransactionsWithCommitmentsList

    CashReceiptCommitments is a Panel
        business class is CashLedgerCashReceiptHeader
            list is CashReceiptDepositsWithCommitmentsList

    PaymentCommitments is a Panel
        business class is CashLedgerPayment
            list is CashLedgerPaymentsWithCommitmentsList

    EFTCommitments is a Panel
        business class is CashLedgerElectronicFundsTransferTransaction
            list is ElectronicFundsTransferTransactionsWithCommitmentsList

    ReturnedPaymentCommitments is a Panel
        business class is CashLedgerReturnedPayment
            list is CashLedgerReturnedPaymentsWithCommitmentsList

    VarianceAdjustmentCommitments is a Panel
        business class is CashLedgerReconciliationAdjustment
            list is ReconciliationVarianceAdjustmentsWithCommitmentsList

    BankFundTransferCommitments is a Panel
        business class is CashLedgerFundTransferHeader
            list is BankFundTransfersWithCommitmentsList

    NonCashLedgerTransactionCommitments is a Panel
        business class is NonCashLedgerTransaction
            list is NonCashLedgerTransactionsWithCommitmentsList

    BankStatementLineCommitments is a Panel
        business class is BankStatementLine
            list is BankStatementLinesWithCommitmentsList

    WireTransferCommitments is a Panel
        business class is WireTransferBatchDetail
            list is WireTransfersWithCommitmentsList

