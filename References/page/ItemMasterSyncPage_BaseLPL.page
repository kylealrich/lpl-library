ItemMasterSyncPage is a Page
    title is "ItemMasterSynchronization"

    MyItems is a Panel
        business class is ItemMasterSync
            list is MyItemMasterSyncList

    AllItems is a Panel
        business class is ItemMasterSync
            list is ItemMasterSyncList

    MyCompletedItems is a Panel
        business class is ItemMasterSyncBatchNumber
            list is MyCompletedSynchronizations

    AllCompletedItems is a Panel
        business class is ItemMasterSyncBatchNumber
            list is AllCompletedSynchronizations

    CompletedSynchronizationsByItem is a Panel
        business class is ItemMasterSyncReport
            list is CompletedItemsByItemReportList

