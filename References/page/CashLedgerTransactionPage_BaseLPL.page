CashLedgerTransactionPage is a Page
    title is "ManageTransactions"
    show panel control on left

    AllTransactions is a Panel
        business class is CashLedgerTransaction
            list is AllCashLedgerTransactionsList

    ReleasedTransactions is a Panel
        business class is CashLedgerTransaction
            list is ReleasedCashLedgerTransactionsList

    UnreleasedTransactions is a Panel
        business class is CashLedgerTransaction
            list is UnreleasedCashLedgerTransactionsList

    UnreconciledTransactionsOver60Days is a Panel
        business class is CashLedgerTransaction
            list is UnreconciledTransactionsOver60DaysList

    UnreleasedFundTransfers is a Panel
        business class is CashLedgerFundTransferHeader
            list is UnreleasedFundTransfersList

    UnreleasedCashReceipts is a Panel
        business class is CashLedgerCashReceiptHeader
            list is UnreleasedCashReceiptsList

    UnreleasedPayments is a Panel
        business class is CashLedgerPayment
            list is UnreleasedCashLedgerPaymentsList

    UnreleasedReturnedPayments is a Panel
        business class is CashLedgerReturnedPayment
            list is UnreleasedReturnedPaymentsList

    UnreleasedElectronicFundsTransfers is a Panel
        business class is CashLedgerElectronicFundsTransferTransaction
            list is UnreleasedElectronicFundsTransfersList

    ClosedPeriodTransactions is a Panel
        business class is CashLedgerTransaction
            list is ClosedPeriodTransactionsList

    TransactionInterface is a Panel
        business class is CashLedgerTransactionImport
            list is CashLedgerTransactionImportList

    CashReceiptInterface is a Panel
        business class is CashLedgerReceiptImport
            list is CashLedgerReceiptImportList

    PaymentInterface is a Panel
        business class is CashLedgerPaymentImport
            list is CashLedgerPaymentImportList

    ReturnedPaymentInterface is a Panel
        business class is CashLedgerReturnedPaymentImport
            list is CashLedgerReturnedPaymentImportList

    InactiveCashManagementAccounts is a Panel
        business class is CashManagementAccount
            list is InactiveCashManagementAccountsList

    ProcessingErrors is a Panel
        business class is CashLedgerTransactionError
            list is CashLedgerErrorsList

    JobResults is a Panel
        business class is CashLedgerJobResult
            list is CashLedgerJobResultsPrimaryList

    OpenTransactionsPreviouslyStaleDated is a Panel
        business class is CashLedgerTransaction
            list is OpenTransactionsPreviouslyStaleDated

