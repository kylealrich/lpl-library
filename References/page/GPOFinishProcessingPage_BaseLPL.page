GPOFinishProcessingPage is a Page
    title is "ProcessingToFinish"

    UpdatedTiers is a Panel
        business class is ContractImportTierMember
            list is ContractImportTierMembersToBeUpdated

    UpdatedVendorItemsByContract is a Panel
        business class is ContractImport
            list is UpdatedAndMismatchedVendorItems

    UpdatedVendorItemsByLine is a MultiPanePanel
        pane 1
            business class is ContractLineImportDistributor
                list is UpdatedVendorItems
        pane 2
            business class is ContractLineImport
                list is UpdatedVendorItems

    Discontinued is a Panel
        business class is ContractImport
            list is ContractsWithNoGPOItemData

    DistributorMarkupChanges is a Panel
        business class is ContractImportDistributor
            list is DistributorMarkupChanges

    SupplierChanges is a MultiPanePanel
        pane 1
            business class is ContractImport
                list is ChangedManufacturerSuppliers
        pane 2
            business class is ContractImportDistributor
                list is ChangedDistributorSuppliers

    DistributorContractsToCreate is a Panel
        business class is ContractImportDistributor
            list is DistributorsToCreateContracts

