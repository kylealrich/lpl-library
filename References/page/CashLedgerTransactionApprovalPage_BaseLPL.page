CashLedgerTransactionApprovalPage is a Page
    title is "ManageTransactionApprovals"
    show panel control on left

    Payments is a Panel
        business class is CashLedgerPayment
            list is PendingCashLedgerPaymentsList

    CashReceipts is a Panel
        business class is CashLedgerCashReceiptHeader
            list is PendingCashReceiptsList

    ElectronicFundsTransferTransactions is a Panel
        business class is CashLedgerElectronicFundsTransferTransaction
            list is PendingElectronicFundsTransfersList

    Transactions is a Panel
        business class is CashLedgerTransaction
            list is PendingCashLedgerTransactionsList

    PayablesPayments is a Panel
        business class is CashLedgerPayablesPayment
            list is PendingCashLedgerPayablesPaymentsList

    ReturnedPayments is a Panel
        business class is CashLedgerReturnedPayment
            list is PendingCashLedgerReturnedPaymentList

    BatchApprovals is a Panel
        business class is CashLedgerBatchApproval
            list is PendingCashLedgerBatchApprovalsList

