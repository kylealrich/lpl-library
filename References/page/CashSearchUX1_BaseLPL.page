CashSearchUX1 is a Page
    title is "Search"
    implements MenuSearch

    Transactions is a Panel
        business class is CashLedgerTransaction
            list is CashLedgerTransactionsByIdentifierList

    Payments is a Panel
        business class is CashLedgerPayment
            list is CashLedgerPaymentsByIdentifier

    PayablesPayments is a Panel
        business class is CashLedgerPayablesPayment
            list is PayablesPaymentsByIdentifierList

    ElectronicFundsTransferTransactions is a Panel
        business class is CashLedgerElectronicFundsTransferTransaction
            list is ElectronicFundsTransferByIdentifierList

    CommitmentDetails is a Panel
        business class is CashLedgerGLDistribution
            list is CommitmentDetails

    OpenBankStatements is a Panel
        business class is BankStatement
            list is OpenBankStatementList

    IntradayBankStatements is a Panel
        business class is BankStatement
            list is IntradayBankStatementList

    AllBankStatements is a Panel
        business class is BankStatement
            list is AllBankStatementsList

