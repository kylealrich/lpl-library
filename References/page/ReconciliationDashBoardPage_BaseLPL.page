ReconciliationDashBoardPage is a Page
    title is "ReconciliationStatus"
    is report for ApplicationAdministrator,ReconciliationManager,Reconciliation

    OpenReconciliations is a Panel
        business class is PeriodEndReconciliation
            list is AllOpenMgrViewReconciliations

    OpenReconciliationsByStructure is a Panel
        business class is ReconciliationAccountStructure
            list is ReconciliationAccountStructureCardOpenList

    OpenReconciliationsByResource is a MultiPanePanel
        pane 1
            business class is FinanceResource
                list is ReconciliationResourceCard
        pane 3
            business class is pane 1.ReconciliationFinanceResourceRel
                list is ByStatusReconciliationGraphList
        pane 4
            business class is pane 3.AllReconciliationByStatusRel
                list is ReconciliationsByResourceChartList

    AllReconciliationsByPeriod is a Panel
        business class is ReconciliationAccountStructure
            list is ReconciliationAccountStructureCardPeriodList

    ReconcilingItems is a Panel
        business class is ReconciliationAccountStructure
            list is ReconciliationAccountStructureCardReasonList

    OpenBankReconciliations is a Panel
        business class is BankStatement
            list is BankStatementReconciliationDashBoard

