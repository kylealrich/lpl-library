MobileSupplyChainUploadsPage is a Page
    title is "ParCount"

    ParLevelUpdates is a Panel
        business class is ParLevelUpdateUpload
            list is ParLevelUpdateUploadList

    ParCounts is a Panel
        business class is MobileSupplyChainParCount
            list is ParCountList

    PickForPar is a Panel
        business class is PickForParTicket
            list is PickForParTickets

