GPOAuditRecords is a Page
    title is "HistoricalUpdatesAndAdditions"
    show panel control on left

    TierChanges is a Panel
        business class is ContractTierMember
            list is TierChanges

    NewLines is a Panel
        business class is ContractLine
            list is NewLines

    NewMembers is a Panel
        business class is ContractTierMember
            list is NewMembers

    SupplierChanges is a Panel
        business class is ContractVendorChange
            list is GPOSupplierChanges

    VendorItemChanges is a Panel
        business class is ContractLine
            list is VendorItemChanges

    ContractDateChanges is a Panel
        business class is ContractImport
            list is DateChanges

    DistributorMarkupChanges is a Panel
        business class is ContractDistributorPricing
            list is MarkupChanges

