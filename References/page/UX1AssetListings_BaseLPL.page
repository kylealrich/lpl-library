UX1AssetListings is a Page
    title is "Listings"
    implements MenuSearch

    Additions is a Panel
        title is "Assets"
        business class is AssetBook
            list is AssetBookDetail
                title is "Assets"

    Adjustments is a Panel
        business class is AssetAdjustment
            list is AssetAdjustmentDashBoardList

    AdjustmentList is a Panel
        business class is AssetAdjustment
            list is AssetAdjustmentList

    Transfers is a Panel
        business class is AssetTransfer
            list is NewAssetTransfers

    TransferList is a Panel
        business class is AssetTransfer
            list is AssetTransferList

    Disposals is a Panel
        business class is AssetDisposal
            list is AssetDisposals

    DisposalList is a Panel
        business class is AssetDisposal
            list is AssetDisposal

    DisposedAssets is a Panel
        title is "DisposedAssets"
        business class is Asset
            list is AllDisposedAssets
                title is "DisposedAssets"

    DisposedBooks is a Panel
        title is "DisposedBooks"
        business class is AssetBookDisposal
            list is AssetBookDisposalListView
                title is "DisposedBooks"

    Revaluations is a Panel
        business class is Asset
            list is RevaluedAssets

    Impairments is a Panel
        business class is AssetImpairmentBook
            list is AssetImpairmentBooks

    Reinstatements is a Panel
        business class is AssetReinstatement
            list is AssetReinstatementList

    AssetCatalog is a Panel
        business class is AssetBook
            list is AssetCatalogue

    DisposalProperties is a Panel
        business class is DisposalProperty
            list is DisposalPropertyList

    AssetItems is a Panel
        business class is AssetItem
            list is AssetItemReport

    SummaryGroups is a Panel
        business class is AssetSummaryGroup
            list is AssetSummaryGroupList

    PODetails is a Panel
        business class is PurchaseOrderLineAsset
            list is PurchaseOrderLineAssetDetailList

