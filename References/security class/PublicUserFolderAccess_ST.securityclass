PublicUserFolderAccess_ST is a SecurityClass

	Access Rights

		la Module
			is accessible
				for all actions
				unconditionally

		UserFolder BusinessClass
			is accessible
				for all actions
				when (Actor=blank)

		UserFolderItem BusinessClass
			is accessible
				for all actions
				when (UserFolder.Actor=blank)

		UserFolderManager BusinessTask
			is accessible
				for all actions
				unconditionally						

