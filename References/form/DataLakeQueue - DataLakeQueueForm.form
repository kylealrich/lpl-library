DataLakeQueueForm is a Form
    is primary
    Layout
        single column
            visible when (Type.Stream)
                DataLakeQueue
                    label is "CorrelationId"
            visible when (FromRepSet)
                ReplicationSet
                RepSetBC
            DocumentName
            DocumentSize
            Instances
            Type
                label is "IngestionType"
            PreEncoded
            visible when (DeveloperMode)
                Document
                visible when (PreEncoded)
                    UnCompressedMessage
            Status
            ExecuteTime
                label is "LastAttempt"
            ErrorCode
            ErrorMessage
