ExpenseManagementInterfaceHeader_LastName_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.ExpenseManagementInterfaceHeader
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_LastName)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.LastName
                label is untranslatable:"Last Name"
            LastName
                label is untranslatable:"Translated"
