EditReportDetailsForm is a Form
    Layout
        single column
            Name
            paragraph
                Sharing
                    show up to 50 characters
                visible when (Sharing.Group)
                    ActorGroup
                        select is ActorGroupHierarchyWithCountsSelectList
                    visible when (ActorGroup entered)
                        ActorGroup.GroupMemberExpandedRollupCount
                            label is "Count"
                            display as tag
                                color of purple
                visible when (Sharing.Role)
                    Role
                visible when (Sharing.RoleGroup)
                    RoleGroup
            visible when (not IsPage)
                Search
                    label is "Filter"
                    raise blue alert when (true)
                        mouse over text is "ThisFilterIsAddedToTheList_LPL_Instance_Selection"
        single column
            paragraph
                PDFPageOptions
                PDFOrientation
            PDFFontOffset
            visible when (IsListReportOrPage)
                RetentionDuration
                    mouse over text is "DefaultDocumentRetentionDuration,InDays,ForThisReport"
            paragraph
                DataSource
                    show up to 50 characters
                visible when (DataSource.AsOfDate)
                    AsOfDate
                ViewArchiveData
