ManualApprove is an ActionForm
    action is PendingApproval.ManualApprovePlus
    Layout
        header3 of "EnterYourReasonCodeAndComment"
        visible when (Company.VendorGroup.RequireReasonCodeForManualInvoiceApproval)
            paragraph
                reason code
                    display as required when (Company.VendorGroup.RequireReasonCodeForManualInvoiceApproval)
                reason code.Description
                    no label
                    display as text
        single column
            PrmComment
                label is "Comment"
                use text area of 10 lines
                display as required when (Company.VendorGroup.RequireReasonCodeForManualInvoiceApproval)
