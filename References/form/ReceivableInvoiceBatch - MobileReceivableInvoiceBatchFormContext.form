MobileReceivableInvoiceBatchFormContext is a Form
    title is "InvoiceBatch"
    Layout
        single column
            Company
                display as text
                select is ReceivableCompanyList
            Company.Name
                label is "CompanyName"
                display as text
        two column
            Company.Currency
                display as text
            visible when (OriginalCurrency entered)
                OriginalCurrency
                    display as text
        single column
            visible when (BackgroundProcessing.ReleaseInProcess)
                BackgroundProcessing
                    label is "BackgroundProcessing...RefreshToViewProgress"
                    display as tag
                        color of red
            visible when (BackgroundProcessing.ReleaseFailure)
                BackgroundProcessing
                    label is "BackgroundProcessingFinishedWithErrors"
                    display as tag
                        color of red
        paragraph
            ReceivableInvoiceBatch
                label is "InvoiceBatch"
                display as text
            visible when (DoesExist)
                Status
                    display as text
