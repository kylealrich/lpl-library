ParLevelUpdateUploadComposite is a CompositeForm
    title is "ParUpdate"
    context form is ParLevelUpdateUploadContext
        background is theme color
    Actions
        action is previous record
            label is "_"
        action is next record
            label is "_"
        action is refresh
            label is "_"
    ParUpdateLines is a Panel
        list is ParLevelUpdateLineUpload set.primary
