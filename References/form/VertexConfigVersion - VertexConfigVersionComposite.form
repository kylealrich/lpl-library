VertexConfigVersionComposite is a CompositeForm
    is primary
    context form is VertexConfigVersion
    show panel control on left
    OAuthSetupParameters is a Panel
        visible when (VertexAccessUsingOAuth)
        form is OAuthSetupForm
    TaxLogHeader is a Panel
        visible when (TaxLogHeaderRelExist)
        list is TaxLogHeaderRel.TaxLogHeaderShortList
    ValidationAddress is a Panel
        form is ValidationAddressForm
