SubcontractorContact_ContactTitle_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.SubcontractorContact
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_ContactTitle)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.ContactTitle
                label is untranslatable:"Contact Title"
            ContactTitle
                label is untranslatable:"Translated"
