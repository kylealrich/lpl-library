MobileVendorLocationForm is a CompositeForm
    title is "VendorLocation"
    context form is MobileVendorLocationContext
    restrict action Delete
    restrict action CopyVendorLocation
    Actions
        action is save
        action is previous record
        action is next record
        action is refresh
    Main is a Panel
        form is MobileMain
    Options is a Panel
        form is MobileOptions
