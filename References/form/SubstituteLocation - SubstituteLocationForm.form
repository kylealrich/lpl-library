SubstituteLocationForm is a Form
    is primary
    title is "SubstituteLocation"
    Layout
        paragraph
            visible when (not SequenceExists)
                Company
                    show up to 10 characters
                Company.Name
                    no label
                    display as text
                    show up to 80 characters
            visible when (SequenceExists)
                Company.RepresentativeText
                    display as text
        paragraph
            visible when (not SequenceExists)
                InventoryLocation
                    show up to 10 characters
                InventoryLocation.Name
                    no label
                    display as text
            visible when (SequenceExists)
                InventoryLocation.RepresentativeText
                    label is "Location"
                    display as text
        paragraph
            SubLocation
                label is "SubstituteLocation"
                show up to 10 characters
            SubLocation.Name
                no label
                display as text
        visible when (SequenceExists)
            SubstituteLocation
                label is "Sequence"
                display as text
                show up to 10 characters
