ProcessPendingAdjustments is an ActionForm
    action is ProcessPendingAdjustments
    Layout
        paragraph
            visible when (not Company.CashOperatorRequired)
                text of "Continue?"
            visible when (Company.CashOperatorRequired)
                Operator
                    label is "Operator"
