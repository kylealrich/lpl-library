ConvertDistributions is an ActionForm
    action is ConvertDistributions
    Layout
        single column
            PrmRunGroup
                label is "RunGroup"
        paragraph
            PrmCompany
                label is "Company"
            PrmCompany.Name
                no label
                display as text
