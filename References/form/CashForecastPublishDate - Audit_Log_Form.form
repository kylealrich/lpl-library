Audit_Log_Form is a Form
    is primary
    propagate as a drill
    Layout
        header1 of untranslatable:"Changes"
        single column
            audit period
            applied stamp
            effective date
            actor
            authenticated actor
            action
            subject
            reason code
            effective time zone
            action tag
            action comment
        visible when (CashManagementGroup has audit change)
            two column
                old CashManagementGroup
                CashManagementGroup
                    no label
        visible when (CashForecast has audit change)
            two column
                old CashForecast
                CashForecast
                    no label
        visible when (CashForecastPublishDate has audit change)
            two column
                old CashForecastPublishDate
                CashForecastPublishDate
                    no label
        visible when (Description has audit change)
            two column
                old Description
                Description
                    no label
        visible when (PublishType has audit change)
            two column
                old PublishType
                PublishType
                    no label
        visible when (PublishDateRange.Begin has audit change)
            two column
                old PublishDateRange.Begin
                PublishDateRange.Begin
                    no label
        visible when (PublishDateRange.End has audit change)
            two column
                old PublishDateRange.End
                PublishDateRange.End
                    no label
