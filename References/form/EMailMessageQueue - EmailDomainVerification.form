EmailDomainVerification is a Form
    Layout
        three column
            NonVerifiedEmailAddress
                label is "Non\\-verifiedEmailAddress"
                display as text
            visible when (NonVerifiedFallbackAddress entered)
                NonVerifiedFallbackAddress
                    label is "Non-verifiedFallbackAddress"
                    display as text
            VerifiedDomains
                label is "VerifiedDomains"
                display as text
        two column
            NonVerifiedBusinessClass
                label is "BusinessClass"
                display as text
            NonVerifiedKeys
                label is "BusinessClassKeys"
                display as text
        two column
            create stamp.actor
                label is "RequestingActor"
                display as text
            visible when (ActorToNotify entered)
                ActorToNotify
                    label is "ActorToNotify"
                    display as text
