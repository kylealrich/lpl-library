WorkunitMetricForm is a Form
    title is "WorkUnitMetricForm"
    Layout
        four column
            MemoryAllocationAsString
                raise red alert when (MemoryAllocationRedAlert)
                    mouse over text is "<MemoryAllocationDifferenceAsString>"
                raise yellow alert when (MemoryAllocationYellowAlert)
                    mouse over text is "<MemoryAllocationDifferenceAsString>"
                raise green alert when (MemoryAllocationGreenAlert)
                    mouse over text is "<MemoryAllocationDifferenceAsString>"
                raise blue alert when (MemoryAllocationBlueAlert)
                    mouse over text is "<MemoryAllocationDifferenceAsString>"
            CpuTimeAsString
                raise red alert when (CpuTimeRedAlert)
                    mouse over text is "<CpuTimeDifferenceAsString>"
                raise yellow alert when (CpuTimeYellowAlert)
                    mouse over text is "<CpuTimeDifferenceAsString>"
                raise green alert when (CpuTimeGreenAlert)
                    mouse over text is "<CpuTimeDifferenceAsString>"
                raise blue alert when (CpuTimeBlueAlert)
                    mouse over text is "<CpuTimeDifferenceAsString>"
            ElapsedTimeAsString
                raise red alert when (ElapsedTimeRedAlert)
                    mouse over text is "<ElapsedTimeDifferenceAsString>"
                raise yellow alert when (ElapsedTimeYellowAlert)
                    mouse over text is "<ElapsedTimeDifferenceAsString>"
                raise green alert when (ElapsedTimeGreenAlert)
                    mouse over text is "<ElapsedTimeDifferenceAsString>"
                raise blue alert when (ElapsedTimeBlueAlert)
                    mouse over text is "<ElapsedTimeDifferenceAsString>"
            ActivityCountDisplay
                raise red alert when (ActivityCountRedAlert)
                    mouse over text is "<ActivityCountDifferenceAsString>"
                raise yellow alert when (ActivityCountYellowAlert)
                    mouse over text is "<ActivityCountDifferenceAsString>"
                raise green alert when (ActivityCountGreenAlert)
                    mouse over text is "<ActivityCountDifferenceAsString>"
                raise blue alert when (ActivityCountBlueAlert)
                    mouse over text is "<ActivityCountDifferenceAsString>"
            Bucket
                display only
            CreateTimeStamp
                display only
            ProcessVersion
                display only
