PaymentLoadMaintenance is a CompositeForm
    is primary
    title is "PaymentInterfaceMaintenance"
    context form is PaymentLoadMaintenanceContext
        background is theme color
    Main is a Panel
        form is Main
    Payee is a Panel
        form is Payee
    Defaults is a Panel
        form is Defaults
    Bank is a Panel
        form is Bank
    RemittanceComment is a Panel
        title is "Remittance"
        form is RemittanceComment
    TransactionData is a Panel
        form is TransactionData
    UserFieldsPanel is a Panel
        title is "UserFields"
        form is UserFieldForm
    Distribution is a Panel
        form is Distribution
