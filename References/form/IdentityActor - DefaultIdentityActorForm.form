DefaultIdentityActorForm is a Form
    is primary
    title is "ActorIdentity"
    Layout
        single column
            Service
                select is PrimaryServiceList
            Identity
            Actor
