EnableDBSessionDebugActionForm is an ActionForm
    action is EnableDBSessionDebug
    title is "EnableDatabaseTierSessionDebug"
    Layout
        single column
            EnableFor
            DBSessionDebugLevel
                label is "Detail"
                display as radio buttons
                when value changed
                    refresh DBSessionDebugLevel
            visible when (not DBSessionDebugLevel.Minimal)
                LPLTrace
