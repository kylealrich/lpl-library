StructureDefaultCodeContextForm is a Form
    title is "StructureDefaultCode"
    Layout
        single column
            header3 of "<DerivedDescription>"
            paragraph
                valid when (DirtyCode)
                    valid when (DirtyCode)
                        button of "ApplyChanges"
                            valid when (DirtyCode)
                            action is UpdateOrderArrays
                visible when (DirtyCode)
                    text of "<DerivedDescription>Updated.Click_\\Apply_\\ChangesButtonToUpdateTheHierarchy."
                        color of red
            text of "<CommittedContractMessage>"
                bold
