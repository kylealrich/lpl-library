WarehouseShipmentOut_BillToName_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.WarehouseShipmentOut
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_BillToName)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.BillToName
                label is untranslatable:"Bill To Name"
            BillToName
                label is untranslatable:"Translated"
