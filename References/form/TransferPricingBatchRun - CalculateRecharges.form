CalculateRecharges is an ActionForm
    action is CalculateRecharges
    Layout
        single column
            PrmRechargeOption
            visible when (PrmRechargeOption.RechargeType)
                PrmRechargeType
            visible when (PrmRechargeOption.Single)
                PrmRecharge
                    select is TransferPricingActiveRechargesList
            visible when (PrmRechargeOption.Group)
                PrmCustomGroup
            PrmDescription
                display as required when (true)
            PrmDate
                display as required when (true)
