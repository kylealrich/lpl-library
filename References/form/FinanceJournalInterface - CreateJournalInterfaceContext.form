CreateJournalInterfaceContext is a Form
    title is "JournalInterface"
    Layout
        single column
            FinanceJournalInterface
                display as text
        two column
            RunGroup
            PostingDate
                label is "DefaultCashDistributionsPostingDate"
        single column
            visible when (FinanceGroup.JournalEntryApprovalRequired)
                ApprovalCode
        single column
            Status
                display as text
