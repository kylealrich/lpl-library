RecallTaskInstructionQuestion is a Form
    title is "TaskInstructionQuestion"
    Layout
        single column
            ResponseType
        paragraph
            visible when (HasAttachment)
                Attachment.Title
            Attachment.File
                label is "Attachment"
        single column
            AllowResponseAttachment
        single column
            visible when (ResponseType.Text)
                ResponseRules
            visible when (ResponseType.Number)
                ResponseRules
            visible when (ResponseType.Date)
                ResponseRules
            visible when (ResponseType.List)
                ResponseRules
            visible when (ResponseType.YesNoText)
                YesNoTextResponseRules
            visible when (ResponseType.YesNo)
                YesNoResponseRules
