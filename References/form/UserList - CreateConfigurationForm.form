CreateConfigurationForm is an ActionForm
    action is CreateConfiguration
        on completion navigate to ConfigConsoleNav
    title is "ConfigureList"
    Layout
        single column
            BusinessView
                select is BusinessClassSelectList
            List
                select is ConfigureSelectList
