ParsedData is a Form
    Layout
        single column
            visible when (ParsedData1Used)
                ParsedData1
            visible when (ParsedData2Used)
                ParsedData2
            visible when (ParsedData3Used)
                ParsedData3
            visible when (ParsedData4Used)
                ParsedData4
            visible when (ParsedData5Used)
                ParsedData5
            visible when (ParsedData6Used)
                ParsedData6
            visible when (ParsedData7Used)
                ParsedData7
            visible when (ParsedData8Used)
                ParsedData8
            visible when (ParsedData9Used)
                ParsedData9
            visible when (ParsedData10Used)
                ParsedData10
