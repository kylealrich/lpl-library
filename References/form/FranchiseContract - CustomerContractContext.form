CustomerContractContext is a Form
    Layout
        three column distributed
            column1
                paragraph
                    visible when (not RecordExists)
                        Company
                    visible when (RecordExists)
                        DerivedCompany
                            display as text
                paragraph
                    visible when (not RecordExists)
                        Customer
                            select is CustomerContextCompanySelectList
                    visible when (RecordExists)
                        DerivedCustomer
                            display as text
            column2
                paragraph
                    visible when (not RecordExists)
                        FranchiseContract
                            label is "Contract"
                    ProcessLevel
                paragraph
                    visible when (not RecordExists)
                        CustomerShipTo
                            label is "ShipTo"
                    visible when (RecordExists)
                        DerivedShipTo
                            display as text
            column3
                paragraph
                    Currency
                paragraph
                    visible when (RecordExists)
                        ActiveStatus
                            label is "Status"
                            display as tag
                                color of gray when (not Active)
                                color of green when (Active)
