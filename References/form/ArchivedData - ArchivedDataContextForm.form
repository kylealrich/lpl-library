ArchivedDataContextForm is a Form
    title is "ArchivedData"
    Layout
        two column
            ArchivedData.PurgedBusinessClass
            ParentBusinessClass
        single column
            ArchivedData.Sequence
            visible when (IsUnderScoreBusinessClass)
                RecordCount
            PurgeActionResultUniqueID
            PurgeActionResultBusClassUniqueID
        paragraph
            SentToArchive
            visible when (LastRestoredStamp entered)
                LastRestoredStamp
        single column
            visible when (HasError)
                ErrorMessage
            button of "View"
                visible when (HasDocument)
                view DecodedJSONData
