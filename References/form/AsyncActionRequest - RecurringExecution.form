RecurringExecution is a Form
    title is "ScheduledRuns"
    Layout
        single column
            TimeToExec
                label is "NextRun"
                display only
                raise blue alert when (LastTimeToExec entered)
                    mouse over text is "ScheduleEndDate:<LastTimeToExec>"
        single column
            visible when (NextScheduledExecution1 entered)
                NextScheduledExecution1
                    no label
            visible when (NextScheduledExecution2 entered)
                NextScheduledExecution2
                    no label
            visible when (NextScheduledExecution3 entered)
                NextScheduledExecution3
                    no label
            visible when (NextScheduledExecution4 entered)
                NextScheduledExecution4
                    no label
            visible when (NextScheduledExecution5 entered)
                NextScheduledExecution5
                    no label
