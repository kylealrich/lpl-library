MBillingInvoiceComments is a Form
    title is "Comments"
    Actions
        action is refresh
            label is "_"
        action is save
            label is "_"
            hide when invalid
        action is previous record
            label is "_"
            hide when invalid
        action is next record
            label is "_"
            hide when invalid
    Layout
        two column distributed
            Company
                display as text
            BillingInvoiceComments
                label is "Sequence"
                display as text
        single column
            visible when (BillingInvoiceLine entered)
                BillingInvoiceLine
        two column distributed
            display as text
                BillingInvoice.DerivedDisplayBillingInvoice
                    label is "BillingInvoice"
        single column
            PrintCode
            CommentText
