AdditionalPanel is a Form
    title is "Main"
    Layout
        display as text
            single column
                Status
                    label is "Status"
                CreationDate
                    label is "RequestDate"
                visible when (RequestedDeliveryDate entered)
                    RequestedDeliveryDate
                visible when (DerivedPreviousApprover entered)
                    DerivedPreviousApprover
                        label is "PreviousApprover"
