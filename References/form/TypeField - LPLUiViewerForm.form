LPLUiViewerForm is a Form
    title is "LPLFor<TypeField>"
    Layout
        single column
            TypeField
                no label
                header2
                display as text
                raise blue alert when (IsKeyField)
                    mouse over text is "Key_Field"
            UiLPL
                no label
                display only
                use text area of 30 lines
