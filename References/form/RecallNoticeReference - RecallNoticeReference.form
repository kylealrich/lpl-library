RecallNoticeReference is a Form
    is primary
    title is "Reference"
    Layout
        paragraph
            RecallNotice
                display as text
            RecallNotice.Name
                no label
                display as text
        single column
            Description
                use text area
        two column
            IdentifierId
            UseAsWorkingId
        paragraph
            Link
            visible when (not HasLink)
                text of "<RecallGroup.LinkExampleText>"
            visible when (HasLink)
                button of "ViewLink"
                    link is external "<Link>"
                    display as link
                DisplayLinkOnTask
        paragraph
            visible when (HasAttachment)
                Attachment.Title
            Attachment.File
                label is "Attachment"
            visible when (HasAttachment)
                DisplayAttachmentOnTask
        single column
            ReferenceDate
