IONConnection is a Form
    is primary
    title is "IONConnection"
    Layout
        single column
            column1
                header1 of "Connection"
                    show border color of black
                Description
                    label is "Connection"
                    left align
                header1 of "Status"
                    show border color of black
                Status
                    label is "Status"
                    display only
                    raise red alert when (Alert.Red)
                        mouse over text is "<AlertMessage>"
                    raise yellow alert when (Alert.Yellow)
                        mouse over text is "<AlertMessage>"
                    raise green alert when (Alert.Green)
                        mouse over text is "<AlertMessage>"
                ErrorCode
                StatusMessage
                    display only
                    use text area of 4 lines
                visible when (HasSupportedVersions)
                    SupportedVersions
                        display only
                visible when (HasVersionInformation)
                    Version
                        display only
                LogicalId
                    display only
                OAuth
                    display only
