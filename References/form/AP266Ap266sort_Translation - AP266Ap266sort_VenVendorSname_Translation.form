AP266Ap266sort_VenVendorSname_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.UniqueID
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_VenVendorSname)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.VenVendorSname
                label is untranslatable:"Ven Vendor Sname"
            VenVendorSname
                label is untranslatable:"Translated"
