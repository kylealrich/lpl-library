UpgradeWithStagingEnvironment is a CompositeForm
    is primary
    propagate as a drill
    use for action Create
    show panel navigation
    Actions
        action is refresh
    PreUpgradeSteps is a Panel
        title is "Pre-UpgradeSteps"
        completed when (PreUpgradeStepsCompleted)
        form is PreUpgradeStepsForm
    Step1UpgradeStaging is a Panel
        title is "Step1:UpgradeStaging"
        valid when (PreUpgradeStepsCompleted)
        completed when (IsUpgradeStagingIsCompleted)
        form is UpgradeStaging
    Step2MergeCustomization is a Panel
        title is "Step2:MergeCustomization"
        valid when (IsUpgradeStagingIsCompleted)
        completed when (IsMergeCustomizationIsCompleted)
        form is MergeCustomizations
    Step3LoadDataToStagingEnvironment is a Panel
        title is "Step3:LoadDataToStagingEnvironment"
        valid when (IsMergeCustomizationIsCompleted)
        completed when (IsLoadDataToStagingIsCompleted)
        form is LoadDataToStaging
    Step4ValidateStaging is a Panel
        title is "Step4:ValidateStaging"
        valid when (IsLoadDataToStagingIsCompleted)
        completed when (IsDoneValidatingStageEnvironment)
        form is ValidateStaging
    Step5StageToActiveSpaces is a Panel
        title is "Step5:StageToActiveSpaces"
        valid when (IsDoneValidatingStageEnvironment)
        completed when (IsStageToActiveIsCompleted)
        form is StageToActiveSpaces
    UpgradeStatus is a Panel
        title is "UpgradeStatus"
        valid when (IsStageToActiveIsCompleted)
        completed when (IsStageToActiveIsCompleted)
        form is UpgradeCompleted
