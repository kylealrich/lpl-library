NCUAConfigurationForm is a Form
    title is "NCUAConfiguration"
    Layout
        four column
            PfiApprovalFlow
                when value changed
                    refresh LinkBackUrl
                    refresh IsLinkBackUrlEmpty
            PfiUserActionActivity
            Enabled
        two column
            visible when (not IsLinkBackUrlEmpty)
                button of "View"
                    link is external "<LinkBackUrl>"
                    label is "ViewProcess"
            visible when (false)
                LinkBackUrl
        three column
            Priority
            PriorityVariable
            Category
