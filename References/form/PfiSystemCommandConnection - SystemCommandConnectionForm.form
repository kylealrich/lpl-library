SystemCommandConnectionForm is a Form
    title is "SystemCommandConnection"
    Layout
        single column
            PfiConfiguration
        header2 of "LSFConnectionParameters"
        single column
            Remote
            valid when (Remote)
                WebRoot
                text of "Example:_https://example.\\com:\\8443"
                    italics
            valid when (Remote)
                User
            valid when (Remote)
                Password
            valid when (Remote)
                Timeout
            valid when (Remote)
                button of "TestConnection"
                    action is TestConnection
        header2 of "LSFVariables"
        single column
            GenDir
            LawDir
        header2 of "CommandExecutionParameters"
        single column
            CmdUser
            CmdPassword
            CmdTimeout
