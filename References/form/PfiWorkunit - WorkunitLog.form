WorkunitLog is a Form
    title is "WorkUnitLog"
    Layout
        single column
            LogSizeDisplay
        three column
            visible when (IsS3DirectDownload)
                button of "Download"
                    link is external "<S3LogDownloadURL>"
                S3LogDownloadMessage
            visible when (IsDBDirectDownload)
                button of "Download"
                    action is DownloadLogFile
            visible when (IsLogDownloading)
                text of "LogIsDownloading...PleaseRefreshThePageInAFewSeconds"
            visible when (IsThereLogRemainingTime)
                DownloadedLogRemainingTimeInMinutes
            visible when (NoLogRemainingTime)
                text of "DownloadedLogCopyWillBeDeletedSoon"
        single column
            visible when (CanDisplayLog)
                button of "Open"
                    show editor for LogAttachment
                        using mime type "text/plain"
                LogDisplay
                    display as text
