EFTRejectUpdate is an ActionForm
    action is EFTRejectUpdate
    title is "EFTRejectUpdate"
    Layout
        single column
            PrmCashManagementGroup
                label is "CashManagementGroup"
        three column
            PrmCompany
                label is "Company"
                select is Company
            PrmCompany.Name
                no label
                display as text
            PrmCompany.Currency
                no label
                display as text
        two column
            PrmCompanyGroup
                label is "CompanyGroup"
                select is CompanyGroups
        blank line
        three column
            PrmCashCode
                label is "CashCode"
                select is CashCode
            PrmCashCode.Description
                no label
                display as text
            PrmCashCode.Currency
                no label
                display as text
        two column
            PrmCashCodeGroup
                label is "CashCodeGroup"
                select is CashCodeGroup
            PrmCashCodeGroup.Description
                no label
                display as text
        blank line
        single column
            PrmRejectCode
                label is "RejectPaymentCode"
                select is BankDebitTransactionCodes
