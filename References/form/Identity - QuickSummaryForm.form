QuickSummaryForm is a SummaryForm
    title is "QuickIdentitySummary"
    Layout
        single column
            Service
                select is PrimaryServiceList
            Identity
            ServiceType
        visible when (IsDatabaseIdentity)
            DatabaseIdentityProperties
        visible when (IsFormBasedIdentity)
            FormBasedIdentityProperties
        visible when (IsMISocketIdentity)
            MISocketIdentityProperties
        visible when (IsUnixIdentity)
            UnixIdentityProperties
        visible when (IsWindowsIdentity)
            WindowsIdentityProperties
        visible when (IsAS400Identity)
            AS400IdentityProperties
        blank line
        single column
            PasswordLastSet
                display only
            IsPasswordResetRequired
