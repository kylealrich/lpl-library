MVenLocationForm is a CompositeForm
    title is "VendorLocation"
    called out actions only
    context form is MVenLocationContext
    restrict action Delete
    restrict action CopyVendorLocation
    Actions
        action is save
            label is "_"
        action is previous record
            label is "_"
        action is next record
            label is "_"
        action is refresh
            label is "_"
    Main is a Panel
        form is VenLocationMobileMain
    Options is a Panel
        form is VenLocationMobileOptions
