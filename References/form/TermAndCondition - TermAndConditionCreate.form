TermAndConditionCreate is a Form
    title is "TermAndCondition"
    Layout
        two column
            Title
            HeaderText
        single column
            Description
                use text area of 10 lines
        two column
            visible when (not ParentTermAndConditionExists)
                Category
                    select is BuyerCategories
                SubCategory
                    select is BuyerSubCategories
        visible when (ParentTermAndConditionExists)
            two column
                Category
                    display as text
                SubCategory
                    display as text
        single column
            AllowModification
            TermDisplayOrder
                label is "DisplayOrder"
                display as text
