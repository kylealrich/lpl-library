BatchTaskContext is a Form
    title is "Job"
    Layout
        two column
            BatchTask
            visible when (WarnPrivateUsedInStream)
                text of "<BatchTask><PrivateStreamResidentWarningMessage>"
                    right of field
                    color of blue
                    align as label
            visible when (WarnPublicUsedInStream)
                text of "<BatchTask><PublicStreamResidentWarningMessage>"
                    right of field
                    color of orange
                    align as label
        single column
            TypeTransientField
            Description
        two column
            Owner
                display as text
            IsPrivate
                display as text
        visible when (TypeTransientField.TaskStream)
            single column
                AsyncTasks
                    label is "StepParallelProcessing"
