AddEmail is an ActionForm
    action is AddEmailForTesting
    Layout
        single column
            EntryType
            visible when (EntryType.EmailAddress)
                EmailPrm
            visible when (EntryType.Employee)
                EmployeePrm
