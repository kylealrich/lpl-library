ClosedStructurePeriodForm is a CompositeForm
    propagate as a drill
    title is "ReconciliationsByPeriod"
    context form is ClosedStructurePeriodContextForm
        background is theme color
    Reconciliations is a Panel
        visible when (ReconciliationAccountStructure.ReconciliationType.Account)
        list is PeriodEndReconciliationHierarchyRel.AllReconciliationByStatus
    NonAccountReconciliations is a Panel
        title is "Reconciliations"
        visible when (ReconciliationAccountStructure.ReconciliationType.NonAccount)
        list is NonAccountPeriodEndReconciliationRel.AllNonAccountReconciliationByStatus
