ActionRequest_StatusNotificationTitle_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.ActionRequest
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_StatusNotificationTitle)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.StatusNotificationTitle
                label is untranslatable:"Status Notification Title"
            StatusNotificationTitle
                label is untranslatable:"Translated"
