LineErrorsComposite is a CompositeForm
    title is "LineErrors"
    context form is ContractContextErrors
        background is theme color
    Actions
        action is CleanUpLineErrors
        action is refresh
    LineErrors is a Panel
        list is UpdateableLineErrorRel.LineErrors
