DataTranslationForm is a Form
    Layout
        single column
            visible when (CanGUIEditProdLine)
                check control
                    valid when (CanGUIEditProdLine)
                    visible when (EntityType.ProductLine)
                    checked state is TEnableTranslations
                    check action is EnableDataTranslation
                    uncheck action is DisableDataTranslation
                    label is "DataTranslation"
                    display as switch
