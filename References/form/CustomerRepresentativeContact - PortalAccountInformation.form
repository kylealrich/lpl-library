PortalAccountInformation is a CompositeForm
    title is "<DerivedCustomerFormTitle>"
    disable field options
    called out actions only
    context form is CustomerContext
        show context form on left
        background is theme color
    restrict action Create
    restrict action CustomerUpdateRequest
    restrict action Delete
    restrict action InactivateContact
    restrict action ResetCustomerContactPassword
    restrict action SelectPaymentMethod
    restrict action Update
    restrict action UpdateMyContactInformation
    restrict action UnwindDeclinedPaymentRequest
    restrict action SendEmailForExternalUserMigrationUsernameChange
    Actions
        action is refresh
            label is "_"
    Main is a DashBoardPanel
        Inactive is a Panel
            visible when (not Active)
            form is ActivityContextForm
        NotValidated is a Panel
            visible when (IsActiveButIsNotValidated)
            form is ActivityContextForm
        CustomerProfile is a Panel
            visible when (IsActiveAndHasBeenValidated)
            form is CustomerProfile
        MultifactorAuthentication is a Panel
            visible when (DerivedMFAEnabled)
            form is RegisteredMFADeviceDisplay
        Balances is a Panel
            visible when (ShowBalances)
            form is CustomerBalance
        Questions is a Panel
            visible when (ShowQuestionResponse)
            overview navigation is CustomerQuestionResponseNav
            list is CustomerRepresentative.CustomerQuestionResponseRels.CustomerQuestionsCardList
        Contacts is a Panel
            visible when (CanUpdateCustomer)
            triple width
            list is CustomerRepresentative.CustomerRepresentativeContact set.CustomerContactCreateUpdate
