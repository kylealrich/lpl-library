StudentActivitySchoolTypeForm is a CompositeForm
    is primary
    title is "<mode>SchoolType"
    context form is StudentActivitySchoolTypeContextForm
    show panel control on left
    Actions
        action is Create
        action is save
        action is save
        action is save and new
        action is next record
        action is previous record
        action is refresh
    Schools is a Panel
        list is StudentActivitySchool set.CurrentPeriodSchoolList
    CurrentAssetAccountBalance is a Panel
        list is StudentActivityAccountRel.CurrentAssetAccountBalanceByTypeList
    CurrentLiabilityAccountBalance is a Panel
        list is StudentActivityAccountRel.CurrentLiabilityAccountBalanceByTypeList
    CurrentRevenueAccountBalance is a Panel
        list is StudentActivityAccountRel.CurrentRevenueAccountBalanceByTypeList
