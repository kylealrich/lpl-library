CustomerRepresentativeContactUpdate is a Form
    title is "CustomerContactInformation"
    Layout
        visible when (not CustomerRepresentative.BillingRecvExtEntityEnabled)
            single column
                LoginName
                    display only
        two column
            MainContact.GivenName
                label is "FirstName"
            MainContact.FamilyName
                label is "LastName"
        paragraph
            TelephoneNumber
        two column
            visible when (not CustomerRepresentative.BillingRecvExtEntityEnabled)
                EmailAddress
            visible when (CustomerRepresentative.BillingRecvExtEntityEnabled)
                paragraph
                    EmailAddress
                        display as text
                    button of "Update" with icon.edit
                        visible when (EmailAddress entered)
                        action is AdminUpdateEmail
                        display as link
            ReceiveEmailNotification
        two column
            PrimaryContact
            DerivedActive
                label is "Active"
                display only
