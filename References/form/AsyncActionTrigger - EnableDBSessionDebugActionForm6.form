EnableDBSessionDebugActionForm6 is an ActionForm
    action is InRetry.EnableDBSessionDebug
    title is "EnableDatabaseTierSessionDebug"
    Layout
        single column
            DBSessionDebugLevel
                label is "Detail"
                display as radio buttons
                when value changed
                    refresh DBSessionDebugLevel
            visible when (not DBSessionDebugLevel.Minimal)
                LPLTrace
