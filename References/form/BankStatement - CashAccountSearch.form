CashAccountSearch is a SearchForm
    title is "AccountSearch"
    Layout
        single column
            CashManagementAccount
                select is CashManagementAccountsForRecon
            StatementStatus
