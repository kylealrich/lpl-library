ConfigBundleSnapshotItem is a Form
    is primary
    title is "SnapshotItem"
    Layout
        two column
            ConfigBundleSnapshot.Name
                label is "Snapshot"
            Independent
        single column
            ConfigSnapshotItemInfo
            Status
            Description
            ConfigValidationStatus
            DeletedRecord
            visible when (HasErrorText)
                ErrorText
            LPL
                use text area of 25 lines
            visible when (HasIndexLPL)
                LPLIndex
                    use text area of 10 lines
            visible when (HasExtendedInfo)
                ExtendedInfo
