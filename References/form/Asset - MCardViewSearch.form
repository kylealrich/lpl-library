MCardViewSearch is a SearchForm
    title is "SearchFields"
    Layout
        single column
            Status
            AssetType
                label is "Type"
                compact format
                select is MAssetTypeList
            Company.GeneralLedgerCompany.Name
                label is "Company"
