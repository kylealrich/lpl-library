UpdateNotification is a Form
    use for action Update
    use for action View
    use for desktop
    use for browser
    Layout
        single column
            SourceType
                label is "Type"
                display only
            visible when (SourceType.Message)
                create stamp.actor
                    label is "From"
            Priority
            Description
                show up to 200 characters
            Details
                use text area of 20 lines
            visible when (not SourceType.Message)
                SourceObject
                    display only
            visible when (SourceType.Document)
                MimeType
                    display only
