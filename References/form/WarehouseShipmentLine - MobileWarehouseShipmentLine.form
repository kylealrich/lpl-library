MobileWarehouseShipmentLine is a CompositeForm
    title is "WarehouseShipmentLine"
    context form is WarehouseShipmentLineContext
    show panel control on left
    restrict action Delete
    Actions
        action is save
            label is "_"
            hide when invalid
        action is Delete
            action icon is delete
            label is "_"
            mouse over text is "Delete"
            hide when invalid
    Main is a Panel
        form is MainForm
    Details is a Panel
        visible when (IsDetailRequired)
        list is WarehouseShipmentLineDetailsRel.Details
    Components is a Panel
        visible when (IsKitItem)
        list is WarehouseShipmentLineComponentsRel.Components
    UserFields is a Panel
        form is UserFields
