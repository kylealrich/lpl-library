EscheatmentOutputFileHeaderContext is a Form
    title is "EscheatmentOutputFileHeader"
    Layout
        display as text
            two column distributed
                column1
                    CashManagementGroup
                    EscheatmentOutputFileHeader
                    CreateDate
                column2
                    TotalRecordCount
                    TotalAmount
                    Status
                        raise red alert when (HasErrors)
                            mouse over text is "ProcessIncomplete;UnresolvedErrorsExist"
                        raise yellow alert when (IsReadyToUpdate)
                            mouse over text is "ProcessIncomplete;UpdateHasNotBeenRunYet"
            paragraph
                DetailFile.Title
                    label is "DetailFile"
                visible when (HasDetailAttachment)
                    button of "View"
                        view DetailFile.File
                        display as link
                DetailOutputFileName
