ProcessFlowQueueContext is a Form
    Layout
        two column
            Type
                display only
            EventType
            Status
                display only
            Name
            visible when (not Type.Execute)
                Title
                ObjectKey
                    display only
                CategoryKey
                CategoryValue
        visible when (Type.Execute)
            single column
                ExecuteAction
                    display only
                ActionReason
                    display only
                ActionComment
                    display only
                QueueTask.BusinessClassName
                    display only
                QueueTask.BusinessObjectKey
                    display only
        visible when (not Type.Execute)
            three column
                FlowCriteria.ProcessFlowCriteria[1]
                    label is "Criteria"
                FlowCriteria.ProcessFlowCriteria[2]
                    no label
                FlowCriteria.ProcessFlowCriteria[3]
                    no label
        two column
            EffectiveDate
            visible when (not Type.Execute)
                WorkUnitID
                    display only
                ActivityID
                    display only
            ExecuteTime
                label is "LastExecuteTime"
                display only
            FailureCount
                display only
        visible when (IsNonTriggerError)
            single column
                ErrorMessage
                    label is "Error"
                    display only
                    use text area of 10 lines
