RemoveSubjectForm is an ActionForm
    action is RemoveSubject
    title is "RemoveSubject"
    Layout
        single column
            SubjectPrm
                select is ConfiguredBusinessSubjectList
