ToleranceErrorComposite is a CompositeForm
    title is "ContractLinePriceChange"
    context form is ToleranceErrorContext
    ToleranceDetails is a Panel
        visible when (ProcessingType.ImportUpdate)
        form is ContractLinePricingToleranceErrors
    CostComparison is a Panel
        visible when (ComparisonOnly)
        form is ContractLinePricingCostComparison
