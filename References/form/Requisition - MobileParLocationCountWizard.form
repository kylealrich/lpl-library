MobileParLocationCountWizard is a CompositeForm
    use for action Unreleased.MobileCreateFromParLocation
    title is "ParLocationCountFor<RequestingLocation>"
    suppress standard toolbar
    show panel navigation
    restrict action Unreleased.Create
    restrict action Unreleased.CreateUsingCopyFromTemplate
    restrict action Unreleased.CreateUsingCopyFromRequisition
    restrict action Unreleased.MobileCreate
    restrict action Unreleased.RSSLPLCreate
    restrict action Unreleased.RSSLPLDelete
    restrict action Unreleased.RSSLPLShop
    restrict action IssueDraft
    restrict action IssueFinal
    restrict action Release
    restrict action Unreleased.Update
    restrict action Unreleased.Delete
    restrict action Unreleased.RSSLPLCopyAllLinesFromRequisition
    Actions
        action is refresh
            label is "_"
            hide when invalid
            valid when (false)
        action is save
            label is "_"
            hide when invalid
            valid when (false)
        action is save and new
            label is "_"
            hide when invalid
            valid when (false)
    ParLocation is a Panel
        mouse over text is "ParLocationCount"
        form is MobileParLocationItems
        save on next
    ParLocationItems is a Panel
        mouse over text is "EnterParLocationCount"
        list is ParLocationsRel.MobileParLocationItems
    ParLocationSummary is a Panel
        action is MobileRelease
