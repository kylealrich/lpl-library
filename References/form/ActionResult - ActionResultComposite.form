ActionResultComposite is a CompositeForm
    is primary
    context form is ActionResultContext
    show panel control on left
    Result is a Panel
        visible when (HasResult)
        form is ActionResultMessage
    ChildResults is a Panel
        valid when (HasChildren)
        list is ChildResults.ActionResultList
    Parent is a Panel
        valid when (HasParent)
        form is ParentInformation
    ReportData is a Panel
        valid when (HasReportData)
        list is ReportDataRel.ReportDatatList
