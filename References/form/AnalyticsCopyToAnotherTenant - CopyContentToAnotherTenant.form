CopyContentToAnotherTenant is a CompositeForm
    is primary
    propagate as a drill
    use for action Create
    show panel navigation
    Actions
        action is refresh
    PreUpgradeSteps is a Panel
        title is "Pre-UpgradeSteps"
        completed when (PreUpgradeStepsCompleted)
        form is PreUpgradeStepsForm
    Step1Setup is a Panel
        title is "Step1:SetupOverview"
        valid when (PreUpgradeStepsCompleted)
        completed when (PreUpgradeStepsCompleted)
        form is SetupToCopyContent
    Step2CopyProcess is a Panel
        title is "Step2:CopyProcess"
        valid when (PreUpgradeStepsCompleted)
        completed when (CopyProcessCompleted)
        form is CopyProcess
    UpgradeCompleted is a Panel
        title is "UpgradeCompleted"
        valid when (CopyProcessCompleted)
        completed when (IsStatusCompleted)
        form is UpgradeCompleted
