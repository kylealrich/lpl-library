MobileContext is a Form
    title is "RequisitionLineContext"
    Layout
        three column table
            column1
                visible when (UseLinkedImage)
                    DerivedImageURL
                        display as image
                visible when (not UseLinkedImage)
                    DerivedPicture
                        no label
                        align as label
                        display as full image up to 6 lines
                            missing image is random background
                                foreground text is "<RequisitionLine.RequisitionLineItemAbbreviation>"
            display as text
                column2
                    Item
                column3
                    Description
        display as text
            paragraph
                DerivedQuantityWithUOM
                    label is "Quantity"
                DerivedUnitCostWithCurrency
                    label is "UnitCost"
                DerivedApprovalValueWithBaseCurrency
                    label is "Total"
