ContractLineCommentAction is an ActionForm
    action is Create
    title is "CreateContractLineComment"
    Layout
        display as text
            paragraph
                Contract.RepresentativeText
                ContractLine
                    label is "Line"
        three column
            visible when (ContractCommentType entered)
                ContractCommentType
                    label is "CommentType"
                    display as text
            CommentUsedFor
            visible when (Contract.UseIDM)
                PrintOnContract
        single column
            CommentTitle
                label is "Title"
            Description
                use text area of 8 lines
            visible when (EligibleForSourcing)
                SSComment
                    label is "SendCommentToSourcing"
            Attachment.File
                label is "Attachment"
        two column
            EmailAttachment
                label is "IncludeAttachmentInEmail"
            SupplierCanView
