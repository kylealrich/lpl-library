ComponentItemsForm is a CompositeForm
    is primary
    title is "ComponentItemLine"
    context form is CompositeItemContext
        background is theme color
    Main is a Panel
        form is Main
    Errors is a Panel
        visible when (ErrorExists)
        list is ComponentItemErrorRel.PreferenceCardErrorList
