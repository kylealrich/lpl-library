ContainerContextForm is a Form
    Layout
        paragraph
            visible when (RecordExists)
                Company.DerivedCompanyDisplay
                    label is "Company"
                    display as text
            visible when (not RecordExists)
                Company
                    label is "Company"
                Company.Name
                    no label
                    display as text
        paragraph
            visible when (not RecordExists)
                Container
            visible when (RecordExists)
                Container
                    display as text
            Description
