MobileSupplyChainLocationForm is a CompositeForm
    is primary
    title is "MobileSupplyChainLocation"
    called out actions only
    context form is MobileSupplyChainLocationContextForm
        background is theme color
    Actions
        action is previous record
            label is "_"
        action is next record
            label is "_"
        action is save
            label is "_"
        action is refresh
            label is "_"
    MobileSupplyChain is a Panel
        form is MobileSupplyChainForm
    MSCMDefaultPrinters is a Panel
        form is MSCMDefaultPrintersForm
