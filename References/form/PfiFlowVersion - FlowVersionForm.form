FlowVersionForm is a Form
    title is "ProcessVersion"
    restrict action Create
        when (PfiFlowDefinition.FlowType.SystemDefined)
    restrict action Update
        when (PfiFlowDefinition.FlowType.SystemDefined)
    restrict action Delete
        when (PfiFlowDefinition.FlowType.SystemDefined)
    restrict action MakeCurrentVersion
        when (PfiFlowDefinition.FlowType.SystemDefined)
    restrict action MakeCurrentVersionOnActionPendingWorkunits
        when (PfiFlowDefinition.FlowType.SystemDefined)
    Layout
        single column
            PfiFlowDefinition
            PfiFlowVersion
            IsStaged
                display only
            visible when (AutoRestartInXmlToggle)
                AutoRestartText
            button of icon.fullview
                visible when (CanViewProcess)
                link is "<IPDWebLinkbackUrl>"
                label is "Open"
            Comment
            FlowXml
                display as text
                use text area of 20 lines
        two column
            visible when (UploadByEntered)
                UploadBy
                    display only
            visible when (UploadByEntered)
                text of "<UploadByName>"
        single column
            visible when (UploadDateEntered)
                UploadDate
                    display only
