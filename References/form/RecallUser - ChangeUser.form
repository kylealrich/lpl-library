ChangeUser is an ActionForm
    action is ChangeUser
    title is "ReassignCoordinators,OrganizationUsers,_andTaskOwners"
    Layout
        single column
            RecallGroup
                display as text
        paragraph
            RecallUser
                label is "FromUser"
                display as text
            RecallUser.Employee.PresentationNameSnapshot
                no label
                display as text
        single column
            visible when (ChangeAll)
                InactivateFromUser
        paragraph
            PrmToUser
                select is Users
            PrmToUser.Employee.PresentationNameSnapshot
                no label
                display as text
        single column
            text of "Reassign:"
                italics
            ChangeAll
            visible when (not ChangeAll)
                ChangeCoordinators
                visible when (ChangeCoordinators)
                    ChangeOpenNotices
                ChangeRespondersWatchersAndReviewers
                ReassignTaskOwners
