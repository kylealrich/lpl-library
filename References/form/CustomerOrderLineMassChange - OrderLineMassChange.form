OrderLineMassChange is a CompositeForm
    is primary
    title is "OrderLineMassChange"
    context form is OrderLineMassChangeContext
        background is theme color
    Main is a Panel
        form is Main
    ShipTo is a Panel
        form is ShipTo
    Sales is a Panel
        form is Sales
    User is a Panel
        form is User
