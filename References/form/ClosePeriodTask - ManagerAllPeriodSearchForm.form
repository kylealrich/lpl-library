ManagerAllPeriodSearchForm is a SearchForm
    Layout
        single column
            TaskName
                label is "Process"
            ClosePeriod
                label is "Period"
                compact format
                select is OpenPeriodSelectList
            TaskStatus
                label is "Status"
