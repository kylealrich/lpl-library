WorkunitRoutingForm is a Form
    title is "WorkUnitRouting"
    Layout
        display as text
            two column
                WorkTitle
                    no label
                    header2
            single column
                Status
                    display as tag
                        color of purple when (Status.Ready)
                        color of yellow when (Status.Waiting)
                        color of green when (Status.Completed)
                        color of red when (Status.Failed)
            paragraph
                StartDate
                CloseDate
