ReplicationSetSearch is a SearchForm
    Layout
        single column
            ExportFormat
            LastRefreshStamp
                default filter operator is greater than or equals
            Description
