ReportData is a Form
    is primary
    title is "ReportData"
    Layout
        single column
            Name
            create stamp
                display only
            Description
                display only
            MimeType
                display only
            ReportData
                label is "UniqueID"
            button of "Data"
                visible when (HasGroupFieldData)
                view GroupFieldDataBinary
            button of "ReportTemplate"
                visible when (HasReportSource)
                view RptFileBinary
