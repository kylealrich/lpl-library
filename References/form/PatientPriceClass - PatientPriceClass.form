PatientPriceClass is a Form
    Layout
        visible when (not RecordExists)
            paragraph
                ItemGroup
                ItemGroup.Description
                    no label
                    display as text
        visible when (RecordExists)
            ItemGroup.RepresentativeText
                label is "ItemGroup"
                display as text
        paragraph
            visible when (not RecordExists)
                PatientPriceClass
            visible when (RecordExists)
                PatientPriceClass
                    display as text
            Description
            Active
