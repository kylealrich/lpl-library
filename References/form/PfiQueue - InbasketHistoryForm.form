InbasketHistoryForm is a Form
    title is "ActionDetails"
    Layout
        two column
            PfiActivity.ActivityID
            PfiActivity.ActStatus
        three column
            ActionTaken
                display only
            PfiActivity.StartDate
            PfiActivity.EndDate
        three column
            ProLogicType
                display only
            ProLogicVal1
                display only
            visible when (not ProLogicType.AnyOneUser)
                ProLogicVal2
                    display only
        three column
            visible when (not TimeOutType.NotSet)
                TimeOutType
                    display only
            visible when (not TimeOutType.NotSet)
                TimeOutHour
                    display only
            visible when (not TimeOutType.NotSet)
                TimeoutAction
                    display only
