EmailToSupplierContactsActionForm is an ActionForm
    action is EmailToSupplierContacts
    title is "ExpiringCertificationsEmailToSupplierOrContact"
    Layout
        single column
            FromEmail
                label is "FromEmailAddress"
            visible when (not SourceId)
                SupplierToEmail
                    label is "ToEmailAddress"
                    display as text
            visible when (SourceId)
                ToEmail
                    label is "ToEmailAddress"
                    display as text
            Subject
                use text area of 1 lines
            EmailContents
                use text area of 8 lines
