WizardQuestionsActions is an ActionForm
    action is CreateFromWizard
    title is "CreateAQuestion"
    Layout
        single column
            QuestionText
                use text area of 8 lines
            ResponseType
                when value changed
                    refresh NonYesNoResponseType
        visible when (EligibleForWeighting)
            single column
                QuestionWeighting
        single column
            AllowResponseAttachment
                label is "AllowAttachmentUploadWithAnswer"
        single column
            visible when (NonYesNoResponseType)
                ResponseRules
                    display as required when (NonYesNoResponseType)
            visible when (ResponseType.YesNoText)
                YesNoTextResponseRules
                    display as required when (ResponseType.YesNoText)
            visible when (ResponseType.YesNo)
                YesNoResponseRules
                    display as required when (ResponseType.YesNo)
        single column
            visible when (EligibleForCM)
                CMQuestion
                    label is "SendQuestionToContractManagement"
        paragraph
            Attachment.File
                label is "Attachment"
            visible when (HasAttachment)
                button of "View"
                    view Attachment.File
                    display as link
        single column
            DisplayOrder
                display as text
