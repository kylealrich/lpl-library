PeriodEndReconciliation_ReconciliationGroup_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.PeriodEndReconciliation
                display as text
            Base_BusinessClass.PeriodEndReconciliationTitle
                no label
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_ReconciliationGroup)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.ReconciliationGroup
                label is untranslatable:"Reconciliation Group"
            ReconciliationGroup
                label is untranslatable:"Translated"
