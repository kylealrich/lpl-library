ProcessLevelForm is a CompositeForm
    is primary
    title is "ProcessLevel"
    context form is ProcessLevelContext
        background is theme color
    Address is a Panel
        form is AddressForm
    Processing is a Panel
        form is ProcessingForm
    BillOfExchange is a Panel
        visible when (IsBOECompany)
        form is BillOfExchangeForm
    DocumentTemplates is a Panel
        visible when (Company.UseIDM)
        form is DocumentTemplatesForm
