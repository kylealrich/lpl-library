AsyncBackgroundGroupCompositeForm is a CompositeForm
    is primary
    title is "BackgroundGroup"
    context form is AsyncBackgroundGroupForm
    ActiveMembers is a Panel
        list is ActiveRequests.primary
    Members is a Panel
        list is Requests.primary
    DependentRequests is a Panel
        list is DependentRequests.primary
