AsyncWorkUnitTriggerForm is a Form
    is primary
    Layout
        create stamp
            label is "CreateTimestamp"
            display only
        AsyncQueueDefinition
            label is "QueueName"
            display only
        Status
            label is "Status"
            display only
        DataArea
            label is "DataArea"
            display only
        WorkUnit
            label is "WorkUnit"
            display only
        FlowDefinition
            label is "FlowDefinition"
            display only
        Actor
            label is "Actor"
            display only
        Priority
            label is "Priority"
        Type
            label is "Type"
            display only
