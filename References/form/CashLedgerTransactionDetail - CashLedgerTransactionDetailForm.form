CashLedgerTransactionDetailForm is a Form
    title is "TransactionDetail"
    Layout
        single column
            Description
                use text area of 2 lines
        two column distributed
            column1
                IssuedBankAmount
                DetailTransactionNumber
                Reference
            column2
                visible when (CashLedgerTransaction entered)
                    CashLedgerSourceRecord
                        display as text
                    CashManagementAccount
                        display as text
