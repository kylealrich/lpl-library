InitialLoad is a CompositeForm
    is primary
    title is "MassBODPublish"
    context form is BODDataInitialLoadEntryContext
    show panel control on left
    Actions
        action is TriggerNow
        action is CreateLayout.Create
        action is save
        action is refresh
        action is previous record
        action is next record
        action is export to pdf
            action icon is print
            label is "PrintReport"
    SupplierPartyMaster is a Panel
        visible when (IsCondition1)
        form is SupplyPartyMasterHeader
    CustomerPartyMaster is a Panel
        visible when (IsCondition20)
        form is CustomerPartyMasterHeader
    FinancialPartyMaster is a Panel
        visible when (IsPartyMaster)
        form is FinancialPartyMasterHeader
    ShipFromPartyMaster is a Panel
        visible when (IsCondition4)
        form is ShipFromPartyMasterHeader
    ShipToPartyMaster is a Panel
        visible when (IsCondition2)
        form is ShipToPartyMasterHeader
    BillToPartyMaster is a Panel
        visible when (IsCondition3)
        form is BillToPartyMasterHeader
    ItemMaster is a Panel
        visible when (IsCondition15)
        form is ItemMasterHeader
    AssetMaster is a Panel
        visible when (IsCondition14)
        form is AssetMasterHeader
    ContactMasterRQ is a Panel
        visible when (IsMaster)
        form is ContactMasterRQHeader
    ContactMasterPO is a Panel
        visible when (IsMaster)
        form is ContactMasterPOHeader
    ContactMasterAP is a Panel
        visible when (IsMaster)
        form is ContactMasterAPHeader
    ContactMasterAR is a Panel
        visible when (IsCondition6)
        form is ContactMasterARHeader
    AccountingEntity is a Panel
        visible when (IsCondition7)
        form is AccountingEntityHeader
    CurrencyExchangeRateMaster is a Panel
        visible when (IsCondition8)
        form is CurrencyExchangeRateMasterHeader
    RequestingLocation is a Panel
        visible when (IsCondition5)
        form is RequestingLocationHeader
    InventoryLocation is a Panel
        visible when (IsMaster)
        form is InventoryLocationHeader
    AccountingChart is a Panel
        visible when (IsCondition9)
        form is AccountingChartHeader
    ProjectMaster is a Panel
        visible when (IsMaster)
        form is ProjectMasterHeader
    CurrencyExchangePeriodRate is a Panel
        visible when (IsMaster)
        form is CurrencyExchangePeriodRateHeader
    CodeDefinitionAccountingUnit is a Panel
        visible when (IsCondition10)
        form is CodeDefinitionAccountingUnitHeader
    CodeDefinitionUOM is a Panel
        visible when (IsCondition19)
        form is CodeDefinitionUOMHeader
    CodeDefinitionAccount is a Panel
        visible when (IsCondition10)
        form is CodeDefinitionAccountHeader
    CodeDefinitionFinanceDimensions is a Panel
        visible when (IsCondition12)
        form is FinanceDimensionsHeader
    CodeDefinitionCustomerTypes is a Panel
        visible when (IsCondition11)
        form is CodeDefinitionCustomerTypesHeader
    CodeDefinitionTerritory is a Panel
        visible when (IsCodeDefinition)
        form is CodeDefinitionTerritoryHeader
    CodeDefinitionAEGroupMember is a Panel
        visible when (IsCodeDefinition)
        form is CodeDefinitionAEGroupMemberHeader
    CodeDefinitionReportingBasisLedger is a Panel
        visible when (IsCodeDefinition)
        form is ReportingBasisLedgerHeader
    CodeDefinitionCashCode is a Panel
        visible when (IsCodeDefinition)
        form is CashCodeHeader
    CodeDefinitionGlobalLedgerCompany is a Panel
        visible when (IsCodeDefinition)
        form is GlobalLedgerCompanyHeader
    SupplierInvoice is a Panel
        visible when (IsCondition18)
        form is SupplierInvoiceHeader
    InventoryCount is a Panel
        visible when (IsCondition18)
        form is InventoryCountHeader
    RequisitionReturn is a Panel
        visible when (IsSCEBODs)
        form is RequisitionReturnHeader
    Invoice is a Panel
        visible when (IsCondition17)
        form is InvoiceHeader
    ChartOfAccounts is a Panel
        visible when (IsCondition21)
        form is ChartOfAccountsHeader
    SourceSystemJournalEntry is a Panel
        visible when (IsTransactionData)
        form is SourceSystemJournalEntryHeader
    SourceSystemGLMovement is a Panel
        visible when (IsTransactionData)
        form is SourceSystemGLMovementHeader
    SupplierBalance is a Panel
        visible when (IsTransactionData)
        form is SupplierBalanceHeader
    PayableTransaction is a Panel
        visible when (IsTransactionData)
        form is PayableTransactionHeader
    ReceivablePayment is a Panel
        visible when (IsTransactionData)
        form is ReceivablePaymentHeader
    RemittanceAdvice is a Panel
        visible when (IsTransactionData)
        form is RemittanceAdviceHeader
    PurchaseOrder is a Panel
        visible when (IsTransactionData)
        form is PurchaseOrderHeader
    ReceiveDelivery is a Panel
        visible when (IsTransactionData)
        form is ReceiveDeliveryHeader
    Contract is a Panel
        visible when (IsTransactionData)
        form is ContractHeader
