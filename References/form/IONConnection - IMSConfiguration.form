IMSConfiguration is an ActionForm
    action is ConfigureIMSConnection
    title is "ConfigureConnection"
    Layout
        single column
            PEndpoint
            valid when (ConfigurableTenant)
                PTenant
            IONAPICredentials
