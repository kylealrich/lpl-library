KanbanCountComposite is a CompositeForm
    title is "KanbanCount"
    context form is KanbanCountContextForm
        background is theme color
    Actions
        action is Reprocess
            hide when invalid
        action is previous record
            label is "_"
        action is next record
            label is "_"
        action is refresh
            label is "_"
    Main is a Panel
        form is Main
    KanbanCountLines is a Panel
        list is KanbanCountLine set.primary
