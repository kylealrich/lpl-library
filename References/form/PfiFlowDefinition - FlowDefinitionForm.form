FlowDefinitionForm is a Form
    title is "ProcessDefinition"
    restrict action Delete
        when (FlowType.SystemDefined)
    Layout
        single column
            PfiFlowDefinition
            Description
            visible when (not FlowType.SystemDefined)
                FlowType
                CurrentFlowVersion
                    display only
            IsFlowEnabled
            IsSubProcess
                display only
        two column
            Checkpoint
            ContinueWithCheckPointError
            FlowLogLevel
            ErrorLimit
            CaptureActivityTimes
            visible when (AutoRestartInXmlToggle)
                AutoRestartText
            visible when (not AutoRestartInXmlToggle)
                ClassicAutoRestart
                    label is "AutoRestart"
        three column
            LogBufferSize
            NotifyByEmail
                when value changed
                    refresh IsResourceMetricAlertValid
            visible when (IsResourceMetricAlertValid)
                SendResourceMetricAlert
        single column
            DisableResourceMetricAlert
