CreateConfigurationParameterForm is an ActionForm
    action is CreateParameter
    title is "CreateParameter"
    Layout
        two column
            column1 of 40%
                ConfigurationParameterCategory
                    label is "Component"
                    display only
                    show up to 20 characters
                valid when (not Usage.Key)
                    Name
                        show up to 200 characters
                Type
                visible when (not Type.FixedSize)
                    valid when (not Type.FixedSize)
                        Length
                visible when (Type.Number)
                    valid when (Type.Number)
                        Precision
                visible when (not HasStates)
                    Value
                        show up to 200 characters
            column2 of 60%
                PreferredCategory
                    show up to 20 characters
                FormatExample
                    label is "ExampleFormat"
                    display as text
        visible when (HasStates)
            paragraph
                StateValue
                    when value changed
                        Value =  StateValue
                visible when (not StrictStates)
                    text of "_or_"
                    Value
                        when value changed
                            StateValue =  blank
        paragraph
            HelpText
                display as text
