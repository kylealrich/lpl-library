AR251Custdet_CurrencyDescription_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.UniqueID
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_CurrencyDescription)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.CurrencyDescription
                label is untranslatable:"Currency Description"
            CurrencyDescription
                label is untranslatable:"Translated"
