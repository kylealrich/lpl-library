UpdateLineForm is an ActionForm
    action is UpdateLineFromChangeOrder
    title is "UpdateLine"
    Layout
        three column distributed
            Contract.RepresentativeText
                display as text
            ContractLine
                display as text
        three column distributed
            ParmDescription
            ParmChangeReason
        two column distributed
            visible when (not ServiceItemType)
                ParmBaseCost
                    label is "BaseCostChangeAmount"
                BaseCost
                    label is "CurrentAmount"
                    display as text
        two column distributed
            visible when (ServiceItemType)
                ParmBaseCost
                BaseCost
                    label is "CurrentBaseCost"
                    display as text
        two column distributed
            visible when (QuantityServiceItem)
                ParmQuantity
                LifetimeMaximumQuantity
                    label is "CurrentQuantity"
                    display as text
        single column
            ParmAttachment.File
                label is "Document"
