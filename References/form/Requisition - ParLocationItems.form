ParLocationItems is a Form
    restrict action Unreleased.UpdateRequisitionLineFillOrKill
    Layout
        paragraph
            Company
                display as text
            Company.Name
                no label
                display as text
        paragraph
            RequestingLocation
                label is "Location"
                display as text
            RequestingLocation.Name
                no label
                display as text
        paragraph
            Requester
                display as required when (RequesterRequired)
            Requester.Employee.PresentationNameSnapshot
                no label
                display as text
        visible when (false)
            RequisitionSource
