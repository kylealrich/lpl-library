MobileRecallNoticeTask is a CompositeForm
    title is "Task"
    context form is MobileRecallNoticeTaskContext
    Actions
        action is save
            label is "_"
        action is previous record
            label is "_"
        action is next record
            label is "_"
        action is refresh
            label is "_"
    Main is a Panel
        form is MobileMain
    Questions is a Panel
        visible when (ManuallyCreatedTask)
        list is RecallNoticeTaskQuestion set.MobileRecallNoticeTaskQuestions
