ProcessFlowQueueComposite is a CompositeForm
    is primary
    title is "ProcessFlowQueue"
    context form is ProcessFlowQueueContext
    Errors is a Panel
        visible when (IsTriggerError)
        form is ErrorForm
    Folders is a Panel
        visible when (Type.Trigger)
        list is ProcessFlowFolder set.PFFList
    Variables is a Panel
        visible when (Type.Trigger)
        list is ProcessFlowVariable set.PFVList
