CreateSerialNumber is an ActionForm
    action is CreateSerialNumber
    title is "CreateSerialNumber"
    Layout
        paragraph
            Product
                select is ProductSelect
            Product.ProductName
                no label
                display only
