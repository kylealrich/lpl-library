CreateIdentityForm is a Form
    use for action CreateIdentity
    title is "CreateIdentity"
    Layout
        single column
            Service
                label is "Service"
                select is PrimaryServiceList
            Identity
                label is "Username"
            NewPassword
                label is "Password"
