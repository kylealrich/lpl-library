CreateForm is an ActionForm
    action is Create
    title is "CreatePeriod"
    Layout
        single column
            FinanceEnterpriseGroup
            CreatePeriodType
                label is "PeriodType"
            visible when (CreatePeriodType.Year)
                Year
                DisplayPeriod
            visible when (CreatePeriodType.EndDate)
                CreateEndDate
                    label is "PeriodEndDate"
