EnableDBSessionDebugActionForm5 is an ActionForm
    action is TerminatedAbnormally.RequeueWithDBSessionDebug
    title is "EnableDatabaseTierSessionDebug"
    Layout
        single column
            DBSessionDebugLevel
                label is "Detail"
                display as radio buttons
                when value changed
                    refresh DBSessionDebugLevel
            visible when (not DBSessionDebugLevel.Minimal)
                LPLTrace
