CreateWindowsIdentity is a Form
    use for action CreateWindowsIdentity
    title is "CreateWindowsIdentity"
    Layout
        single column
            Service
                select is WindowsServiceList
            Identity
                label is "SID"
            WindowsIdentityProperties.Password
            WindowsIdentityProperties.DomainUser
