PerformanceEvaluationTemplateContext is a Form
    Layout
        paragraph
            SupplierGroup
            SupplierGroup.Description
                display as text
        single column
            Name
        paragraph
            Active
            visible when (CreationDate entered)
                CreationDate
                    display as text
            TotalCategoryWeighting
                display as text
        paragraph
            ForSupplier
            ForContract
        visible when (PerformanceCategoriesExist)
            visible when (ForSupplier)
                two column
                    CurrentTemplateForEvaluations
                        label is "CurrentTemplateForSupplierEvaluations"
                    visible when (CurrentEvaluationTemplateDate entered)
                        CurrentEvaluationTemplateDate
                            label is "EvaluationTemplateDate"
                            display as text
                    CurrentTemplateForScores
                        label is "CurrentTemplateForSupplierScores"
                    visible when (CurrentScoringTemplateDate entered)
                        CurrentScoringTemplateDate
                            label is "ScoringTemplateDate"
                            display as text
