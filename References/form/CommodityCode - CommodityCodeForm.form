CommodityCodeForm is a Form
    is primary
    title is "CommodityCode"
    Layout
        paragraph
            visible when (ItemGroup not entered)
                ItemGroup
                ItemGroup.Description
                    no label
                    display as text
            visible when (ItemGroup entered)
                ItemGroup.ItemGroupDescription
                    label is "ItemGroup"
                    display as text
        single column
            CommodityCode
                compact format
            Description
                show up to 60 characters
            DepthLevel
                display as text
            Active
