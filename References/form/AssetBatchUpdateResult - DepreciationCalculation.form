DepreciationCalculation is an ActionForm
    action is DepreciationCalculation
    title is "DepreciationCalculation"
    Layout
        header2 of "SelectionOptions"
        single column
            FinanceEnterpriseGroup
                display as text
        paragraph
            Book
                label is "Book"
                select is ActiveDepreciationBooks
            text of "Or"
            BookGroup
                label is "BookGroup"
                select is BookGroups
        paragraph
            Company
                label is "Company"
                select is Companies
            text of "Or"
            CompanyGroup
                label is "CompanyGroup"
        single column
            Classification
            Compute
                label is "ComputeOption"
            Simulated
            Queue
