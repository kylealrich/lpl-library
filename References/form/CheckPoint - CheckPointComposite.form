CheckPointComposite is a CompositeForm
    is primary
    title is "Checkpoint"
    context form is CheckPointContext
    RestartInformation is a Panel
        form is RestartInformation
    Data is a Panel
        list is CheckPointRestartData set.DataList
    LargeObjects is a Panel
        list is CheckPointField set.CheckPointFieldList
