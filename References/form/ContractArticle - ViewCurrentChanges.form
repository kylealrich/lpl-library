ViewCurrentChanges is a Form
    title is "ArticleUpdates"
    Actions
        action is refresh
    Layout
        visible when (not ActiveUpdatesToTextExist)
            single column
                HeaderText
                    no label
                    display as text
        visible when (ActiveUpdatesToTextExist)
            single column
                DerivedCompareCurrentPreviousHeaderText
                    no label
                    display as text
