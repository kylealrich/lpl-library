ReconciliationAccountGroupContextForm is a Form
    title is "ReconciliationReportGroup"
    Layout
        four column
            ReconciliationAccountGroup
            Description
            visible when (ParentAccountGroup not entered)
                ReconciliationAccountType
            visible when (ParentAccountGroup entered)
                ParentAccountGroup.ReconciliationAccountType
                    display as text
            Active
