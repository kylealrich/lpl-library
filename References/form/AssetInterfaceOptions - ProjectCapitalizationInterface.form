ProjectCapitalizationInterface is an ActionForm
    action is ProjectCapitalizationInterface
    title is "ProjectAssetInterface"
    Layout
        visible when (not Repair)
            single column
                PrmPostingDate
        visible when (Repair)
            single column
                PrmRepairDate
        visible when (PurchaseDate not entered)
            single column
                PrmPurchaseDate
        visible when (PurchaseDate entered)
            single column
                PurchaseDate
        visible when (IsValidForPostWIPToProject)
            single column
                PrmPostWIPToProject
                    label is "PostWIPToProject"
