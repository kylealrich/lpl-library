UserCubeForm is a Form
    is primary
    Layout
        paragraph
            BusinessClass
                select is ConfigurableBusinessViewList
            Description
        single column
            AnalyticCubeRel.Mode
                label is "CubeMode"
                raise red alert when (not AnalyticCubeRel.Mode.Active)
                    mouse over text is "<AnalyticCubeRel.ModeStatusMessage>"
        two column
            HasPendingChanges
                display only
            PendingDelete
        four column
            Status
                raise red alert when (Status.Invalid)
            update stamp
                label is "LastUpdate"
                display as text
            update stamp.actor
                label is "LastChangedBy"
                display as text
            UserEditorType
                label is "LastEditedVia"
                display as text
        single column
            UserCube
                label is "UserCubeId"
        single column
            LPL
                display only
                use text area of 30 lines
            visible when (HasErrorText)
                ErrorText
                    use text area of 5 lines
