MobileAttachmentForm is a Form
    title is "CustomerOrderLineAttachment"
    restrict action Create
    restrict action Delete
    Actions
        action is Delete
            hide when invalid
        action is save
            label is "_"
            hide when invalid
    Layout
        paragraph
            CustomerOrderLine
                display as text
        single column
            CustomerOrderLineAttachment
                label is "AttachmentNumber"
                display as text
        paragraph
            visible when (HasAttachment)
                Attachment.Title
            Attachment.File
                label is "Attachment"
            visible when (HasAttachment)
                button of "View"
                    view Attachment.File
                    display as link
        single column
            Comment
