EnableUser is a Form
    use for action EnableUser
    title is "EnableUser"
    Layout
        single column
            EntityId
            UserIdentity
            IdentifierId
            IdentifierValue
            Language
