Audit_Log_Form is a Form
    is primary
    propagate as a drill
    Layout
        header1 of untranslatable:"Changes"
        single column
            audit period
            applied stamp
            effective date
            actor
            authenticated actor
            action
            subject
            reason code
            effective time zone
            action tag
            action comment
        visible when (CloseManagementGroup has audit change)
            two column
                old CloseManagementGroup
                CloseManagementGroup
                    no label
        visible when (ClosePeriod.CloseCalendarPeriod.Year has audit change)
            two column
                old ClosePeriod.CloseCalendarPeriod.Year
                ClosePeriod.CloseCalendarPeriod.Year
                    no label
        visible when (ClosePeriod.CloseCalendarPeriod.Period has audit change)
            two column
                old ClosePeriod.CloseCalendarPeriod.Period
                ClosePeriod.CloseCalendarPeriod.Period
                    no label
        visible when (ClosePeriod.CloseCalendar has audit change)
            two column
                old ClosePeriod.CloseCalendar
                ClosePeriod.CloseCalendar
                    no label
        visible when (ClosePeriodTask has audit change)
            two column
                old ClosePeriodTask
                ClosePeriodTask
                    no label
        visible when (OneTimeTaskApprovalResource has audit change)
            two column
                old OneTimeTaskApprovalResource
                OneTimeTaskApprovalResource
                    no label
        visible when (ApprovalLevel has audit change)
            two column
                old ApprovalLevel
                ApprovalLevel
                    no label
        visible when (Approver has audit change)
            two column
                old Approver
                Approver
                    no label
        visible when (ApprovalTeam has audit change)
            two column
                old ApprovalTeam
                ApprovalTeam
                    no label
        visible when (EscalationDays has audit change)
            two column
                old EscalationDays
                EscalationDays
                    no label
        visible when (EscalationHours has audit change)
            two column
                old EscalationHours
                EscalationHours
                    no label
        visible when (EscalateTo has audit change)
            two column
                old EscalateTo
                EscalateTo
                    no label
        visible when (EscalationApprovalLevel has audit change)
            two column
                old EscalationApprovalLevel
                EscalationApprovalLevel
                    no label
        visible when (EscalationLevelFromCode has audit change)
            two column
                old EscalationLevelFromCode
                EscalationLevelFromCode
                    no label
