InterfaceBudgetsContext is a Form
    Layout
        three column distributed
            column1
                paragraph
                    visible when (not RecordExists)
                        Company
                    visible when (RecordExists)
                        Company.BillingCompany.RepresentativeText
                            display as text
                paragraph
                    visible when (not RecordExists)
                        Customer
                            select is CustomerSelectList
                    visible when (RecordExists)
                        FranchiseContract.DerivedCustomer
                            display as text
            column2
                paragraph
                    visible when (not RecordExists)
                        CustomerShipTo
                            label is "ShipTo"
                    visible when (RecordExists)
                        FranchiseContract.DerivedShipTo
                            display as text
                paragraph
                    visible when (not RecordExists)
                        FranchiseContract
                            label is "Contract"
                    visible when (RecordExists)
                        FranchiseContract
                            label is "Contract"
                            display as text
            column3
                visible when (not RecordExists)
                    FranchiseBudgetImport
                        label is "Year"
                        compact format
                visible when (RecordExists)
                    FranchiseBudgetImport
                        label is "Year"
                        display as text
