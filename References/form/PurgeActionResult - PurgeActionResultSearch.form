PurgeActionResultSearch is a SearchForm
    Layout
        single column
            BusinessAction
            RunTimeStamp
                default filter operator is greater than or equals
            EndTimeStamp
                default filter operator is greater than or equals
