VendorImport_PovVendorContact_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.VendorImport
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_PovVendorContact)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.PovVendorContact
                label is untranslatable:"Vendor Contact"
            PovVendorContact
                label is untranslatable:"Translated"
