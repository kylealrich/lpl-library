AsyncActionInvocationContextForm is a Form
    propagate as a drill
    title is "Job"
    restrict action Update
    restrict action Create
    Layout
        two column
            Name
                display only
            Archived
                display only
        two column
            AsyncActionInvocationStatus
                display only
            Actor
                label is "SubmittingActor"
                display only
        two column
            ImplementingClass
                display only
            AsyncAction
                display only
        two column
            Group
                display only
            DataArea
                display only
        single column
            StatusChanged
                label is "StatusLastChanged"
                display only
        two column
            Started
                label is "EnteredInProgressState"
                display only
            AsyncActionTrigger
                label is "Occurrence"
                display only
        two column
            Finished
                label is "EnteredFinishedState"
                display only
            TimeToExec
                label is "NextTimeToExecute"
                display only
        two column
            Type
                display only
            visible when (AsyncAction.IsParameterized)
                ActionParameters
                    display only
            visible when (NonParameterizedParameterFormToDisplay)
                BatchTaskId
                    label is "Job"
            visible when (NoNonParameterizedParameterFormToDisplay)
                text of "NoParameterFormToDisplay"
        two column
            AdditionalInfo
                display only
            FailureCode
                display only
            ErrorMessage
                display only
            blank line
            Result
                display only
        two column
            visible when (InvokedFromButInvokingParentDoesNotExist)
                InvokedFromName
                    display only
                text of "InvokedFromJobInvocationRecordNoLongerExistsFor<InvokedFromName>"
        visible when (HasRunOnErrorAction)
            header2 of "ErrorProcessing"
            two column
                RunOnErrorAction.ImplementingClass
                    display only
                RunOnErrorAction.AsyncAction
                    display only
            two column
                RunOnErrorFailureCode
                    display only
                RunOnErrorErrorMessage
                    display only
                blank line
                RunOnErrorResult
                    display only
