AssetBookMainForm is a CompositeForm
    is primary
    title is "AssetBook_-_<Book>"
    context form is AssetBookContextForm
        background is theme color
    Main is a Panel
        form is AssetBookForm
    ProjectionsResults is a Panel
        visible when (AssetProjectionExists)
        list is AssetProjectionRel.ProjectionResults
