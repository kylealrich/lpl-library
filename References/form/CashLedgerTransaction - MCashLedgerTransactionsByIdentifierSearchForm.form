MCashLedgerTransactionsByIdentifierSearchForm is a SearchForm
    title is "CashLedgerTransactionSearch"
    Layout
        single column
            Company
            CashLedgerTransactionIdentifier
            CashCode
            BankTransactionCode
            TransactionNumber
                raise red alert when (BudgetEditErrorExists)
                    mouse over text is "<DerivedFirstErrorMessage>"
                raise yellow alert when (CashLedgerTransactionErrorsExist)
                    mouse over text is "CashLedgerTransactionErrorRecordsExist"
            IssueDate
            Status
                raise red alert when (IsUnreleasedCashLedgerReconciliationAdjustmentExist)
                    mouse over text is "UnreleasedVarianceAdjustmentExists"
                raise yellow alert when (VarianceExists)
                    mouse over text is "<DerivedVarianceExistsMessage>"
