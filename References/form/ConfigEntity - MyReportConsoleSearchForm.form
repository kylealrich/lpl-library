MyReportConsoleSearchForm is a SearchForm
    Layout
        single column
            UserFolder(UserFolder).Name
                label is "ReportName"
            UserFolder(UserFolder).Actor
                label is "SharedWithActor"
            BusinessView
