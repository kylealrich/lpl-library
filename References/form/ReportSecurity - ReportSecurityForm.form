ReportSecurityForm is a Form
    is primary
    title is "ReportSecurity"
    Layout
        single column
            ReportSecurity.Role
            Description
            Configuration
            Status
            CopiedFrom.Role
                label is "CopiedFromRole"
                display only
            visible when (CanEditDelivered)
                Delivered
                DeliveredConfiguration
                ItemsToMerge
            visible when (not CanEditDelivered)
                Delivered
                    display only
                DeliveredConfiguration
                    display only
                ItemsToMerge
                    display only
