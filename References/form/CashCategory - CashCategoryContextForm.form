CashCategoryContextForm is a Form
    Layout
        visible when (RecordExists)
            three column distributed
                column1
                    CashManagementGroup
                        display as text
                column2
                    paragraph
                        CashCategory
                            display as text
                        Description
                column3
                    display as text
                        visible when (CashProcessLevel entered)
                            CashProcessLevel.CashProcessLevelName
                                label is "ProcessLevel"
                        visible when (CashProcessLevel not entered)
                            CashProcessLevel
                                label is "ProcessLevel"
            single column
                Active
        visible when (not RecordExists)
            CashManagementGroup
                display as text
            paragraph
                CashCategory
                Description
            paragraph
                CashProcessLevel
                    label is "ProcessLevel"
                CashProcessLevel.Name
                    no label
                    display as text
