InvoiceLineFactConsolidateUNSPSCForm is a Form
    title is "UNSPSC"
    Layout
        four column
            DerivedSegment
                label is "Segment"
            visible when (FamilyEntered)
                DerivedFamily
                    label is "Family"
            visible when (ClassEntered)
                DerivedClass
                    label is "Class"
            visible when (CommodityEntered)
                DerivedCommodity
                    label is "Commodity"
