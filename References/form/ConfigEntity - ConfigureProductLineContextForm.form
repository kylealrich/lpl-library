ConfigureProductLineContextForm is a Form
    Layout
        two column distributed
            column1
                Name
                    no label
                    header1
                    bold
                    display as text
                    raise red alert when (Status.Invalid)
                        mouse over text is "Invalid"
                    raise yellow alert when (Status.Inactive)
                        mouse over text is "Inactive"
                    raise green alert when (Status.Active)
                        mouse over text is "Active"
                EntityType
                    header3
                    display as text
            column2
                DerivedLastChange
                    no label
                    display as tag
                        color of blue
        paragraph
            visible when (not CanGUIEditProdLine)
                text of "A_Data_AreaConfigurationCanNotBeEditedWithThisEditorUnlessItIsActive"
