ReportOptionsDefaultForm is a CompositeForm
    is primary
    title is "ReportOptions"
    context form is ReportOptionsContextForm
    PDFOptions is a Panel
        visible when (ReportExportType.PDF)
        form is PDFOptionsForm
    CSVOptions is a Panel
        visible when (ReportExportType.CSV)
        form is CSVOptionsForm
