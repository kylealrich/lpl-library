CopyRowView is an ActionForm
    action is CopyRowView
    title is "CopyRow"
    Layout
        single column
            PrmGLRowView
                label is "RowName"
        single column
            PrmDescription
                label is "Description"
