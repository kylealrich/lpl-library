CancelLine is an ActionForm
    action is CancelLine
    title is "CancelLine"
    Layout
        single column
            visible when (not IsServiceCodeAmount)
                PrmCancelQuantity
                CancelQuantity
                    label is "CurrentCancelQuantity"
                    display as text
            visible when (IsServiceCodeAmount)
                PrmServiceCancelAmount
                ServiceCancelAmount
                    label is "CurrentServiceCancelAmount"
                    display as text
