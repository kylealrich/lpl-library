TrueCostDriver is a Form
    is primary
    title is "True\\CostDriver"
    Layout
        single column
            visible when (TrueCostDriver not entered)
                TransientTrueCostDriver
            visible when (TrueCostDriver entered)
                TrueCostDriver
                    label is "True\\CostDriver"
            Description
            IsUnitCostDriver
                display only
