ActorProxyForm is a Form
    use for action Create
    title is "ProxyGrantedToActor"
    Layout
        single column
            ActorProxy.Grantee
                label is "GrantedTo"
                select is ActorList
            Proxy
                label is "Proxy"
                compact format
