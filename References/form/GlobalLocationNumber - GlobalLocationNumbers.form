GlobalLocationNumbers is a CompositeForm
    is primary
    title is "GlobalLocationNumber"
    context form is GlobalLocationNumber
        background is theme color
    Assignments is a Panel
        visible when (IsRecordExist)
        list is GlobalLocationNumberDetailsRel.GlobalLocationNumberDetailLists
