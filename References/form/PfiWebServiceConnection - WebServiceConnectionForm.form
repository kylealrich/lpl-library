WebServiceConnectionForm is a Form
    title is untranslatable:"WebServiceConnection"
    Layout
        single column
            Description
            ConnectionTimeOut
                label is "ConnectionTimeout(Seconds)"
            RequestTimeOut
                label is "RequestTimeout(Seconds)"
            AllowChunking
        header2 of "Authentication"
        single column
            Authentication
                when value changed
                    refresh IsUserAndPasswordAuthentication
            visible when (IsUserAndPasswordAuthentication)
                User
                Password
            visible when (false)
                IsUserAndPasswordAuthentication
