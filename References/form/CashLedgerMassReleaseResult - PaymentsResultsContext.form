PaymentsResultsContext is a Form
    Layout
        single column
            display as text
                CashManagementGroup
                CashLedgerMassReleaseResult
                CreateDate
                Status
                    raise yellow alert when (PaymentErrorsExist)
                        mouse over text is "NotAllPaymentsReleased;UnresolvedErrorsExist"
