RequisitionInterfaceResultComposite is a CompositeForm
    is primary
    title is "RequisitionInterfaceResult"
    context form is RequisitionInterfaceResultContext
    show panel control on left
    Actions
        action is RerunForErrors
            hide when invalid
        action is Delete
        action is refresh
    RequisitionsCreated is a Panel
        visible when (HasRecordsProcessed)
        list is RequisitionsRel.InterfaceListing
    PendingRequisitionsForRunGroup is a Panel
        visible when (ShowRunGroupPanel)
        list is RunGroupProcessedRequisitionImportRel.RequisitionImportListForResults
    PendingRequisitionsForErrorRunGroup is a Panel
        visible when (ShowErrorRunGroupPanel)
        list is ErrorRunGroupRequisitionImportRel.RequisitionImportListForResults
