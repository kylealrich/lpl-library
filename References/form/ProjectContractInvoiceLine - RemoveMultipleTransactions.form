RemoveMultipleTransactions is an ActionForm
    action is RemoveMultipleTransactions
    title is "RemoveMultipleTransactions"
    Layout
        single column
            PlaceOnHold
            visible when (EligibleToTransfer)
                Transfer
            visible when (Transfer)
                PrmFinanceCodeBlock
                    label is "ToFinanceStructure"
                    compact format
                PrmPostingDate
                    label is "PostingDate"
