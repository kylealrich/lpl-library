ExternalEntityRegistrationProfile is a CompositeForm
    use for action CustomerCreateImport
    title is "<DerivedExternalEntityRegistrationTitle>"
    disable field options
    called out actions only
    Actions
        action is save
            hide when invalid
            valid when (not IsAnonymous)
        action is refresh
            hide when invalid
            valid when (not IsAnonymous)
    TermsAndConditions is a Panel
        visible when (IsAnonymousAndFullRegistration)
        mouse over text is "<CustomerGroup.AccessRequestTermText>"
        form is RegistrationTerm
    CompanyAndContactInformation is a Panel
        valid when (AcceptTerms)
        visible when (IsAnonymousAndFullRegistration)
        form is ExternalEntityCustomerPortalRegistration
    CompleteAccountRegistration is a Panel
        visible when (IsAnonymousAndCompleteAccountRegistration)
        form is ExternalEntityRegistrationForExistingContact
    Questions is a Panel
        visible when (ShowQuestions)
        mouse over text is "<CustomerGroup.AccessRequestQuestionText>"
        template is CustomerGroup.ActiveCustomerGroupQuestionRel
            business class is CustomerRepresentativeQuestionResponse
                form is CustomerQuestionResponse
    Status is a Panel
        visible when (not IsAnonymous)
        form is AccessRequestSummary
