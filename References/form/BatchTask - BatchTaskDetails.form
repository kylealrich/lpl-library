BatchTaskDetails is a Form
    title is "JobDetails"
    Layout
        single column
            visible when (not TypeTransientField.TaskStream)
                BusinessClass
                    label is "Class"
                TaskAction
            visible when (not TypeTransientField.TaskStream)
                Data
                    label is "JobParameters"
