ValidateStaging is a Form
    title is "ValidateStaging"
    Layout
        single column
            valid when (Status.InProgress)
                display as text
                ValidateDataInDashboards
                    label is "Have_you_tested_and_validated_the_staging_environment_dashboards?"
            valid when (Status.InProgress)
                display as text
                CheckedCustomsReport
                    label is "Have_you_tested_and_validated_your_custom_reports,_if_any?"
