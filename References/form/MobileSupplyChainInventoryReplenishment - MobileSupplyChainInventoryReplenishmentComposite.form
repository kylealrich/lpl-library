MobileSupplyChainInventoryReplenishmentComposite is a CompositeForm
    title is "InventoryReplenishment"
    context form is MobileSupplyChainInventoryReplenishmentContextForm
        background is theme color
    Actions
        action is previous record
            label is "Previous"
        action is next record
            label is "Next"
        action is refresh
            label is "Refresh"
    Main is a Panel
        form is Main
    Lines is a Panel
        list is MobileSupplyChainInventoryReplenishmentLine set.primary
