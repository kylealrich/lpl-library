MobileRequisitionLineApproval is a CompositeForm
    title is "_"
    context form is MobileRequisitionLineApprovalContext
        background is theme color
    Line is a Panel
        form is MobileLineApprovalMainForm
    Comments is a Panel
        visible when (HasComments)
        list is RequisitionLineCommentRel.PfiCommentsCardList
    Distributions is a Panel
        visible when (DistributionExists)
        list is RequisitionLineDistributionsRel.MobileCardviewList
