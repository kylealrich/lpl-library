MobileAPVendorContext is a Form
    title is "Vendor"
    Layout
        paragraph
            visible when (HasLogo)
                Logo.File
                    no label
                    align as label
                    display as full image up to 5 lines
                        missing image is missingpicture96px
        display as text
            two column table
                column1
                    RepresentativeText
                        label is "Vendor"
                column2
                    VendorStatus
                        no label
                        display as tag
                            color of red when (not VendorStatus.Active)
                            color of green when (VendorStatus.Active)
