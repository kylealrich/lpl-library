RouteForm is a CompositeForm
    is primary
    title is "RouteHeader"
    context form is RoutesFormContext
    show panel control on left
    Main is a Panel
        form is Main
    RouteStops is a Panel
        list is RouteStop set.RouteStopsList
