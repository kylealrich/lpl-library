FinanceJournalInterfaceContext is a Form
    title is "InterfaceDistribution"
    Layout
        single column
            FinanceJournalInterface
                display as text
        two column
            RunGroup
            PostingDate
                label is "DefaultCashDistributionsPostingDate"
        single column
            Status
                display as text
        single column
            visible when (FinanceGroup.JournalEntryApprovalRequired)
                ApprovalCode
