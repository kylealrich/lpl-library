Audit_Log_Form is a Form
    is primary
    propagate as a drill
    Layout
        header1 of untranslatable:"Changes"
        single column
            audit period
            applied stamp
            effective date
            actor
            authenticated actor
            action
            subject
            reason code
            effective time zone
            action tag
            action comment
        visible when (CashManagementGroup has audit change)
            two column
                old CashManagementGroup
                CashManagementGroup
                    no label
        visible when (CashCode has audit change)
            two column
                old CashCode
                CashCode
                    no label
        visible when (FromBankTransactionCode has audit change)
            two column
                old FromBankTransactionCode
                FromBankTransactionCode
                    no label
        visible when (ToBankTransactionCode has audit change)
            two column
                old ToBankTransactionCode
                ToBankTransactionCode
                    no label
        visible when (ExpenseBankTransactionCode has audit change)
            two column
                old ExpenseBankTransactionCode
                ExpenseBankTransactionCode
                    no label
