CreateUnixIdentity is a Form
    use for action CreateUnixIdentity
    title is "CreateUnixIdentity"
    Layout
        single column
            Service
                select is UnixServiceList
            Identity
                label is "Username"
            UnixIdentityProperties.Password
