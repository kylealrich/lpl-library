Copy is an ActionForm
    action is Copy
    title is "Copy"
    Layout
        single column
            ToProject.Project
                select is AssetInterfaceProjectSelect
            visible when (ToProject.Project.ProjectType.Summary)
                PrmAssetCurrency
