IONDocumentContext is a Form
    Layout
        single column
            IONDocument
            InputDocumentName
                label is "DocumentName"
            InputLogicalId
                label is "LogicalId"
                raise red alert when (not LogicalIdExists)
                    mouse over text is "LogicalIdIsNotDefned"
        four column
            Send
            Import
            Replicate
                raise yellow alert when (SchemaRegistrationRequired)
                    mouse over text is "SchemaRegistrationRequired"
            SendConfirm
