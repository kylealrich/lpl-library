CreateCalendarYear is an ActionForm
    action is CreateCalendarYear
    Layout
        single column
            Year
            visible when (ShowStartDate)
                StartDate
        two column
            visible when (CalendarType.Days)
                EndDate
            visible when (445OrVariant)
                EndDate
                text of "EnterAn_End_DateIfExtraDaysMustBeAddedToThe_Calendar"
        single column
            visible when (false)
                RecreatingFollowingYear
