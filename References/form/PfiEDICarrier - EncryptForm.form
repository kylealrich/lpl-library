EncryptForm is a Form
    title is "EncryptSettings"
    Layout
        single column
            EncryptionType
            EncryptionServiceName
            Sign
            visible when (Sign)
                SignServiceName
            EnableIntegrityPacket
