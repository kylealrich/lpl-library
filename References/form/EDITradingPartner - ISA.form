ISA is a Form
    Layout
        two column
            visible when (Direction.Out)
                column2
                    IsaSenderIdQualifier
                        label is "Sender_Qualifier"
                column1
                    IsaSenderId
                        label is "Sender_(ISA06)"
                        show up to 18 characters
            visible when (Direction.In)
                column2
                column1
                    IsaReceiverId
                        label is "Receiver_(ISA08)"
                        show up to 18 characters
        two column
            visible when (Direction.Out)
                column2
                    IsaReceiverIdQualifier
                        label is "Receiver_Qualifier"
                column1
                    IsaReceiverId
                        label is "Receiver_(ISA08)"
                        show up to 18 characters
            visible when (Direction.In)
                column2
                column1
                    IsaSenderId
                        label is "Sender_(ISA06)"
                        show up to 18 characters
        InterchangeControlNumber
            label is "ISAControlNumber"
