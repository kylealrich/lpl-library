DefaultGeneralInfo is a Form
    is primary
    title is "SecurityClassDetails"
    restrict action Update
        when (StandardTemplate)
    Layout
        single column
            SecurityClass
                label is "SecurityClassName"
            visible when (InheritanceEnabled)
                BaseClass
            Description
            InActive
            LPL
                use text area of 40 lines
