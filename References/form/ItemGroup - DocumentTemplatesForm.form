DocumentTemplatesForm is a Form
    Layout
        header2 of "Options"
        single column
            UseIDMTemplate
        visible when (UseIDMTemplate)
            header2 of "IDMTemplates"
            visible when (RecipeManagementInUse)
                paragraph
                    RecipeTemplate
                        select is RecipeIDMTemplate
                    button of "<RecipeTemplate.Name>"
                        link is external "<RecipeTemplate.RetrieveUrl>"
                        display as link
            visible when (not RecipeManagementInUse)
                paragraph
                    RecipeTemplate
                        display only
