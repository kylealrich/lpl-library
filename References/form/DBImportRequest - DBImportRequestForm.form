DBImportRequestForm is a Form
    is primary
    title is "DBImportRequest"
    Layout
        single column
            BusinessClass
            DBImportRequest
            Status
        paragraph
            StartTime
            EndTime
            ElapsedTimeComponentsText
        paragraph
            NumberOfRecords
            valid when (ImportData entered)
                button of "ViewImportData"
                    view ImportData
                        using mime type ImportDataMimeType
        valid when (RunLog entered)
            single column
                button of "RunLog"
                    view RunLog
                        using mime type "text/plain"
        paragraph
            NumberOfExceptions
            valid when (BadRecordLog entered)
                three column
                    button of "ViewBadRecordLog"
                        view BadRecordLog
                            using mime type ImportDataMimeType
