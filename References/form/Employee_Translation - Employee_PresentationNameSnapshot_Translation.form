Employee_PresentationNameSnapshot_Translation is a Form
    Layout
        paragraph
            Base_BusinessClass.Employee
                display as text
        single column
            IsoLocale
                compact format
                raise yellow alert when (VerifyTranslations_PresentationNameSnapshot)
                    mouse over text is untranslatable:"The base data has been changed but this translation has not been verified"
        single column
            blank line
            Base_BusinessClass.PresentationNameSnapshot
                label is untranslatable:"Name"
            PresentationNameSnapshot
                label is untranslatable:"Translated"
