ASConfigurationHeader is a Form
    Layout
        four column grid distributed
            ASConfiguration
            Description
            valid when (CanEditBaseFields)
                VersionNumber
            paragraph
                ProductLine
                CreateTimeStamp
                    label is "Created"
                    display as tag
                        color of green
                BaseConfiguration
                    display only
                BaseVersionNumber
                    display only
                valid when (DevelopmentMode)
                    Delivered
                valid when (not IsPrimary)
                    button of "MakePrimaryForTagging"
                        action is MakePrimary
            paragraph
                ExportFromTimeStamp
                    label is "ExportFrom"
                DefaultEffectiveDate
                Inactive
                button of "ViewLogs"
                    visible when (ShowLogs)
                    link is LogRecords
