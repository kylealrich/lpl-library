CreateRow is a CompositeForm
    use for action CreateRow
    title is "CreateRow:<FileCreationSetup.Description>"
    Actions
        action is save and close
            label is "Save"
    Detail is a Panel
        form is RowDetail
    Conditions is a Panel
        visible when (not RowExtendsOneToManyRow)
        form is Conditions
    OneToMany is a Panel
        visible when (FileCreationSetup.SupportsOneToMany)
        form is OneToManyOptions
