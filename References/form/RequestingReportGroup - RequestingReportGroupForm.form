RequestingReportGroupForm is a Form
    Layout
        paragraph
            visible when (RecordExist)
                Company.DerivedCompanyDisplay
                    label is "Company"
                    display as text
            visible when (not RecordExist)
                Company
                Company.Name
                    no label
                    display as text
        paragraph
            visible when (RecordExist)
                RequestingReportGroup
                    display as text
            visible when (not RecordExist)
                RequestingReportGroup
            Name
            AddNewRequestingLocations
                label is "AddNewRequestingLocationsToThisReportGroup"
