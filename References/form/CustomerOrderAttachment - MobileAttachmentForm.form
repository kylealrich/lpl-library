MobileAttachmentForm is a Form
    title is "CustomerOrderAttachment"
    restrict action Create
    restrict action Delete
    Actions
        action is Delete
            hide when invalid
    Layout
        paragraph
            CustomerOrder
                display as text
        single column
            CustomerOrderAttachment
                label is "AttachmentNumber"
                display as text
        paragraph
            visible when (HasAttachment)
                Attachment.Title
            Attachment.File
                label is "Attachment"
            visible when (HasAttachment)
                button of "View"
                    view Attachment.File
                    display as link
        single column
            Comment
