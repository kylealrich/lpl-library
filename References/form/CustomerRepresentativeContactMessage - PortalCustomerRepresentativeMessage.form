PortalCustomerRepresentativeMessage is a Form
    title is "MessageDetails"
    disable field options
    called out actions only
    Actions
        action is MarkAsRead
            hide when invalid
        action is MarkAsUnread
            hide when invalid
        action is MoveToInbox
            hide when invalid
        action is MarkAsDeleted
            hide when invalid
        action is refresh
            label is "_"
    Layout
        display as text
            single column
                MessageTitle
                    label is "Title"
                MessageText
                    label is "Message"
                    use text area of 5 lines
            visible when (HasAttachment)
                paragraph
                    Attachment.File
                        label is "Attachment"
                    visible when (HasAttachment)
                        button of icon.attach
                            view Attachment.File
                            label is "Attachment"
            two column
                Priority
                Status
            single column
                CreationDateTime
                    label is "CreationDate/Time"
