MobileComposite is a CompositeForm
    title is "Requisition<Requisition>Line<RequisitionLine>"
    called out actions only
    context form is MobileContext
        background is theme color
    restrict action Unreleased.Delete
    Actions
        action is Unreleased.Delete
            hide when invalid
    Distributions is a Panel
        visible when (DistributionExists)
        list is RequisitionLineDistributionsRel.MobileCardviewList
    Comments is a Panel
        visible when (HasComments)
        list is RequisitionLineCommentRel.PfiCommentsCardList
    ApprovalHistory is a Panel
        visible when (HasWorkunit)
        form is ApprovalHistory
    Additional is a Panel
        form is MobileAdditional
