PeriodEndDistributionForm is a CompositeForm
    is primary
    propagate as a drill
    title is "Distribution"
    context form is PeriodEndDIstributionContextForm
        background is theme color
    Main is a Panel
        form is PeriodEndDistributionMain
    ApprovalInformation is a DashBoardPanel
        visible when (DisplayApprovalInformationPanel)
        show 4 columns
        ApprovalProgress is a Panel
            visible when (DisplayDistributionApprovalProgressPanel)
            triple width
            form is DistributionProcessIndicatorForm
        RejectHistory is a Panel
            visible when (ShowApprovalCommentsTab)
            triple width
            list is DistributionRejectReasonCodeUsageRel.ReconciliationReasonCodeUsageList
