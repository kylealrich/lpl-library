PayVendorMaintenance is a Form
    is primary
    title is "PayVendor"
    Layout
        paragraph
            visible when (not RecordExists)
                VendorGroup
            visible when (RecordExists)
                VendorGroup
                    display as text
            VendorGroup.Description
                no label
                display as text
        two column distributed
            paragraph
                visible when (not RecordExists)
                    Vendor
                visible when (RecordExists)
                    Vendor
                        display as text
                Vendor.VendorName
                    no label
                    display as text
            paragraph
                visible when (not RecordExists)
                    PayVendor
                visible when (RecordExists)
                    PayVendor
                        display as text
                PayVendor.VendorName
                    no label
                    display as text
        two column distributed
            PayType
            Default
