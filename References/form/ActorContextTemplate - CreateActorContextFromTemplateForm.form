CreateActorContextFromTemplateForm is an ActionForm
    action is CreateActorContextFromTemplate
    title is "CreateActorContextFromTemplate"
    Layout
        single column
            For
                display as horizontal radio buttons
            visible when (For.Actor)
                ParamSpecifyActor
                    label is "RunForSingleActor"
                visible when (ParamSpecifyActor)
                    ParamActor
            visible when (For.ActorGroup)
                ParamActorGroup
                ParamIncludeDescendants
                    display as horizontal radio buttons
            visible when (For.Role)
                ParamRole
            visible when (For.RoleGroup)
                ParamRoleGroup
            ParamDuplicateHandling
                display as horizontal radio buttons
