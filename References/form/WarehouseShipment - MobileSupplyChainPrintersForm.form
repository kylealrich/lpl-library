MobileSupplyChainPrintersForm is a Form
    Layout
        single column
            visible when (IsPrintReport)
                MSCMReportPrinter
                    select is MSCMReportCompanyPrinterSelect
            visible when (IsPrintLabel)
                MSCMLabelPrinter
                    select is MSCMLabelCompanyPrinterSelect
