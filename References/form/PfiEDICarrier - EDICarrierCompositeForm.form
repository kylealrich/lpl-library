EDICarrierCompositeForm is a CompositeForm
    title is "EDICarrier"
    EDICarrier is a Panel
        form is EDICarrierForm
    Encrypt is a Panel
        visible when (Encrypt)
        form is EncryptForm
    Decrypt is a Panel
        visible when (Decrypt)
        form is DecryptForm
