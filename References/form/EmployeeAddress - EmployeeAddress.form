EmployeeAddress is a Form
    is primary
    title is "Address"
    Layout
        two column
            Employee
                label is "EmploymentID"
            Employee.Name.PreferredFirstAndLastName
                no label
                display as text
        single column
            effective date
            Active
                display only
            Description
        single column
            PostalAddress
                form is PostalAddressMinimum
        single column
            visible when (IsMailingAddress)
                IsMailingAddress
                    label is "SendMailToThisAddress"
            visible when (IsPayrollAddress)
                IsPayrollAddress
                    label is "ILiveAtThisAddress"
