ReconciliationAccountCommentActionForm is an ActionForm
    action is Create
    title is "AddComment"
    Layout
        single column
            Subject
                display as required when (true)
            Comment
                use text area of 4 lines
                display as required when (true)
            Attachment.File
                label is "Attachment"
            visible when (not HasAuditorRole)
                ReconCommentType
            visible when (false)
                From
