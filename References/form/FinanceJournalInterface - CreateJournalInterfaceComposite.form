CreateJournalInterfaceComposite is a CompositeForm
    use for action Open.Create
    title is "JournalInterface"
    context form is CreateJournalInterfaceContext
    Actions
        action is save
        action is Update
        action is refresh
    InterfaceLines is a Panel
        visible when (HasInterfaceLines)
        list is FinanceJournalInterfaceLine set.FinanceJournalInterfaceLineList
