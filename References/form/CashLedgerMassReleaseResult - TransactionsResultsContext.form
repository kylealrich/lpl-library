TransactionsResultsContext is a Form
    Layout
        single column
            display as text
                CashManagementGroup
                CashLedgerMassReleaseResult
                CreateDate
                Status
                    raise red alert when (TransactionErrorsExist)
                        mouse over text is "NotAllTransactionsReleased;UnresolvedErrorsExist"
