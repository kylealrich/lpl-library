MenuSecurityForm is a Form
    is primary
    title is "MenuSecurity"
    Layout
        single column
            MenuSecurity.WebApp
            MenuSecurity.Role
            Description
            Configuration
            Status
            CopiedFrom.WebApp
                label is "CopiedFromWebApp"
                display only
            CopiedFrom.Role
                label is "CopiedFromRole"
                display only
            visible when (CanEditDelivered)
                Delivered
                DeliveredConfiguration
                ItemsToMerge
            visible when (not CanEditDelivered)
                Delivered
                    display only
                DeliveredConfiguration
                    display only
                ItemsToMerge
                    display only
