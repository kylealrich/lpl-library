CloseTaskTypeForm is a Form
    is primary
    title is "TaskType"
    Layout
        visible when (HasParent)
            single column
                ParentCloseTaskType
                    label is "Parent"
                    display as text
        single column
            CloseTaskType
                label is "Type"
            Description
            IsDefault
            Active
