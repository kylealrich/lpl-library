POVendorFormContext is a Form
    Layout
        three column distributed
            column1
                paragraph
                    visible when (VendorGroupRecordExists)
                        VendorGroup.RepresentativeText
                            label is "VendorGroup"
                            display as text
                    visible when (not VendorGroupRecordExists)
                        VendorGroup
                        VendorGroup.Description
                            no label
                            display as text
            column2
                paragraph
                    visible when (VendorRecordExists)
                        Vendor.Vendor.DerivedVendorNameID
                            display as text
                    visible when (not VendorRecordExists)
                        Vendor
                        Vendor.Vendor.VendorName
                            no label
                            display as text
            column3
                paragraph
                    GlobalLocationNumber
                        display as text
