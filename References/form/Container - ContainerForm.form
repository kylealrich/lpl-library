ContainerForm is a CompositeForm
    is primary
    title is "Container"
    context form is ContainerContextForm
        background is theme color
    ContainerDetail is a Panel
        form is ContainerDetails
