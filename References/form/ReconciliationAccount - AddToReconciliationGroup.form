AddToReconciliationGroup is an ActionForm
    action is AddToReconciliationGroup
    title is "AddToReconciliationGroup"
    Layout
        paragraph
            ReconciliationAccountStructure
            ReconciliationAccountStructure.Description
                no label
                display as text
        paragraph
            PrmReconciliationGroup
                label is "ReconciliationGroup"
                select is ReconciliationGroupList
            PrmReconciliationGroup.ReconciliationGroup
                no label
                display as text
