AdjustFromTo is a Form
    title is "TransferFrom&ToDetails"
    Layout
        single column
            visible when (IsActiveAdjusted)
                Active
                    display as text
            visible when (IsActiveAdjusted)
                ToActive
                    display as text
            visible when (IsBookRegulation)
                BookRegulation
                    display as text
            visible when (IsBookRegulation)
                ToBookRegulation
                    display as text
            visible when (IsLifeAdjusted)
                Life
                    display as text
            visible when (IsActiveAdjusted)
                ToLife
                    display as text
            visible when (IsLifeRemainingAdjusted)
                LifeRemaining
                    display as text
            visible when (IsLifeRemainingAdjusted)
                ToLifeRemaining
                    display as text
            visible when (IsBasisAdjusted)
                Basis
                    display as text
            visible when (IsBasisAdjusted)
                ToBasis
                    display as text
            visible when (IsMethodAdjusted)
                Method
                    display as text
            visible when (IsMethodAdjusted)
                ToMethod
                    display as text
            visible when (IsDepreciationMethodSwitchedAdjusted)
                DepreciationMethodSwitched
                    display as text
            visible when (IsDepreciationMethodSwitchedAdjusted)
                ToDepreciationMethodSwitched
                    display as text
            visible when (IsInServiceDateAdjusted)
                InServiceDate
                    display as text
            visible when (IsInServiceDateAdjusted)
                ToInServiceDate
                    display as text
            visible when (IsYearPlacedInServiceAdjusted)
                YearPlacedInService
                    display as text
            visible when (IsYearPlacedInServiceAdjusted)
                ToYearPlacedInService
                    display as text
            visible when (IsConventionAdjusted)
                Convention
                    display as text
            visible when (IsConventionAdjusted)
                ToConvention
                    display as text
            visible when (IsLifeInYearsAdjusted)
                LifeInYears
                    display as text
            visible when (IsLifeInYearsAdjusted)
                ToLifeInYears
                    display as text
            visible when (IsYearToDateDepreciationAdjusted)
                YearToDateDepreciation
                    display as text
            visible when (IsYearToDateDepreciationAdjusted)
                ToYearToDateDepreciation
                    display as text
            visible when (IsYearToDateDepreciationOverrideAdjusted)
                YearToDateDepreciationOverride
                    display as text
            visible when (IsYearToDateDepreciationOverrideAdjusted)
                ToYearToDateDepreciationOverride
                    display as text
            visible when (IsLifeToDateDepreciationAdjusted)
                LifeToDateDepreciation
                    display as text
            visible when (IsLifeToDateDepreciationAdjusted)
                ToLifeToDateDepreciation
                    display as text
            visible when (IsSalvageValueAdjusted)
                SalvageValue
                    display as text
            visible when (IsSalvageValueAdjusted)
                ToSalvageValue
                    display as text
            visible when (IsBusinessPercentAdjusted)
                BusinessPercent
                    display as text
            visible when (IsBusinessPercentAdjusted)
                ToBusinessPercent
                    display as text
            visible when (IsPersonalUseCodeAdjusted)
                PersonalUseCode
                    label is "VehicleLimitCode"
                    display as text
            visible when (IsPersonalUseCodeAdjusted)
                ToPersonalUseCode
                    label is "ToVehicleLimitCode"
                    display as text
            visible when (IsFirstYearExpenseAmountAdjusted)
                FirstYearExpenseAmount
                    display as text
            visible when (IsFirstYearExpenseAmountAdjusted)
                ToFirstYearExpenseAmount
                    display as text
            visible when (IsAssetTaxCreditTableAdjusted)
                AssetTaxCreditTable
                    display as text
            visible when (IsAssetTaxCreditTableAdjusted)
                ToAssetTaxCreditTable
                    display as text
            visible when (IsTaxCreditPropertyAmountAdjusted)
                TaxCreditPropertyAmount
                    display as text
            visible when (IsTaxCreditPropertyAmountAdjusted)
                ToTaxCreditPropertyAmount
                    display as text
            visible when (IsTaxCreditAdjusted)
                TaxCredit
                    display as text
            visible when (IsTaxCreditAdjusted)
                ToTaxCredit
                    display as text
            visible when (IsInsuranceCalculationMethodAdjusted)
                InsuranceCalculationMethod
                    display as text
            visible when (IsInsuranceCalculationMethodAdjusted)
                ToInsuranceCalculationMethod
                    display as text
            visible when (IsActualInsuranceValueAdjusted)
                ActualInsuranceValue
                    display as text
            visible when (IsActualInsuranceValueAdjusted)
                ToActualInsuranceValue
                    display as text
            visible when (IsCalculatedInsuranceValueAdjusted)
                CalculatedInsuranceValue
                    display as text
            visible when (IsCalculatedInsuranceValueAdjusted)
                ToCalculatedInsuranceValue
                    display as text
            visible when (IsReplacementCalculationMethodAdjusted)
                ReplacementCalculationMethod
                    display as text
            visible when (IsReplacementCalculationMethodAdjusted)
                ToReplacementCalculationMethod
                    display as text
            visible when (IsCalculatedReplacementValueAdjusted)
                CalculatedReplacementValue
                    display as text
            visible when (IsCalculatedReplacementValueAdjusted)
                ToCalculatedReplacementValue
                    display as text
