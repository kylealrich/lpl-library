MobileInventoryLocationContext is a Form
    title is "InventoryLocation"
    Layout
        two column
            column1
                Picture.File
                    display as portrait image up to 3 lines
                        missing image is random background
            column2
                DerivedInventoryLocationIDName
                    label is "Location"
                    display as text
                paragraph
                    visible when (RecordExists)
                        LocationType
                            display as text
                    Status
                        display as tag
                            color of green when (Status.Active)
                            color of gray when (Status.Inactive)
                            color of gray when (Status.AddInactive)
