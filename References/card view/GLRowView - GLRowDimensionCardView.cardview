GLRowDimensionCardView is a CardView
    main column
        GLRowView
        Description
    button menu
        button of "AddRowToFavorites"
            visible when (not IsActorFavoriteRow)
            action is AddRowToFavorite
        button of "RemoveRowFromFavorites"
            visible when (IsActorFavoriteRow)
            action is RemoveRowFromFavorite
