MyIDMJobsCardView is a CardView
    show labels
    main column
        FileName
            no label
            bold
        CreatedDate
            label is "Created"
            display date and hours and minutes
        Status
            display as tag
                color of green when (Status.Finished)
                color of red when (Status.Failed)
                color of orange when (Status.TimedOut)
                color of blue when (Status.Submitted)
