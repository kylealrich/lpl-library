XiFinanceDimension7StructureCardView is a CardView
    main column
        FinanceDimension7Structure
            bold
        Description
        StatusMessage
            display as tag
                color of yellow when (not Active)
            visible when (not Active)
    right column
        IsEnterpriseStructureMessage
            display as tag
                color of green when (IsEnterpriseStructure)
