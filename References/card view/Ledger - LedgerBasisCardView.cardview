LedgerBasisCardView is a CardView
    main column
        Ledger
            bold
        Description
    right column
        DerivedStatus
            no label
            display as tag
                color of green when (Active)
                color of yellow when (not Active)
