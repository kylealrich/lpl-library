LeaseRateTableMaintenanceCard is a CardView
    show labels
    main column
        LeaseRateTable
            no label
            bold
        AllowOverride
            display as tag
                color of green when (AllowOverride)
                color of red when (not AllowOverride)
        Currency
        LeaseType
    right column
        Description
            no label
