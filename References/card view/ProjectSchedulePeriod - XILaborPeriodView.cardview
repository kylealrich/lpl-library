XILaborPeriodView is a CardView
    show labels
    main column
        DateRange.Begin
            raise yellow alert when (HasNewAssignments)
                mouse over text is "AssignmentsBeginDuringLaborPeriod"
        DateRange.End
            raise yellow alert when (HasEndingAssignments)
                mouse over text is "AssignmentsEndDuringLaborPeriod"
    right column
        ProjectSchedulePeriod
            label is "Period"
