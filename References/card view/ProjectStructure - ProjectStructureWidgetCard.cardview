ProjectStructureWidgetCard is a CardView
    main column
        ProjectStructure
            raise blue alert when (IsEnterpriseStructure)
                mouse over text is "EnterpriseStructure"
        StatusMessage
            display as tag
                color of yellow
            visible when (not Active)
    right column
        Description
