ProjectCardView is a CardView
    main column
        DisplayProject
            raise blue alert when (HasProjectDateRange)
                mouse over text is "Date_Range:<ProjectDateRange.BeginDate>-<ProjectDateRange.EndDate>"
        Description
            raise blue alert when (HasPersonResponsible)
                mouse over text is "Person_Responsible:<PersonResponsible.Name.PreferredFirstAndLastName>"
