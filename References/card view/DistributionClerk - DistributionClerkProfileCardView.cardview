DistributionClerkProfileCardView is a CardView
    left column is representative image
        missing image is random background
            foreground text is "<DistributionClerk.Employee.Name.Initials>"
    main column
        DerivedFirstLastNameID
            bold
        StatusMessage
            no label
            display as tag
                color of red
            visible when (not EmployeeIsActive)
