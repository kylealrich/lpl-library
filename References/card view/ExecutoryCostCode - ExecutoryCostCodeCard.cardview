ExecutoryCostCodeCard is a CardView
    show labels
    main column
        ExecutoryCostCode
            no label
            bold
        Active
            display as tag
                color of green when (Active)
                color of yellow when (not Active)
    right column
        Description
            no label
