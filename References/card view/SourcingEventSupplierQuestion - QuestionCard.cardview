QuestionCard is a CardView
    main column
        DerivedQuestionText
        RequiredQuestion
            no label
            display as tag
                color of red
            visible when (SourcingEventQuestionRel.ResponseRequired)
        button row
            button of "<Attachment.Title>" with icon.attach
                visible when (HasAttachment)
                view Attachment.File
