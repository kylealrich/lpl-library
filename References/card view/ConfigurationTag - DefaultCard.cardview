DefaultCard is a CardView
    hide labels
    main column
        ConfigurationTag
            bold
            raise red alert when (not Active)
                mouse over text is "Inactive"
        Owner
