ReportsConsoleCard is a CardView
    hide labels
    main column
        Name
            no label
            bold
            raise red alert when (ConfigEntityRel.Status.Invalid)
                mouse over text is "Invalid"
            raise yellow alert when (ConfigEntityRel.Status.Inactive)
                mouse over text is "Inactive"
        Owner
            color of purple
        button row
            button of "UpdateReport"
                action is EditReport
