LineQuestionCard is a CardView
    main column
        DerivedQuestionText
        button row
            button of "<Attachment.Title>" with icon.attach
                visible when (HasAttachment)
                view Attachment.File
    right column
        RequiredTag
            no label
            display as tag
                color of green
            visible when (ResponseRequired)
