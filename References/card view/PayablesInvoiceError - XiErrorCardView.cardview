XiErrorCardView is a CardView
    main column
        ErrorLevel
            raise red alert when (ErrorLevel.Error)
            raise yellow alert when (ErrorLevel.Warning)
        ErrorClass
        ErrorMessage
