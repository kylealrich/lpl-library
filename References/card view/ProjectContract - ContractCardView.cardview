ContractCardView is a CardView
    show labels
    main column
        DisplayContract
            no label
            bold
            raise blue alert when (HasProjectContractComments)
                mouse over text is "CommentsExists"
        Description
            no label
        text of "<ProjectContract.Project.ProjectDateRange.BeginDate>_-_<ProjectContract.Project.ProjectDateRange.EndDate>"
        AmountDescriptionMessage
            no label
