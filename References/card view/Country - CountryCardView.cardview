CountryCardView is a CardView
    left column is FlagPicture.File
        missing image is missingimageFlag
    main column
        Country
        Name
            raise red alert when (OnWatchList)
                mouse over text is "OnWatchList"
