LeaseDateCodeMaitenanceCard is a CardView
    show labels
    main column
        LeaseDateCode
            no label
            bold
        Active
            display as tag
                color of green when (Active)
                color of red when (not Active)
    right column
        Description
            no label
