ReportsConsoleCard is a CardView
    hide labels
    main column
        UserFolder(UserFolder).Name
            no label
            bold
            raise red alert when (Status.Invalid)
                mouse over text is "Invalid"
            raise yellow alert when (Status.Inactive)
                mouse over text is "Inactive"
        DerivedReportType
        UserFolder(UserFolder).Owner
            color of purple
