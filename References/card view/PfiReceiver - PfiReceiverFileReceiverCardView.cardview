PfiReceiverFileReceiverCardView is a CardView
    main column
        PfiReceiver
        Description
        PfiFlowDefinition
    right column
        FileReceiver.FileName
        Status
            raise red alert when (InFailedOrActiveWithIssueStatus)
            raise yellow alert when (InTransitionStatus)
            raise green alert when (Status.Active)
