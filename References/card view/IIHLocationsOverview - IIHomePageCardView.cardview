IIHomePageCardView is a CardView
    show labels
    left column is actor.agent(Employee).ResourcePicture.File
    main column
        actor.agent(Employee).Name.PreferredFirstAndLastName
            no label
            bold
        actor.agent(Employee).Employee
            no label
        actor.agent(Employee).HROrganization
            label is "Organization"
