MobileReceivablePaymentHeaderCardView is a CardView
    show labels
    main column
        Company
        ReceivablePaymentHeader
            label is "Batch"
        BatchTotal
            raise yellow alert when (TotalsDoNotMatch)
                mouse over text is "ControlTotalsNotInBalance"
        GeneralLedgerDate
            label is "GlobalLedgerDate"
    right column
        Status
            no label
