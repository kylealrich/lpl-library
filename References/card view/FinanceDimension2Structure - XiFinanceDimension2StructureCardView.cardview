XiFinanceDimension2StructureCardView is a CardView
    main column
        FinanceDimension2Structure
            label is "Structure"
            bold
        Description
        StatusMessage
            display as tag
                color of yellow when (not Active)
                color of green when (Active)
        IsEnterpriseStructureMessage
            display as tag
                color of green when (IsEnterpriseStructure)
