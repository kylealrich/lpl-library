PfiWorkunitTrackingCardView is a CardView
    main column
        WorkTitle
        Actor
    right column
        text of "WorkUnit:<PfiWorkunit>"
        Status
            raise red alert when (IsFailed)
                mouse over text is "Failed"
            raise yellow alert when (IsCanceled)
                mouse over text is "Canceled"
