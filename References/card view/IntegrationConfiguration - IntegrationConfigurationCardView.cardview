IntegrationConfigurationCardView is a CardView
    left column is IntegrationImage.File
        missing image is interfaceAmethystOutline
    main column
        Integration
            bold
        button row
            button of "Configure" with icon.fsmEdit
                visible when (ConfigureLakehouseOperationalDataStoreActive)
                action is ConfigureLakehouseOperationalDataStore
    right column
        ActiveDerivedMessage
