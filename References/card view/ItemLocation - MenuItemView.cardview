MenuItemView is a CardView
    show labels
    main column
        blank line
        text of "<MobileItemLabel>"
            bold
        Company
        InventoryLocation
        button row
            button of "PlaceOrder"
                form invoke is CreateKitchenOrderWizard
                label is "_"
    right column
        DerivedTotalKitchenOrders
            no label
            display as tag
                color of red
