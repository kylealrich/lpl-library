ScenarioViewBasic is a CardView
    show labels
    main column
        ScenarioDescription
            no label
            header4
            bold
        Basis
        Status
            no label
            display as tag
                color of gray when (IsNotActiveStatus)
                color of yellow when (Status.Preparing)
                color of green when (Status.Ready)
