XiReportingChartCardView is a CardView
    main column
        ReportingChart
            bold
        Description
        StatusMessage
            display as tag
                color of yellow when (not Active)
            visible when (not Active)
    right column
        IsEnterpriseChartMessage
            display as tag
                color of green when (IsEnterpriseChart)
