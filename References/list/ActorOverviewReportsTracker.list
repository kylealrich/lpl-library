ActorOverviewReportsTrackerList is a List
    is primary
    title is "ActorResponsibilityOverview"
    single select
    initially display search
    Display Fields
        create stamp
            label is "Date"
        InputDataArea
        HCMStatusMessage
            column visible when (IsHCM)
        FSMStatusMessage
            column visible when (IsFSM)
        OverallStatus
            display as tag
                color of yellow when (OverallStatus.Started)
                color of green when (OverallStatus.Completed)
                color of red when (OverallStatus.Failed)
        update stamp
            label is "LastUpdate"
    open action is restricted
    Actions
        action is OverviewReport.GetIdentityActorsData
            label is "CreateReport"
        action is refresh
    Detail Sections
        ActorOverview
            list is ActorOverviewReport set.ActorOverviewCompletedReportList
        ActorContext
            list is ActorOverviewReport set.ActorContext
