DocumentGroupList is a List
    title is "DocumentGroupRecords"
    Display Fields
        DocumentGroup
            sort order is ByDocumentGroup
                is default
        create stamp
        button of "View"
            visible when (HasDocument)
            view Document
    Instance Selection
        where (HasDocumentGroup)
