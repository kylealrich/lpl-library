ChangeDisplayOrderLevelOne is a List
    title is "ChangeDisplayOrderLevelOne"
    single select
    initially display search
    Display Fields
        DisplayOrder
            sort order is ByDisplayOrder
                is default
        NewDisplayOrder
            allow update
        Title
        HeaderText
        Description
        AttachmentExists
            label is "AttachmentsExist"
    Instance Selection
        where (TermLevel = 1)
    restrict action CreateFromWizard
