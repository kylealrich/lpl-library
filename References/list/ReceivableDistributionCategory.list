Categories is a List
    title is "ReceivableDistributionCategories"
    initially display search
    implements InlineCreate
    no data message is "No_Distribution_Categories_Exist"
        description is "To_\\CreateA_\\Distribution_\\Category,ClickThe_'plus'_inTheTopRightOr_\\Create_\\Distribution_\\CategoryBelow."
        icon is icon.icon-empty-no-data
        button of "CreateDistributionCategory"
            action is Create
    Display Fields
        Company
            allow update
        ReceivableDistributionCategory
            label is "Category"
            allow update
        Description
            allow update
        TaxCode
            allow update
        TaxExempt
            label is "PreventTaxCodeDefaulting"
            allow update
        AccountingUnitOverrideFlag
            allow update
        IntrastatLineType
            allow update
        DistributionAccount
            allow update
    Instance Selection
        where (IsValidForActorContext)
    open action is restricted
    Actions
        action is copy row
            action icon is copy
            hide when invalid
        action is Create
            label is "_"
        action is save
            label is "_"
        action is save and new
            label is "_"
        action is Delete
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
