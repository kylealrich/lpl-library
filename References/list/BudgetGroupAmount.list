BudgetGroupAmountList is a List
    is primary
    title is "BudgetAmounts_(<BudgetGroup.DerivedCurrency>)"
    single select
    initially display search
    no data message is "Nothing_to_show"
        icon is icon.icon-empty-no-orders-new
    Display Fields
        GeneralLedgerCalendarPeriod
            label is "Period"
        CurrencyAmount
            label is "BudgetAmount"
        YTD
            label is YearToDate
            column visible when (BudgetTemplate.IsYearToDate)
        LTD
            label is LifeToDate
            column visible when (BudgetTemplate.IsLifeToDate)
    open action is restricted
