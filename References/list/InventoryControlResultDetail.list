CompletedMassRequisitionResultDetailList is a List
    title is "Completed"
    single select
    initially display search
    show result set size
    no data message is "No_Records_Completed"
        icon is icon.icon-empty-new-project
    Display Fields
        ScheduledProcedure
            link is ScheduledProcedureRel.ScheduledProcedureNav
        DerivedProcedureDate
            allow derived field filter
        DerivedProcedureTime
            allow derived field filter
        DerivedRequestingLocation
            allow derived field filter
        Requisition
            link is RequisitionRel.RequisitionView
        DerivedRequisitionStatus
            allow derived field filter
    Instance Selection
        where (Status.Completed)
    open action is restricted
