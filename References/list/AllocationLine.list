AllocationLineForBuilderList is a List
    title is "<DerivedLinesUsingBuilderTitle>"
    Display Fields
        Allocation
        AllocationLine
        Description
        AllocationStep
            label is "Step"
            sort order is ByStepNumber
                is default
    restrict action Create
    restrict action Update
    restrict action Delete
    restrict action CopyAllocationLine
    restrict action RunAllocation
    restrict action CheckSourceDetails
    Actions
        action is open
            action icon is open
            label is "_"
            mouse over text is "Open"
