PurgeActionResultActionGroupMembers is a List
    title is "PurgeActionResultsActionGroupMembers"
    Display Fields
        BusinessView
            raise yellow alert when (RaiseYellowAlert)
                mouse over text is untranslatable:"<YellowAlertMessage>"
            raise blue alert when (IsRestored)
                mouse over text is "DataRestored"
            sort order is primary
        BusinessAction
        RunTimeStamp
            default filter operator is greater than or equals
            sort order is ByRunTimeStamp
                is default descending
        EndTimeStamp
    Detail Sections
        PurgedBusinessClasses
            list is PurgeActionResultBusClass set.PurgeActionResultBusClassNestedList
        Data
            list is AllArchivedDataRel.primary
