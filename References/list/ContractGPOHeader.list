ContractGPOHeaderHT2021List is a List
    title is "ContractInterfaceGPOHeaders"
    single select
    initially display search
    Display Fields
        GPOContractNumber
            sort order is ByGPOContractNumber
        ContractDescription
        ManufacturerSupplier
        ContractVendorName
        StartDate
            raise green alert when (IsFutureContract)
                mouse over text is "FutureContract"
        EndDate
        HPGContractTypeCode
        PurchasingChannel
    form is ContractHeaderHT2021Form
    Actions
        action is search
            label is "_"
