AutoNumberByBankTransactionCodeList is a List
    title is "AutoNumbersByTransactionCode"
    initially display search
    no data message is "No_Auto_Transaction_Numbers_By_Transaction_Code_to_view"
        icon is icon.icon-empty-no-tasks
    Display Fields
        CashManagementGroup
        CashLedgerDocumentType
        BankTransactionCode
            label is "TransactionCode"
        CashLedgerAutoTranNumber
            label is "AutoTransactionNumber"
            sort order is ByBankTransactionCode
                is default
        AutoNumberStatus
        CreationType
        DocumentCashCode
        DocumentBankTransactionCode
            label is "DocumentTransactionCode"
    Actions
        action is Create
            action icon is create
            label is "_"
            mouse over text is "Create"
        action is open
            action icon is open
            label is "_"
            mouse over text is "Open"
        action is Delete
            action icon is delete
            label is "_"
            mouse over text is "Delete"
        action is search
            label is "_"
        action is refresh
            label is "_"
