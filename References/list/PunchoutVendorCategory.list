ActivePunchoutVendorCategories is a List
    is default select
    Display Fields
        VendorGroup
        PunchoutVendorCategory
            label is "PunchoutVendorCategory"
        Description
    Instance Selection
        where (IsActive)
