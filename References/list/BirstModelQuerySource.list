BirstModelQuerySources is a List
    is primary
    Display Fields
        ReplicationSet
        BusinessClass
        DisplayName
            allow update
            sort order is ByDisplayName
                is default
        SourceType
            label is "Type"
            allow update
        IncludeInModel
            allow update
    Actions
        action is Create
        action is open
        action is save
        action is Delete
        action is UpdateModel
        action is refresh
