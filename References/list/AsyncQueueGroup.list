QueueGroupList is a List
    is primary
    propagate as a drill
    title is "QueueGroups"
    Display Fields
        AsyncQueueGroup
            label is "QueueGroup"
        Description
        MaxActiveActions
            allow update
        Suspended
            allow update
            raise yellow alert when (Suspended)
                mouse over text is "QueueGroupIsSuspended"
        Members
            label is "Queues"
