BatchReviewList is a List
    title is "PaymentBatchApplications"
    initially display search
    Display Fields
        Company
        ReceivablePaymentHeader
            label is "Batch"
        Currency
        Status
        CashCode
        DepositDate
        GeneralLedgerDate
            label is "GlobalLedgerDate"
        ReceivableProcessLevel
    Instance Selection
        where (IsValidForActorContext)
    form is BatchReviewForm
    Actions
        action is OpenBatchUnreleased.Release
            action icon is release
            hide when invalid
        action is open
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
