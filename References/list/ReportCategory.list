ActiveReportCategorySelectList is a List
    title is "ReportCategories"
    initially display search
    is default select
    Display Fields
        ReportCategory
            sort order is primary
                is default
        TransDescription
    Instance Selection
        where (not Inactive
        and   UsedInReport)
