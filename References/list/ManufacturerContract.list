AttachedManufacturerContracts is a List
    initially display search
    Display Fields
        MfgContractRel.WorkingContractID
            label is "WorkingContractID"
            link is ManufacturerContract
            raise red alert when (ManufacturerContractClosed)
                mouse over text is "ContractIsClosed"
        MfgContractRel.Name
            label is "ManufacturerContractName"
        ManufacturerContractNumber
            label is "ManufacturerContract"
        MfgContractRel.ContractStatus
            label is "ManufacturerContractStatus"
    Actions
        action is helper list
            label is "AttachManufacturerContract"
        action is AddAllMembersToDistributor
            action icon is transfer
            hide when invalid
        action is AddAllLinesToDistributor
            action icon is transfer
            hide when invalid
        action is AddMissingLinesToDistributor
            action icon is transfer
            hide when invalid
        navigation is MaintainMembers
            label is "View_andMaintainPricingMembers"
            hide when invalid
        action is CopyOrMoveToAnotherDistributorContract
            label is "CopyOrMove"
            hide when invalid
        action is open
            label is "_"
