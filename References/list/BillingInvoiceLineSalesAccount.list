BillingInvoiceLineSalesAccountList is a List
    is primary
    title is "SalesAccounts"
    implements InlineCreate
    suppress sub-totals
    Display Fields
        SalesAccount
            allow update
        Description
            allow update
        SalesAmount
            allow update
            total
        CreatedByFES
            column visible when (Company.FrontEndSplitsEnabled)
        FESManuallyUpdated
            column visible when (Company.FrontEndSplitsEnabled)
    open action is restricted
    Actions
        action is Create
            label is "Add_Row"
        action is save
            label is "_"
        action is save and new
            label is "_"
        action is Delete
            label is "_"
        action is search
            label is "_"
