ActiveAddOnChargesAllowances is a List
    title is "ActiveAddOnCharges"
    single select
    is default select
    Display Fields
        AddOnCharge
            label is "Code"
        Description
        SpreadMethod
            label is "SpreadMethod"
        AddOnChargeType
            label is "Type"
        LandedAddOnCharge
            label is "Landed"
    Instance Selection
        where (IsActive)
