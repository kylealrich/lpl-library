ActionConfigurationSelect is a List
    title is "Action"
    Display Fields
        BusinessAction
        ActionType
    Instance Selection
        where (not IsConfiguration
        and   not HasActionConfiguration
        and   not IsActionExtension
        and   not IsAuditCorrection
        and   not IsAuditAction)
