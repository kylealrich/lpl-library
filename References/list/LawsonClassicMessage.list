LawsonClassicMessageList is a List
    is primary
    title is "ClassicMessageList"
    Display Fields
        LawsonClassicMessageGroup
            label is "Group"
        Sequence
            label is "Sequence"
        LctName
            label is "Name"
        Status
            raise red alert when (IsFailed)
            raise yellow alert when (IsHold)
            raise green alert when (IsComplete)
        Token
        ResponseMessage
