ChargeImportDistributions is a List
    is primary
    title is "MaintainInterfaceDistributions"
    implements InlineCreate
    Display Fields
        ProcurementChargeDistributionType
            label is "Type"
            allow update
        DistributionAccount
            allow update
        DistributionAmount
            label is "Amount"
            allow update
    open action is restricted
