IncomingVendorStatementList is a List
    is primary
    title is "IncomingVendorStatements"
    initially display search
    Display Fields
        VendorGroup
        Vendor
        StatementDate
        IncomingVendorStatement
    Actions
        action is PurgeIncomingVendorStatements
            action icon is delete
        action is open
            label is "_"
        action is save
            label is "_"
        action is Delete
            action icon is delete
            label is "_"
            mouse over text is "Delete"
        action is refresh
            label is "_"
