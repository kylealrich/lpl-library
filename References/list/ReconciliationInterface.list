AccountReconciliationInterfaceList is a List
    title is "TransactionInterface"
    initially display search
    no data message is "No_Transaction_Interface_records_have_been_defined"
        icon is icon.icon-empty-no-tasks
    Display Fields
        ReconciliationAccount.ReconciliationCodeBlock
            raise blue alert when (ReconciliationAccount.IsAccountWithinGroup)
                mouse over text is "AccountWithinReconciliationGroup<ReconciliationAccount.ParentReconciliationAccount.ReconciliationGroup>"
        ReconciliationAccount.ReconciliationCodeBlock.GeneralLedgerChartAccount.AccountDescription
        TransactionNumber
        TransactionDate
            label is "Date"
        SystemCode
        SignedAmount.CurrencyAmount
            label is "SignedAmount"
        Currency
        AmountType
        Description
        Reconciled
            raise green alert when (Reconciled)
                mouse over text is "Reconciled"
    form is ReconciliationInterfaceForm
