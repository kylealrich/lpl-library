AssetPeriodCloseResult is a List
    Display Fields
        Book
        BookGroup
        Company
        CompanyGroup
        CreateDate
            label is "CreationDate"
            sort order is ByCreateDate
                is default
        CreatedBy
        Status
            raise red alert when (ErrorsExist)
                mouse over text is "CompletedWithErrors"
    Instance Selection
        where (Source.AssetPeriodClose)
    form is AssetPeriodCloseResultForm
    restrict action DeleteAllReleaseCompleted
    restrict action ReleaseAssets
