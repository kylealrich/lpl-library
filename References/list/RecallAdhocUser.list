AdhocWatcherHelperList is a List
    title is "AdhocWatchers"
    is default select
    Display Fields
        DerivedName
            sort order is ByName
                is default
        Email
        DerivedPhoneDisplay
    Instance Selection
        where (Active
        and   not UserAttachedToNotice)
