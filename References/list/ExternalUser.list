ExternalUserRequestList is a List
    is primary
    title is "ExternalUserRequest"
    Display Fields
        create stamp
            label is "Date"
            sort order is ByStamp
        ActionName
        RawText
    restrict action GetAllExternalEntity
        when (config.EXTERNAL_ENTITY_DEBUG = blank
        or    config.EXTERNAL_ENTITY_DEBUG = false)
    restrict action GetExternalEntity
        when (config.EXTERNAL_ENTITY_DEBUG = blank
        or    config.EXTERNAL_ENTITY_DEBUG = false)
    restrict action DeleteExternalEntity
        when (config.EXTERNAL_ENTITY_DEBUG = blank
        or    config.EXTERNAL_ENTITY_DEBUG = false)
    restrict action AddExternalEntity
        when (config.EXTERNAL_ENTITY_DEBUG = blank
        or    config.EXTERNAL_ENTITY_DEBUG = false)
