ActiveNetTermsCodes is a List
    title is "ActiveNetPaymentTerms"
    Display Fields
        TermsCode
        Description.Desc[1]
            label is "Description"
    Instance Selection
        where (Active
        and   ProcessType.Net)
