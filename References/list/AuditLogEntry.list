AllApplicationChangesList is a List
    title is "ApplicationChanges"
    single select
    always display search
    search form is AllApplicationChangesSearch
    Display Fields
        SystemStamp
        BusinessView
        Actor
        ActionType
        Action
        Status
        ColumnarStatus
        InstanceSelection
            show up to 100 characters
    Instance Selection
        where (Mode.Update
        and   FrameworkType not entered)
    form is ApplicationChangesForm
    restrict action Create
    restrict action Delete
    Actions
        action is ViewSource
        action is search
            label is "_"
        action is refresh
            label is "_"
