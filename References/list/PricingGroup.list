ActivePricingGroups is a List
    is default select
    Display Fields
        ContractGroup
        PricingGroup
        Description
        PricingGroupType
    Instance Selection
        where (Active)
