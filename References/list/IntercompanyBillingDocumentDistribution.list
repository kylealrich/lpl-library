BillingDocumentExpenseDistributionList is a List
    title is "PayablesExpenseDistributions"
    implements InlineCreate
        allow form create
    show as simple list
    Display Fields
        GeneralLedgerAccount
            label is "Account"
            allow update
        DistributionAmount
            label is "Amount"
            allow update
            total
        Description
            allow update
    Instance Selection
        where (Type.Payables)
    form is BillingDocumentRevenueAndExpenseDistributionForm
    restrict action CreateRevenueDistribution
    Actions
        inline create
            action icon is create
            label is "CreateOnList"
            hide when invalid
        action is CreateExpenseDistribution
            action icon is create
            label is "_"
            hide when invalid
        action is open
            action icon is open
            label is "_"
            hide when invalid
        action is save
            label is "_"
            hide when invalid
        action is save and new
            label is "_"
            hide when invalid
        action is search
            action icon is filter
            label is "_"
        action is DeleteRevenueAndExpense
            action icon is delete
            label is "_"
            hide when invalid
