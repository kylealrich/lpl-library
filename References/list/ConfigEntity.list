CompareAnalysisList is a List
    title is "ImpactedConfigurations"
    show 20 lines
    single select
    initially display search
    search form is ConfigurationConsoleSearchForm
        use as filter
    card view is DefaultConfigurationsCard
        row separator is horizontal rule
        sort order is ByBusinessView
            is default
            label is "ByBusinessView"
    Display Fields
    Instance Selection
        where (HasPreviousVersionChange)
    open action is restricted
    restrict action DisableDataTranslation
    restrict action EnableDataTranslation
    restrict action AddSubject
    restrict action RemoveSubject
    Actions
        action is search
            label is "_"
    Detail Sections
        ApplicationVersionComparison
            form is PreviousVersionDifferenceForm
        Application_vs_ConfigurationComparison
            form is ConfigurationDifferenceForm
