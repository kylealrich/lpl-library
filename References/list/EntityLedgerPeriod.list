ClosedEntityLedgerPeriodForEntityList is a List
    title is "FinalPeriods"
    single select
    initially display search
    default Alpha filter operator is equals
    Display Fields
        Ledger
        GeneralLedgerClosePeriod.PeriodName
            label is "Period"
            sort order is primary
                is default descending
        GeneralLedgerClosePeriod.GeneralLedgerCalendarPeriod.Date
            label is "EndDate"
        AccountingEntity
        AccountingEntity.Name
    Instance Selection
        where (PeriodStatus.Final)
    Actions
        action is search
            label is "_"
