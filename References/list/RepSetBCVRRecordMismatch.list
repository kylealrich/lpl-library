RecordMismatches is a List
    Display Fields
        RecordUniqueID
            column visible when (RepSetBCValidationResult.HasAnyUidRecords)
        Keys
        button of "OpenRecord"
            action is OpenRecord
        MismatchType
            raise blue alert when (FromArchive)
                mouse over text is "RecordResidesInArchiveStorage"
        Notes
            raise yellow alert when (Type.Warning)
    restrict action OpenRecord
    Actions
        action is TouchAllRecordsForResult
        action is search
        action is saved searches
