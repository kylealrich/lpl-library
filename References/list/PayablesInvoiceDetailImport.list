AllInvoiceDetails is a List
    title is "InvoiceInterfaceErrors"
    Display Fields
        DerivedPurchaseOrder
            label is "PurchaseOrder"
            link is DerivedPurchaseOrderNavigation
            column visible when (not ServiceContractInvoice)
        MatchInvoiceImportRel.ServiceContract
            label is "ServiceContract"
            column visible when (ServiceContractInvoice)
        Company
        Invoice
        LineNumber
            allow update
        Item
            allow update
            column visible when (not ServiceContractInvoice)
        VendorItem
            allow update
            column visible when (not ServiceContractInvoice)
        ItemDescription
            label is "Description"
            allow update
        EnteredQuantity
            label is "Quantity"
            allow update
        EnteredUOM
            label is "UOM"
            allow update
        UnitCost
            allow update
        button of "More"
            sub form is More
        DerivedErrorMessage
            label is "ErrorMessage"
    Instance Selection
        where (RecordInError)
    open action is restricted
    restrict action Create
    Actions
        action is save
            label is "_"
        action is save and new
            label is "_"
            hide when invalid
        action is search
            label is "_"
        action is refresh
            label is "_"
    Detail Sections
        POLinesForVendorItem
            visible when (SingleCSFPO)
            list is HeaderPOLineByVendorItemRel.POLinesForMatchInterface
        POLinesForItem
            visible when (SingleCSFPO)
            list is HeaderPOLineByItemRel.POLinesForMatchInterface
        POLinesForLineNumber
            visible when (SingleCSFPO)
            list is HeaderPurchaseOrderLineRel.POLinesForMatchInterface
        ThisLinePOLinesForVendorItem
            title is "POLinesForVendorItem"
            visible when (PurchaseOrderEntered)
            list is ThisLinePOLineByVendorItemRel.POLinesForMatchInterface
        ThisLinePOLinesForItem
            title is "POLinesForItem"
            visible when (PurchaseOrderEntered)
            list is ThisLinePOLineByItemRel.POLinesForMatchInterface
        ThisLinePOLinesForLineNumber
            title is "POLinesForLineNumber"
            visible when (PurchaseOrderEntered)
            list is ThisLinePurchaseOrderLineRel.POLinesForMatchInterface
        AllPOLines
            list is MatchInvoiceImportRel.PurchaseOrder.PurchaseOrderLineRel.POLinesForMatchInterface
