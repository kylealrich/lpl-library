ActiveEmployees is a List
    is default select
    Display Fields
        PresentationNameSnapshot
            label is "Name"
            sort order is ByName
                is default
        Employee
            label is "EmploymentID"
            sort order is primary
        UseForMailing.PostalAddress.Municipality
            label is "City"
        UseForMailing.PostalAddress.StateProvince
            label is "StateOrProvince"
    Instance Selection
        where (IsActive)
    restrict action AddToProject
    restrict action AddToProjectTeam
