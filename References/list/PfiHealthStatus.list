HealthStatusList is a List
    is primary
    title is "HealthStatus"
    initially display search
    Display Fields
        PfiHealthStatus.Status
            link is external "<Url>"
            raise red alert when (IsShowAlert)
            raise green alert when (not IsShowAlert)
        WorkunitStatusCount
            label is "WorkUnitCount"
    open action is restricted
