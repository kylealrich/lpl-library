ManualPaymentsClosingResults is a List
    title is "ManualPaymentsClosingResults"
    Display Fields
        VendorGroup
        PayGroup
        ProcessGroup
        PayablesJobResult
            sort order is primary
                is default descending
        Status
            display as tag
                color of green when (Status.Complete)
                color of red when (Status.Incomplete)
        RunTime
    Instance Selection
        where (JobType.PaymentClose)
    form is ManualPaymentsCloseCompositeForm
    restrict action PerformVendorBalanceYearEnd
    restrict action Create
    restrict action CreateVendorPaymentReport
    restrict action Delete
    Actions
        action is PurgePayablesJobResults
            label is "PurgeResults"
        action is open
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
