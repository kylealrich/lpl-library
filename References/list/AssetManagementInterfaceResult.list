AssetManagementInterfaceResults is a List
    is primary
    title is "AssetImportResults"
    initially display search
    no data message is "No_Results_to_show"
        description is "Run_Perform_Asset_Interface_to_view_results"
        icon is icon.icon-empty-no-data
    Display Fields
        RunGroup
        FinanceEnterpriseGroup
        AssetManagementInterfaceResult
            sort order is primary
                is default descending
        create stamp
            label is "CreationDate"
        create stamp.actor
            label is "CreatedBy"
        Status
            raise red alert when (Incomplete)
                mouse over text is "Incomplete"
    restrict action Delete
    Actions
        action is AssetInterface
            action icon is hidden
            label is "PerformAssetInterface"
        action is open
            action icon is open
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
        action is Purge
            action icon is delete
            label is "_"
