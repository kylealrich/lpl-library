AssetItemFundAuditList is an AuditList
    title is "AssetItemFundAuditHistory"
    initially display search
    implements AuditCompare
        show changed values on right
    Display Fields
        initiating action
        action
        effective date
            label is "ActionDate"
            sort order is ByEffectiveDate
                is default descending
        actor
            sort order is ByActor
        Asset
        AssetItem
        AssetItemFund
    Instance Selection
        where (IsValidForActorContext)
    Actions
        action is audit compare
        action is export to pdf
            action icon is print
            label is "Print"
