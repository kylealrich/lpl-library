AsyncAuditSetupList is a List
    is primary
    title is "AuditSetup"
    single select
    show as simple list
    suppress standard toolbar
    Display Fields
        AsyncAuditSetup
        button of "Review"
            form is primary
            label is "_"
