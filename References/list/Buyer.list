ActiveBuyers is a List
    Display Fields
        Buyer
        Buyer.Employee.PresentationNameSnapshot
            label is "Name"
            disable translation filtering
        OverrideContractCost
        OverrideLastPOLastCost
        GlobalLocationNumber
            label is "G\\L\\N"
        Reference1
        Reference2
    Instance Selection
        where (IsActive)
