ActiveLandingPageCVList is a List
    title is "Active"
    search form is ActiveSearchForm
        use as filter
    card view is LandingPageCV
        row separator is horizontal rule
    Display Fields
    Instance Selection
        where (ActiveDefault)
    form is LandingPageCF
    restrict action Create
    restrict action UserCreate
    restrict action UserNonWebappCreate
    restrict action UpdateTemplate
    restrict action DeleteTemplate
    restrict action MassInactivateLandingPages
    restrict action MassActivateLandingPages
    Actions
        action is CustomCreate
        action is Copy
    Detail Sections
        Details
            link is "<DesignerURL>"
