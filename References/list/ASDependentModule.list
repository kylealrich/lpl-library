ASDependentModuleList is a List
    is primary
    title is "_"
    single select
    implements InlineCreate
    show as simple list
    no data message is "No_Prerequisite_Modules"
        button of "Create"
            action is Create
    Display Fields
        ASDependentModule
        ASDependentModule.ModuleName
    Actions
        action is Create
            action icon is create
        action is save
            action icon is save
        action is search
            action icon is search
        action is Delete
            action icon is delete
