AllReconAdjustments is a List
    no data message is "No_Reconciliation_Variance_Adjustments_to_view"
        icon is icon.icon-empty-no-tasks
    Display Fields
        BankTransactionCode
        TransactionNumber
            raise red alert when (BudgetEditErrorExists)
                mouse over text is "<DerivedFirstErrorMessage>"
        IssuedBankAmount
            label is "IssuedAmount"
        ReconciliationData.CurrencyAmount
            label is "ReconcileAmount"
