AmendmentCardList is a List
    title is "AmendmentsToThisEvent"
    single select
    show as simple list
    disable select in webui
    card view is AmendmentCard
    called out actions only
    Display Fields
    open action is restricted
    Actions
        action is refresh
