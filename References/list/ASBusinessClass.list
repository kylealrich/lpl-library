BusinessClassList is a List
    is primary
    title is "_"
    show 40 lines
    single select
    initially display search
    show as simple list
    Display Fields
        BusinessClass
        Description
            raise yellow alert when (Inactive)
                mouse over text is "Inactive"
        DisplayOrder
            label is "Line"
            sort order is ByDisplayOrder
                is default
        NewDisplayOrder
            label is "NewLine"
            allow update
    Actions
        action is Create
            action icon is create
        action is Update
            action icon is save
        action is search
            action icon is search
        action is Delete
            action icon is delete
