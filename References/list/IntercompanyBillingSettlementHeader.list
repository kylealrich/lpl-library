AllOpenSettlements is a List
    is report for ApplicationAdministrator,FinancialsViewer,FinancialsProcessor,IntercompanyBillingSpecialist
        category is "Settlement"
    title is "AllOpenSettlements"
    single select
    initially display search
    show as simple list
    Display Fields
        IntercompanyBillingSettlementHeader.SettlementID
            label is "SettlementID"
        PaymentDate
            sort order is ByPaymentDate
                is default descending
        PaymentAmount
        SettlementCurrency
            no label
        group label is "Payables"
            IntercompanyBillingSettlementHeader.PayablesCompany
                label is "Company"
            IntercompanyBillingSettlementHeader.PayablesCashCode
                label is "CashCode"
            IntercompanyBillingSettlementHeader.PayablesBankTransactionCode
                label is "BankCode"
        group label is "Receivables"
            ReceivableCompany
                label is "Company"
            ReceivableCashCode
                label is "CashCode"
            ReceivableBankTransactionCode
                label is "BankCode"
    Instance Selection
        where (Status.Unreleased)
    Actions
        action is Release
            action icon is release
            hide when invalid
        action is Create
            action icon is create
            label is "_"
        action is open
            action icon is open
            label is "_"
        action is Delete
            action icon is delete
            label is "_"
        action is search
            action icon is filter
            label is "_"
