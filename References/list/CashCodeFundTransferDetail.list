CashCodeFundTransferDetailList is a List
    is primary
    title is "CashCodeFundTransferDetail"
    implements InlineCreate
    no data message is "No_Cash_Code_Fund_Transfer_Details_to_view"
        icon is icon.icon-empty-no-tasks
    Display Fields
        TransferAccountStructure
            allow update
        TransferAmount
            allow update
    Actions
        action is Create
            action icon is create
            label is "_"
            hide when invalid
        action is open
            label is "_"
            hide when invalid
        action is save
            label is "_"
            hide when invalid
        action is save and new
            label is "_"
            hide when invalid
        action is Delete
            action icon is delete
            label is "_"
            mouse over text is "Delete"
            hide when invalid
        action is search
            label is "_"
        action is refresh
            label is "_"
