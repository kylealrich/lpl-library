AsyncSchedulingForecastList is a List
    is primary
    title is "SchedulingForecasts"
    initially display search
    Display Fields
        AsyncSchedulingForecast
        Description
        LastForecastTime
        TimeSliceSize
        ForecastBuildInProgress
            display as tag
                color of yellow when (ActiveForecastBuildExists)
                color of red when (FailedForecastBuildExists)
            raise red alert when (FailedForecastBuildExists)
                mouse over text is "AForecastBuildHasFailed"
            raise yellow alert when (ActiveForecastBuildExists)
                mouse over text is "AForecastBuildIsInProgress"
    Actions
        action is Create
        action is open
        action is Delete
        action is refresh
        action is BuildScheduleForecast
    Detail Sections
        Forecast
            visible when (ForecastExists)
            list is AsyncForecastSlice set.AsyncForecastSliceList
