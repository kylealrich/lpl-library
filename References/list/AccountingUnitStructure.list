AccountingUnitStructureList is a List
    is primary
    title is "AccountingUnitStructures"
    Display Fields
        FinanceEnterpriseGroup
        AccountingUnitStructure
            label is "Structure"
            raise blue alert when (IsEnterpriseStructure)
                mouse over text is "EnterpriseStructure"
            sort order is ByEnterpriseStructure
                is default
        Description
            raise red alert when (SecurityGroupRequiresRebuild)
                mouse over text is "ChangesToStructureRequiresRebuildOfSecurityGroups"
            raise blue alert when (UsedInASecurityGroup)
                mouse over text is "StructureIsUsedInASecurityGroup"
        Active
