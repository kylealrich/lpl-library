BudgetGroupTotalList is a List
    title is "Obligations_(<BudgetGroup.DerivedCurrency>)"
    single select
    initially display search
    no data message is "Nothing_to_show"
        icon is icon.icon-empty-no-orders-new
    Display Fields
        GeneralLedgerCalendarPeriod
            label is "Period"
        CurrencyAmount
            label is "Amount"
        YTD
            label is YearToDate
            column visible when (BudgetTemplate.IsYearToDate)
        LTD
            label is LifeToDate
            column visible when (DisplayLTD)
        button of "Log"
            visible when (HasLogs)
            link is LogNavigation
            display as link
    open action is restricted
