AllProjectContractFESResultList is a List
    title is "FrontEndSplitResults"
    initially display search
    no data message is "No_front_end_split_transactions_selected_for_reallocation._Run_Select_Transactions_For_Reallocation."
        icon is icon.icon-empty-no-data
    Display Fields
        ProjectContract.DisplayContract
            label is "<FinanceEnterpriseGroup.ProjectLabel>Contract"
        SequenceNumber
            label is "Sequence"
        RunTime
            sort order is ByRunTime
        AccountingEntity
        AccountingEntityGroup
        SelectedDateRange.Begin
            label is "SelectedPostingDateBegin"
        SelectedDateRange.End
            label is "SelectedPostingDateEnd"
        SelectedThroughDate
            label is "TransactionThroughDate"
        TransactionCount
        PostingDate
        JournalizeGroup
            link is GlobalLedgerJournalControl
            raise red alert when (InterfaceTransactionsExist)
                mouse over text is "TransactionInterfaceHasErrors"
    open action is restricted
