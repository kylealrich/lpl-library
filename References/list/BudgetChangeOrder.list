AllocationBudgetChangeOrders is a List
    title is "BudgetChangeRequests"
    Display Fields
        Scenario
            label is "Budget"
        BudgetChangeOrder
            label is "ChangeRequest"
            raise red alert when (HasError)
                mouse over text is "<ErrorMessage>"
            sort order is ByChangeOrderDescending
                is default
        Description
        Status
            display as tag
                color of green when (Status.Released)
                color of yellow when (not Status.Released)
        Currency
        DefaultPeriod
        button of "<CommentsMessage>"
            link is Comments
            display as link
