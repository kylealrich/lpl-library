Distributions is a List
    implements InlineCreate
    no data message is "No_Non_AR_Payment_Import_Distributions_Exist"
    Display Fields
        DistributionAccount
            label is "GlobalLedgerAccount"
            allow update
        TransactionAmount
            allow update
        TransactionTaxableAmount
            label is "TaxableAmount"
            allow update
        TaxCode
            allow update
        TaxOriginCrossReference
            allow update
            column visible when (ShowTaxOriginCrossReference)
    restrict action DeleteAllDistributionsFromRunGroup
