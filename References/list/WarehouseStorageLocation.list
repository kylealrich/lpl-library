ActiveWarehouseStorageLocations is a List
    title is "_"
    single select
    initially display search
    implements InlineCreate
        allow form create
    called out actions only
    Display Fields
        WarehouseStorageLocation
        Name
            allow update
        WarehouseZone
            allow update
        WarehouseZoneComponent
            allow update
        WarehouseZoneComponent.WarehouseComponentType
        Row
            allow update
        WarehouseZoneComponent.WarehouseAisle
    Instance Selection
        where (Active)
    Actions
        action is Create
            label is "_"
        action is save
            label is "_"
        action is save and new
            label is "_"
        action is open
            label is "_"
        action is Delete
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
