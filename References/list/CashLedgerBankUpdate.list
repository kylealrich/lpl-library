CashLedgerBankUpdateList is a List
    is primary
    title is "BankUpdates"
    initially display search
    no data message is "There_are_currently_no_Bank_Updates"
        icon is icon.icon-empty-no-tasks
    Display Fields
        CashManagementGroup
        CashLedgerBankUpdate.RunGroup
        CashLedgerBankUpdate.CashManagementAccount
        CashLedgerBankUpdate.SequenceNumber
            raise red alert when (HasErrorMessage)
                mouse over text is "<ErrorMessage>"
        TransactionAmount
        CashLedgerUpdateAction
        ActionDate
        CheckNumber
        StatusReasonCode
        StatusReasonDescription
