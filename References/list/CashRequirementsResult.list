CashRequirementsResults is a List
    is primary
    title is "CashRequirementsResults"
    single select
    no data message is "There_are_currently_no_Cash_Requirements_Results"
        icon is icon.icon-empty-no-tasks
    Display Fields
        VendorGroup
        PayGroup
        ProcessGroup
        CashRequirementsResult
            sort order is primary
                is default descending
        create stamp
            label is "CreationDate"
        create stamp.actor
            label is "CreatedBy"
        Status
            raise red alert when (Incomplete)
                mouse over text is "Incomplete"
    restrict action PurgeHistoricalCashRequirementsResults
