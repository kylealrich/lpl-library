MyItemListWidget is a List
    title is "SearchMyOrderedItems"
    implements HorizontalScrolling
        freeze first column
    Display Fields
        Item
        Item.Description
    Instance Selection
        where (IsMyOrderedItem)
    open action is restricted
