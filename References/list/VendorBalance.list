CustomerBalanceInterfaceList is a List
    title is "CustomerVendorBalanceForCustomer<Vendor.Customer>-<Vendor.Customer.Name>AndVendor<Vendor>-<Vendor.VendorName>"
    Display Fields
        Company
        VendorLocation
            label is "VendorLocation"
        Company.GeneralLedgerCompany.Currency
        CompanyCustomerRel.CurrentBalance
            label is "CompanyCustomerBalance"
        CurrentBalance
            label is "CompanyVendorBalance"
        TransNetCustomerBalance
            label is "NetCustomerBalance"
            raise red alert when (HasCurrencyMismatch)
                mouse over text is "VendorBalanceCurrencyIs<BalanceCurrency>"
    open action is restricted
    restrict action Update
    restrict action Delete
    restrict action Create
