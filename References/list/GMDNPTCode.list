ActiveGMDNPTCodes is a List
    title is "ActiveGMDN_Terms"
    is default select
    Display Fields
        ItemGroup
        GMDNPTCode
            label is "Code"
        Name
    Instance Selection
        where (Active)
    restrict action AttachToTierQualifier
