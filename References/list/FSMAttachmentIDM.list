FSMAttachmentIDMFailedList is a List
    title is "FailedAttachmentsToIDM"
    Display Fields
        BusinessObjectReference.BusinessClassName
        DocumentType
        LastActionExecuted
        Status
            raise red alert when (FailedAttachment)
        DateAndTime
    Instance Selection
        where (FailedAttachment)
    Actions
        action is DeleteOnIDM
            action icon is delete
            hide when invalid
            valid when (LastActionExecuted.DeleteOnIDM)
        action is open
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
    Detail Sections
        Details
            form is FSMAttachmentDetailsForm
