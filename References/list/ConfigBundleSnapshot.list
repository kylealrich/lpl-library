AppliedConsoleList is a List
    title is "AppliedSnapshots"
    show 20 lines
    single select
    always display search
    search form is ImportConsoleSearchForm
        use as filter
    card view is AppliedCard
        row separator is horizontal rule
        sort order is ByLastAppliedTime
            is default descending
            label is "ByLastImportTime"
        sort order is primary
            label is "BySnapshotKey"
    Display Fields
    Instance Selection
        where (WasApplied)
    open action is restricted
    restrict action CreateSystemSnapshot
    restrict action ValidateSnapshots
        when (RestrictFGSysValidateConfigs)
    Actions
        action is search
            label is "_"
    Detail Sections
        SnapshotDetails
            list is ConfigBundleSnapshotItem set.AppliedConsoleConfigurationList
        RollbackDetails
            visible when (CanRollback)
            list is ConfigBundleSnapshotItem set.RollbackConfigurationList
