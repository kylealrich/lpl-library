AdminAllFoldersList is a List
    title is "AllFolders"
    Display Fields
        Name
            sort order is ReportByName
                is default
        Type
        Owner
            raise blue alert when (ActorCreatedReport)
                mouse over text is "OwnedByMe"
        BusinessClass
        ListName
        update stamp
            label is "LastUpdate"
    Instance Selection
        where (IsFolder)
    restrict action Create
    restrict action CreateListReport
    restrict action CreatePageReport
    restrict action CreateManualReport
    Actions
        action is export to csv
            action icon is export
        action is PurgeFoldersForActor
            label is "PurgeReportsForActor"
        action is Delete
