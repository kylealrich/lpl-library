ASConfigurationList is a List
    is primary
    title is "ApplicationSetupConfigurations"
    single select
    show as simple list
    Display Fields
        ASConfiguration
            label is "Name"
            header4
            bold
        Description
            raise blue alert when (BlueAlert)
                mouse over text is "<BlueAlertMessage>"
        Delivered
            display as tag
                color of blue when (Delivered)
            raise yellow alert when (Inactive)
                mouse over text is "Inactive"
        IsPrimary
            display as tag
                color of blue when (IsPrimary)
        button of "ViewLogs"
            visible when (ShowLogs)
            link is LogRecords
