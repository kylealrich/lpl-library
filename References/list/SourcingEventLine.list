BestAndFinalResponseLines is a List
    title is "EventLines"
    search form is AdvancedSearchForm
        use as filter
    Display Fields
        SourcingEventLine
            label is "Line#"
            show up to 7 characters
            raise yellow alert when (AlternateResponsesExist)
                mouse over text is "AlternateResponsesExistForThisLine"
        Name
        ItemNumber
            label is "Item"
        CommodityCode
        CommodityCode.Description
            label is "CommodityDescription"
        Quantity
        UOM.UnitOfMeasure
        DerivedOutputType
            label is "OutputType"
        DerivedStatus
            label is "Status"
        PerUnitEstimate
            column visible when (SourcingEvent.HasPerUnitEstimateLines)
        DerivedExtendedEstimate
            label is "ExtendedEstimate"
            column visible when (SourcingEvent.HasPerUnitEstimateLines)
        DerivedPerUnitEstimateAOC
            label is "PerUnitEstimateAOC"
            column visible when (SourcingEvent.HasPerUnitEstimateLineAOCs)
        DerivedNetUnitPriceEstimate
            label is "NetUnitPriceEstimate"
            column visible when (SourcingEvent.HasPerUnitEstimateLineAOCs)
        DerivedTotalEstimate
            label is "TotalEstimate"
            column visible when (SourcingEvent.HasPerUnitEstimateLineAOCs)
        CancelledQuantity
            column visible when (SourcingEvent.HasCancelledLineQuantity)
        TotalAwardQuantity
            label is "TotalAwardQuantity"
            raise yellow alert when (QtyAlert)
                mouse over text is "AwardQuantityDoesNotEqualLineQuantity"
    Instance Selection
        where (Requisition not entered
        or    Requisition entered
        and   RequisitionRel exists)
    restrict action Open.Update
    restrict action Open.Create
    restrict action Draft.CreateFromWizard
    Actions
        action is Awarded.Approved.CreateOutput
            action icon is export
            hide when invalid
        action is Awarded.Approved.ChangeAward
            action icon is edit
            hide when invalid
        action is Open.FinalAward
            action icon is award
            hide when invalid
        action is Open.DeleteAward
            action icon is delete
            hide when invalid
        action is Cancelled.Reinstate
            action icon is undo
            hide when invalid
    Detail Sections
        LineResponsesBySupplier
            list is SecondStepsRel.SupplierByLine
