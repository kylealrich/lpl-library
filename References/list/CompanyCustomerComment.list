ARPaymentCommentsList is a List
    title is "Comments"
    implements InlineCreate
    no data message is "No_Payment_Comments_Exist"
    Display Fields
        BeginDate
        EndDate
        PrintCode
        Comment
            label is "Description"
    Instance Selection
        where (CommentType.TransactionComment
        and   TransactionType.CustomerPayment)
    form is ARPaymentComment
    create action is CreateARPaymentComment
    restrict action Create
    restrict action CreateCustomerComment
    restrict action CreateTransactionComment
    restrict action CreateOrderEntryComment
    Detail Sections
        Attachments
            visible when (RecordExists)
            list is CompanyCustomerCommentAttachment set.Attachments
