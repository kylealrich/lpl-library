AccountSigners is a List
    single select
    initially display search
    no data message is "No_Account_Signers_to_view"
        icon is icon.icon-empty-no-tasks
    Display Fields
        CashManagementAccount.CashManagementLocation
        CashManagementAccount.LegalEntity
        CashManagementAccount.FinancialInstitution
        CashManagementAccount
        CashManagementAccount.Title
            label is "AccountTitle"
        FinanceResource
            label is "ResourceId"
            raise red alert when (InactiveResource)
                mouse over text is "ResourceIsInactive"
        FinanceResource.PreferredFirstAndLastName
            label is "Name"
        group label is "SigningAmount"
            SigningAmount
                label is "Limit"
            NoLimitSigning
            SigningCurrency
        SignerInactivationDate
            label is "DeactivationDate"
    Instance Selection
        where (ActiveSigner)
    restrict action Create
    restrict action Update
    restrict action Delete
