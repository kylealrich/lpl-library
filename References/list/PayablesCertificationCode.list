ActiveCertificationCodes is a List
    is default select
    Display Fields
        VendorGroup
        PayablesCertificationCode
            label is "CertificationCode"
        Description
    Instance Selection
        where (Active)
