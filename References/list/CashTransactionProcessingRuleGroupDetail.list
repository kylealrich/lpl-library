CashTransactionProcessingRuleGroupDetails is a List
    is primary
    title is "ProcessingRules"
    no data message is "No_Processing_Rules_are_assigned_to_Group"
        icon is icon.icon-empty-no-tasks
    Display Fields
        Priority
            sort order is ByPriority
                is default
        NewPriority
            allow update
        CashTransactionProcessingRule
        CashTransactionProcessingRule.Description
    Actions
        action is helper list
            action icon is hidden
            label is "AddProcessingRule"
        action is Create
            action icon is create
            label is "_"
            mouse over text is "Create"
        action is open
            label is "_"
        action is Delete
            action icon is delete
            label is "_"
            mouse over text is "Delete"
        action is search
            label is "_"
