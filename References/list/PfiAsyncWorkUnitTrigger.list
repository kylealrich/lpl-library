AsyncWorkUnitTrigger is a List
    is primary
    Display Fields
        Status
            raise yellow alert when (CanDisplayAlert)
                mouse over text is "WorkUnitWillNotBeDispatched"
        AsyncQueueDefinition
        DataArea
        WorkUnit
        FlowDefinition
        Actor
        Priority
            allow update
            sort order is WorkUnitTriggerByPriority
        Type
    form is AsyncWorkUnitTriggerForm
