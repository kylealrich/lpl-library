ActivatedContractTermVersions is a List
    title is "ActivatedVersions"
    single select
    Display Fields
        VersionOpenStamp
            label is "CreationDate"
            sort order is ByVersionOpenStamp
                is default descending
        button of "ViewTerms"
            link is ViewTermsNav
        button of "CompareToOriginal"
            link is CompareToOriginalNav
        button of "CompareToPrevious"
            visible when (not FirstActivatedVersion)
            link is CompareToPreviousActivatedNav
    form is TermsAndConditions
    Actions
        navigation is ViewTermsNav
            label is "ViewTerms"
        navigation is CompareToOriginalNav
            label is "CompareToOriginal"
        navigation is CompareToPreviousActivatedNav
            label is "CompareToPrevious"
            valid when (not FirstActivatedVersion)
