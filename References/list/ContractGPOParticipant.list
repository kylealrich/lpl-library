ContractGPOParticipantHT2021List is a List
    title is "ContractInterfaceGPOParticipants"
    single select
    initially display search
    Display Fields
        GPOContractNumber
            raise blue alert when (HasDistributorRecord)
                mouse over text is "DistributorRecordExists"
            sort order is ByGPOContractNumber
                is default
        GPOMembership
        MembershipName
        TierLevel
        TierName
        StartDate
        EndDate
    form is ContractGPOHT2021Participant
    Actions
        action is search
            label is "_"
