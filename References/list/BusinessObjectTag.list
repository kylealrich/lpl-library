DashboardUnusedTagList is a List
    title is "UnusedActiveTags"
    no data message is "All_TagsAccountedFor"
    Display Fields
        BusinessObjectTag
        Locked
    Instance Selection
        where (NotUsed
        and   not Locked)
    Actions
        action is search
            label is "_"
