CancelledWorkOrderCardList is a List
    no data message is "NoCancelledWorkOrders"
        icon is icon.icon-empty-new-project-new
    card view is WorkOrderNumberCard
        sort order is primary
            is default descending
            label is "_"
    Display Fields
    Instance Selection
        where (IsCancelled)
    restrict action Unreleased.Create
