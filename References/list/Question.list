AllQuestions is a List
    Display Fields
        ProcurementGroup
        Question
            raise yellow alert when (IsListQuestionWithoutAnswers)
                mouse over text is "NoAnswersEnteredForListQuestion"
        QuestionText
        ResponseType
            raise yellow alert when (NoCorrectAnswerDefined)
                mouse over text is "QuestionIndicatesThereIsACorrectAnswer,ButNoAnswersAreEnteredAsCorrect"
        Category
        SubCategory
        HasDirectConditionalQuestions
            label is "HasConditionalForContracts"
        ReferencedAsConditional
        UsedForContractInterview
            label is "InterviewQuestion"
        Active
            allow update
    restrict action AttachQuestionToContract
    restrict action AttachToEvent
    restrict action AttachToEventLine
    restrict action AttachToSupplierGroup
    Detail Sections
        YesNoDetails
            visible when (ResponseType.YesNo)
            list is QuestionAnswer set.QuestionYesNoAnswers
        YesNoTextDetails
            visible when (ResponseType.YesNoText)
            list is QuestionAnswer set.QuestionYesNoTextAnswers
        ListDetails
            visible when (ResponseType.List)
            list is QuestionAnswer set.QuestionListAnswers
        ConditionalQuestions
            visible when (HasDirectConditionalQuestions)
            list is ConditionalQuestionsDirectRel.ConditionalQuestionsFromQuestion
        ThisQuestionIsConditional
            visible when (ReferencedAsConditional)
            list is ThisQuestionConditionalRel.ThisQuestionIsConditional
