FileCreationTestLineList is a List
    is primary
    title is "Line"
    is default select
    Display Fields
        LineNumber
            label is "Line"
        BaselineLineText
            label is "Baseline"
        TestLineText
            label is "Test"
    restrict action Create
    restrict action Update
    restrict action Delete
