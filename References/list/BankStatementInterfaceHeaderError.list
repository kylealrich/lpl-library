BankStatementInterfaceHeaderErrors is a List
    is primary
    title is "Errors"
    no data message is "No_Bank_Statement_Interface_Errors_to_view"
        icon is icon.icon-empty-no-tasks
    Display Fields
        BankStatementInterface
            raise yellow alert when (SkipErrorPending)
                mouse over text is "ErrorWillBeSkippedOnceImported"
            sort order is ByBankStatementInterface
        ErrorMessage
    open action is restricted
    Actions
        action is SkipError
            label is "SkipError"
            mouse over text is "SkipError"
            hide when invalid
        action is UnskipError
            label is "UnskipError"
            mouse over text is "UnskipError"
            hide when invalid
        action is Delete
            action icon is delete
            label is "_"
            mouse over text is "Delete"
        action is search
            label is "_"
