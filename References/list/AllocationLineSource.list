AllocationLineDetailedSourceForReportList is a List
    title is "Sources"
    initially display search
    no data message is "Doesn'tLookLikeThereAreAnySources"
        icon is icon.icon-empty-no-orders
    Display Fields
        Allocation
        AllocationLine
        AllocationLine.DerivedSourceLedger
            label is "Ledger"
            display when blank
        SourceAddress.AccountingEntity
            label is "<actor.context.FinanceEnterpriseGroup.AccountingEntityLabel>"
            display when blank
        TransientAccountingUnit
            display when blank
            column visible when (AllocationRun.AccountingUnitEnabled)
        TransientAccount
            display when blank
            column visible when (AllocationRun.AccountEnabled)
        TransientProject
            display when blank
            column visible when (AllocationRun.ProjectEnabled)
        TransientFinanceDimension1
            display when blank
            column visible when (AllocationRun.Dimension1Enabled)
        TransientFinanceDimension2
            display when blank
            column visible when (AllocationRun.Dimension2Enabled)
        TransientFinanceDimension3
            display when blank
            column visible when (AllocationRun.Dimension3Enabled)
        TransientFinanceDimension4
            display when blank
            column visible when (AllocationRun.Dimension4Enabled)
        TransientFinanceDimension5
            display when blank
            column visible when (AllocationRun.Dimension5Enabled)
        TransientFinanceDimension6
            display when blank
            column visible when (AllocationRun.Dimension6Enabled)
        TransientFinanceDimension7
            display when blank
            column visible when (AllocationRun.Dimension7Enabled)
        TransientFinanceDimension8
            display when blank
            column visible when (AllocationRun.Dimension8Enabled)
        TransientFinanceDimension9
            display when blank
            column visible when (AllocationRun.Dimension9Enabled)
        TransientFinanceDimension10
            display when blank
            column visible when (AllocationRun.Dimension10Enabled)
    open action is restricted
    Detail Sections
        MultipleOverrides
            list is AllocationLineTempOverrideRel.AllocationLineMultipleOverridesForReportList
