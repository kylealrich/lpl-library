ApplicationRegisteredDevices is a List
    title is "MultifactorAuthenticationDevices"
    single select
    show as simple list
    no data message is "_"
        description is "SecureYourAccountByAddingADeviceForVerification."
        button of "Add"
            action is RegisterMFADevice
    disable field options
    called out actions only
    Display Fields
        DeviceDisplayName
            label is "Device"
        DefaultDevice
            no label
            display as tag
                color of blue
            visible when (IsDefault)
        UIPadding
            no label
        UIPadding
            no label
        UIPadding
            no label
        UIPadding
            no label
        UIPadding
            no label
    open action is restricted
    Actions
        action is RegisterMFADevice
            label is "Add"
            valid when (CanRegisterNewDevice)
        action is RemoveRegisteredDevice
            label is "Remove"
            valid when (CanRemoveDevice)
        action is UpdateDeviceName
            label is "Update"
            valid when (IsTOTPDevice)
        action is RegisterAsDefault
            label is "MakeDefault"
            valid when (not IsDefault)
