BundleTaggedConfigurationList is a List
    title is "BundledConfigurations"
    single select
    search form is BundleTaggedConfigurationSearchForm
        use as filter
    show as simple list
    Display Fields
        ConfigEntityRel.BusinessView
        ConfigEntityRel.EntityType
            left align
        ConfigEntityRel.Name
            raise red alert when (OrphanedRecord)
                mouse over text is "ConfigurationNoLongerPhysicallyExistsEvenInADeletedState"
        ConfigEntityRel.Status
        ConfigEntityRel.Independent
        ConfigurationTag
    Instance Selection
        where (ConfigEntityRel.IsConfiguration)
    open action is restricted
    restrict action Create
    Actions
        action is search
            label is "_"
        action is Delete
            label is "RemoveTag"
    Detail Sections
        Configuration
            form is ConfigEntityRel.TaggedEntityDetailForm
