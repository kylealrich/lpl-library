TaxConfigVersionList is a List
    is primary
    title is "TaxVersions"
    implements InlineCreate
    Display Fields
        TaxConfiguration
        TaxConfigVersion.ThirdParty
        TaxConfigVersion.Version
        TaxConfigVersion.Release
        ReleaseNotes
            label is "Notes"
