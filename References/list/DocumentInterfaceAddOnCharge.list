AddOnCharges is a List
    title is "DocumentInterfaceAddOnCharges"
    implements InlineCreate
    Display Fields
        PurchaseOrderLine
            allow update
            column visible when (ShowPurchaseOrderLineColumn)
        AddOnCharge
            allow update
        AddOnChargeAmount
            allow update
        Quantity
            allow update
        UnitCost
            allow update
    Actions
        action is Create
            action icon is create
            label is "_"
        action is save
            label is "_"
        action is save and new
            label is "_"
            hide when invalid
        action is Delete
            action icon is delete
            label is "_"
            hide when invalid
        action is search
            label is "_"
        action is refresh
            label is "_"
    Detail Sections
        Errors
            visible when (RecordInError)
            list is DocumentInterfaceErrorRel.ErrorMessages
