AllCustomerUpdates is a List
    title is "All"
    Display Fields
        LastRunDate
            sort order is ByDescendingLastRunDate
                is default
        Module
        Description
    Instance Selection
        where (not DataMaintenanceAction)
    form is CustomerUpdatesCompositeForm
    restrict action CheckForUpdates
    restrict action Create
    restrict action Update
    Actions
        action is search
            hide when invalid
            valid when (false)
