AllocationRunJobList is a List
    is primary
    title is "AllocationRunJobs"
    Display Fields
        JobType
            left align
        Allocation
        AllocationLine
        AllocationStep
        AllocationWeightGenerator
            label is "WeightBuilder"
        TransientStatus
            raise red alert when (HasError)
                mouse over text is "<HoverAsyncErrorMessage>"
    open action is restricted
