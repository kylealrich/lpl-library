ValidationResults is a List
    Display Fields
        RepSetBCValidationResult
            raise red alert when (Mismatched)
                mouse over text is "ValidationFailed"
            raise yellow alert when (HasWarnings)
                mouse over text is "ValidationPassedWithWarnings"
            raise green alert when (RaiseGreenAlert)
            sort order is primary
                is default descending
        ValidationEndTime
        LastRefreshStamp
        ValidationStartDate
    Detail Sections
        MismatchRecords
            list is RepSetBCVRRecordMismatch set.RecordMismatches
