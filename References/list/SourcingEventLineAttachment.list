AttachmentCardList is a List
    title is "_"
    single select
    show as simple list
    disable select in webui
    card view is AttachmentCard
    called out actions only
    Display Fields
    open action is restricted
    restrict action Create
    restrict action Delete
