ConversionDistributions is a List
    initially display search
    implements InlineCreate
    no data message is "No_Conversion_Distributions_Exist"
    Display Fields
        RunGroup
            allow update
        Company
            allow update
        InvoiceType
            label is "Type"
            allow update
        Invoice
            allow update
        Sequence
            allow update
        DistributionAccount
            allow update
        TransactionAmount
            allow update
        TaxCode
            allow update
        TransactionTaxableAmount
            allow update
        TaxOriginCrossReference
            allow update
            column visible when (ShowTaxOriginCrossReference)
        ErrorMessage
    Instance Selection
        where (IsValidForActorContext)
    form is ConversionDistribution
    create action is CreateFromConversion
    restrict action Create
    restrict action Update
    Actions
        action is ConvertDistributions
            action icon is transfer
        action is CreateFromConversion
            action icon is create
            label is "_"
        action is open
            label is "_"
        action is save
            label is "_"
        action is save and new
            label is "_"
        action is Delete
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
