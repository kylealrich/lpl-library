AsyncBackgroundGroupList is a List
    is primary
    title is "BackgroundGroups"
    Display Fields
        Name
            sort order is ByName
                is default
        Active
    Detail Sections
        ActiveMembers
            title is "ActiveMembers"
            list is ActiveRequests.primary
        Members
            title is "Members"
            list is Requests.primary
        DependentRequests
            title is "DependentRequests"
            list is DependentRequests.primary
