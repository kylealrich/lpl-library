AllocationListingReportList is a List
    is primary
    Display Fields
        AllocationListingReport
            sort order is ByRunDescending
                is default
        Status
        PostingDate
        AllocationPeriod.DisplayPeriod
            label is "Period"
        Allocation
        AllocationGroup
        create stamp
            label is "RunDate/Time"
        RunEndDateTimeForList
        button of "ListingReport" with icon.pdf-file
            visible when (ListingReport entered)
            view ListingReport
            display as link
    open action is restricted
