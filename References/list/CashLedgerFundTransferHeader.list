BankFundTransfersWithCommitmentsList is a List
    title is "BankFundTransfersWithOpenCommitments"
    initially display search
    no data message is "There_are_currently_no_Bank_Fund_Transfers_With_Open_Commitments"
        icon is icon.icon-empty-no-tasks
    Display Fields
        CashCode
        CashLedgerFundTransferHeader
            label is "TransferNumber"
        TransferType
        Company
        TransferDate
        TransferBankAmount
        Status
    Instance Selection
        where (IsValidForActorContext
        and   OpenGLCommitRelExists)
    form is CashLedgerFundTransferHeaderForm
    Detail Sections
        Commitments
            list is OpenGLCommitDrillRel.DrillGLCommitList
