AsyncSynchronizedGroupList is a List
    is primary
    propagate as a drill
    title is "SynchronizedGroups"
    Display Fields
        AsyncSynchronizedGroup
            label is "Name"
        Description
        Suspended
            allow update
            raise yellow alert when (Suspended)
        NoMembers
