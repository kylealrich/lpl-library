DistributionSpeedEntryList is a List
    title is "Distributions"
    implements InlineCreate
        allow form create
    implements HorizontalScrolling
        freeze first column
    Display Fields
        FinanceCodeBlock
            label is "FinanceStructure"
            allow update
        ExpenseAmount
            allow update
            total
    restrict action Process
    restrict action AttachDocument
    restrict action RemoveAttachedDocument
    Actions
        inline create
            label is "_"
            hide when invalid
        action is save
            label is "_"
        action is Delete
            action icon is delete
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "Refresh_/_ShowTotals"
