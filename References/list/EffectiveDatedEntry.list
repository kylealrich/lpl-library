EffectiveDatedEntryList is a List
    is primary
    initially display search
    Display Fields
        RecordID.BusinessClassName
            link is PreviousPendingRequest
            raise red alert when (RedAlert)
                mouse over text is "<RedMessage>"
            raise yellow alert when (YellowAlert)
                mouse over text is "<YellowMessage>"
        ObjectKey
            label is "Key"
        create stamp
            label is "Created"
        create stamp.actor
            label is "Actor"
