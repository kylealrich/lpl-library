DBImportRequestList is a List
    is primary
    title is "DBImportRequests"
    initially display search
    no data message is "NoDBImportRequestsFound"
    Display Fields
        BusinessClass
        DBImportRequest
            raise red alert when (RaiseRequestAlert)
                mouse over text is "<RequestAlertMessage>"
            sort order is primary
                is default descending
        Status
        StartTime
        EndTime
        NumberOfRecords
        NumberOfExceptions
            raise red alert when (RaiseAlert)
        ElapsedTimeComponentsText
        button of "Log"
            view RunLog
                using mime type "text/plain"
