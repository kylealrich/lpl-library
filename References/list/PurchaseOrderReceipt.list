AllPurchaseOrderReceipts is a PurchaseOrderReceiptList List
    title is "AllReceipts"
    initially display search
    Instance Selection
        where (IsValidForActorContext
        and   not InterfaceInProcess)
    Actions
        action is Unreleased.ReceiveAll
            action icon is claim
            hide when invalid
        action is Unreleased.Release
            action icon is release
            hide when invalid
        action is Unreleased.AddReceiveAll
            action icon is create
            hide when invalid
        action is Unreleased.Create
            action icon is create
            label is "_"
        action is open
            label is "_"
        action is Unreleased.Delete
            action icon is delete
            label is "_"
        action is search
            label is "_"
        action is refresh
            label is "_"
