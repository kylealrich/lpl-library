PFQQueueList is a List
    is primary
    title is "ProcessFlowQueueList"
    Display Fields
        create stamp
            label is "Date"
            sort order is ByStamp
                is default descending
        EffectiveDate
        ExecuteTime
        Type
        Status
            raise red alert when (IsFailed)
            raise yellow alert when (IsHold)
            raise green alert when (IsComplete)
        Name
        WorkUnitID
