InboundShipmentLines is a List
    is primary
    implements InlineCreate
        allow form create
    Display Fields
        WarehouseShipmentImportLine
            label is "Line"
            select is ThirdPartyWarehouseShipmentLines
        Item
        LineType
            label is "Type"
        StockUOM
            label is "UOM"
        ProcessDate
            label is "ProcessDateAndTime"
