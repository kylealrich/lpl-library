ActiveCashAllocationCodes is a List
    title is "CashAllocationCodes"
    no data message is "No_Cash_Allocation_Codes_to_view"
        icon is icon.icon-empty-no-tasks
    Display Fields
        CashAllocationCode
            sort order is primary
        Description
            allow update
        AllocationType
            allow update
        ControlTotal
            column visible when (AllocationType.Amount)
    Instance Selection
        where (Active)
    restrict action ApplyAllocationCode
