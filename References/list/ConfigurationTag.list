ActiveTagList is a List
    title is "Tags"
    single select
    keyword search field is ConfigurationTag
        label is "SearchTags"
        always display
    show as simple list
    Display Fields
        ConfigurationTag
            bold
        Owner
    Instance Selection
        where (Active)
    restrict action DynamicCreate
    Actions
        action is search
            label is "_"
    Detail Sections
        TaggedConfigurations
            list is ConfigurationTagItem set.TaggedItemsList
