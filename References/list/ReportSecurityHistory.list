ConsoleConfigurationChangesList is a List
    title is "<ReportSecurity.Role>"
    single select
    show as simple list
    Display Fields
        Changed
            sort order is ByChanged
                is default
        ChangedBy
        Action
        InvokingAction
    Instance Selection
        where (ConfigurationChanged)
    open action is restricted
    Actions
        action is Revert
            label is "RevertToHistory"
        action is search
            label is "_"
        action is refresh
            label is "_"
    Detail Sections
        Compare
            form is ComparePreviousVersionForm
        CompareToCurrent
            form is CompareCurrentConfigurationForm
