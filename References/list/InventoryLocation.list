ActiveInventoryLocationsList is a List
    title is "InventoryLocations"
    initially display search
    disable all actions
    Display Fields
        Company
        InventoryLocation
            label is "Location"
        Name
        LocationType
            label is "Type"
    Instance Selection
        where (IsActive)
