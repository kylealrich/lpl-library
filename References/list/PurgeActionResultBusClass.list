PurgeActionResultBusClassList is a List
    is primary
    title is "PurgeActionResultBusinessClasses"
    Display Fields
        BusinessView
        BusinessAction
        PurgeActionResult
        PurgeActionResultBusClass.PurgedBusinessClass
            raise blue alert when (LastRestoredStamp entered)
                mouse over text is "DataRestoredOn<LastRestoredStamp>"
        RecordCount
    Detail Sections
        Data
            visible when (DisplayData)
            list is ArchivedDataRel.ArchivedDataNestedList
        ActionGroupMembers
            visible when (DisplayActionGroupMembers)
            list is ActionGroupMembers.primary
