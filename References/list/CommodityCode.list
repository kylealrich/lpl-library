AvailableCommodityCodesForASupplierContact is a List
    title is "AvailableCommodityCodes"
    keyword search field is CommoditySearch
        always display
    implements TreeView
        has children when (HasChild)
    show as simple list
    called out actions only
    Display Fields
        CommodityCode
        Description
        SupplierGroup
            column visible when (false)
        Supplier
            column visible when (false)
        SupplierSourceId
            column visible when (false)
        button of "Add"
            visible when (not SupplierHasChildCommodityCode)
            action is AttachToSupplierContactFromForm
            label is "Add"
            is primary
    Instance Selection
        where (RegistrationEligible
        and   AllActive
        and   InternalSupplierCommodityCodeRel not exists)
    open action is restricted
    Actions
        action is AttachToSupplierContactFromForm
