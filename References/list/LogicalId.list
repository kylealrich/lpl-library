LogicalIds is a List
    is primary
    Display Fields
        LogicalId.LogicalId
            raise red alert when (MissingOAuth)
                mouse over text is "Missing_O\\AuthRecord"
            raise yellow alert when (not IsBase)
                mouse over text is "ResidenceInTenant"
        LogicalId.DataAreaName
            label is ProductLabel
        LogicalId.IONConnection.Description
            label is "Connection"
        Default
            label is "DefaultLID"
