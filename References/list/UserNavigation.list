ConfigConsoleList is a List
    title is "UserNavigations"
    always display search
    Display Fields
        BusinessView
            label is "BusinessClass"
        BusinessNavigation
        Status
        Independent
            label is "UserNavigation"
        update stamp.actor
            label is "LastChangedBy"
        update stamp
            label is "LastChanged"
    form is ConfigConsoleForm
    restrict action Create
    Actions
        action is CreateNewNavigation
        action is refresh
        action is Deactivate
            hide when invalid
        action is Activate
            hide when invalid
