MainSegregationOfDutiesAnalysisList is a List
    is primary
    propagate as a drill
    title is "Results"
    initially display search
    Display Fields
        SegregationOfDutiesTemplate.Priority
            raise red alert when (SegregationOfDutiesTemplate.IsHighPriority)
            raise yellow alert when (SegregationOfDutiesTemplate.IsMediumPriority)
            raise green alert when (SegregationOfDutiesTemplate.IsLowPriority)
            sort order is ByPriority
                is default
        SegregationOfDutiesTemplate
        SegregationOfDutiesTemplate.TemplateName
        Actor
        Role
        SecurityClass
        SecurableObjectType
            label is "Type"
        SecurableObjectName
            label is "Name"
        SecurableObjectAction
            label is "Action"
        SecurableObjectCondition
            label is "Condition"
    restrict action Create
    restrict action Update
    Actions
        action is export to csv
        action is export to pdf
        action is search
        action is refresh
