OneSourceTaxAttributeLines is a List
    is primary
    title is "OneSourceAttributes"
    implements InlineCreate
        allow form create
    Display Fields
        DerivedLineNumber
            sort order is ByAttributeNumber
        UserAttributeName
            allow update
            sort order is ByAttributeNumber
        Value
            allow update
        LoadDynamicField
            allow update
            select is DynamicFieldSearchList
        LoadDynamicField.ValueIs
        Description
            allow update
