CashConsolidationsCurrentDayBuild is a BusinessTask
    owned by cb

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                CashConsolidationsCurrentDayBuildReport

    Parameters

    Actions
        CashConsolidationsCurrentDayBuild is a Set Action
