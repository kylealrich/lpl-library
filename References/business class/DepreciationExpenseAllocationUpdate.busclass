DepreciationExpenseAllocationUpdate is a BusinessTask
    owned by am

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                DepreciationExpenseAllocationUpdateReport

    Parameters

    Actions
        DepreciationExpenseAllocationUpdate is a Set Action
            allow unlimited concurrency
