SSODomainProperty is a BusinessClass
    owned by security
    prefix is SDPRP
    stored in environment
	contains environment data
	disable data area copy
		preserve target data

    Ontology
        part of SSODomain
            relative key is SSODomainProperty is Alpha size 80

    Patterns
        implements CRUD

    Persistent Fields
        PropertyValue is a SSODomainPropertyValue
		PropertyBlob is BinaryObject
