AM190Work is a BusinessClass
    owned by am
    prefix is W8779

    Patterns
        disable StaticTranslations
        implements WorkFile

    Persistent Fields
        WorkFile                is UniqueID
        WorkFileSeq             is Numeric size 12
        Company                 is like Company			
        Book                    is like Book			
        Asset                   is like Asset			
        AssetDescription        is like Description		
		Reference1			    is a Reference
		Reference2			    is a Reference        
        SeqNumber               is like Sequence		
        TransStatus             is like TransactionStatus
        AcctGrp                 is like AssetAccountGroup	
        AssetType               is like AssetSubType	
        SubType                 is like AssetSubType	
        AcctType                is like AssetAccountType 
        FinanceEnterpriseGroup
        Account					is a FinanceCodeBlockAccountOnly
        AUCodeBlock             is a FinanceCodeBlockNoAccount
        Description             is like Description		
        TransactionCurrency     is like Currency
        TransAmount             is like InternationalAmount
        BaseAcctunit            is like AccountingUnit
        Activity                is like Project				
        TagNbr                  is like TagNumber		
        AssetProcess            is like AssetProcess
        PdEndDate               is Date					
        NbrDeprPd               is Numeric size 2
        TranNd                  is like NumberOfDecimals		
        YrEndDate               is Date					
        ExpMsgNbr               is like MsgNbr 
        ExpErrorCat             is Alpha size 20
        ExpErrorCodeBlock       is a FinanceCodeBlockNoAccount
        ExpErrorAccount			is like GeneralLedgerChartAccount
        AcmMsgNbr               is like MsgNbr 
        AcmErrorCat             is Alpha size 20
        AcmErrorAcctUnit        is like AccountingUnit
        AcmErrorAccount         is like GeneralLedgerChartAccount	
        AcmErrorSubAcct         is like SubAcct			
        LeaseCompany            is like Company			
        Lease                   is like Lease 
        AmtCurPeriod            is like CurrentPeriod 
        AstAcctUnit             is like AccountingUnit
        AstItemLastDate         is Date					
        AstCurrencyCode         is like Currency			

    Sets

        InsertionOrderSet
            indexed
            no duplicates
            primary
            Sort Order
                WorkFile
                WorkFileSeq

        AM190WorkAM190Sort1
            indexed
            duplicates
            Sort Order
                WorkFile
                Book
                Company
                TransactionCurrency
                Account
                AUCodeBlock
                Asset

        AM190WorkAM190Sort2
            indexed
            duplicates
            Sort Order
                WorkFile
                Book
                Company
                TransactionCurrency
                Asset
                Account
                AUCodeBlock

    Actions

        Create is a Create Action

            Field Rules
                WorkFileSeq
                    autosequence

        Delete is a Delete Action

        DeleteAll is a Set Action
            restricted

            Parameters
                WorkFileHeader is UniqueID

            Instance Selection
                where (WorkFile = WorkFileHeader)

            Local Fields
                LWorkFileHeader is a WorkFile

            Parameter Rules
                WorkFileHeader
                    required
                    LWorkFileHeader = WorkFileHeader

            Action Rules
                Instance Rules
                    invoke Delete

                Empty Set Rules
                    invoke Purge LWorkFileHeader

                Set Rules
                    Exit Rules
                        invoke Purge LWorkFileHeader
