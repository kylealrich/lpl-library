CB300CbhOut is a BusinessClass
    owned by cb
    prefix is W2173

    Patterns
        disable StaticTranslations
        implements WorkFile

    Persistent Fields
        WorkFile                is UniqueID
        WorkFileSeq             is Numeric size 12
        CbhCashCode             is like CashCode
        CbhTransferNbr          is like CashLedgerFundTransferHeader 
        CbhTransferType         is Alpha size 1
        CbhCompany              is like Company 
        CbhTransferAmt          is like InternationalAmount
        CbhAppliedAmt           is like InternationalAmount
        CbhCpyBaseAmt           is like InternationalAmount
        CbhBaseApplAmt          is like InternationalAmount
        CbhTransferDate         is Date    
        CbhPostDate             is Date    
        CbhBexpTranAmt          is like InternationalAmount
        CbhBexpBaseAmt          is like InternationalAmount
        CbhBexpAcctUnit         is like AccountingUnit
        CbhBexpAccount          is like GeneralLedgerChartAccount		
        CbhBexpSubAcct          is like SubAcct
        CbhBankInstCode         is like BankTransactionCode 
        CbhExchangeRate         is like CurrencyRate 
        CbhRecStatus            is Numeric size 1
        CbhDescription          is like Description    
        CbhJrnlBookNbr          is like JournalBook
        	restricted
        CbhJbkSeqNbr            is like JbkSeqNbr 
        	restricted

    Sets

        InsertionOrderSet
            indexed
            no duplicates
            primary
            Sort Order
                WorkFile
                WorkFileSeq

    Actions

        Create is a Create Action

            Field Rules
                WorkFileSeq
                    autosequence

        Delete is a Delete Action

        DeleteAll is a Set Action
            restricted

            Parameters
                WorkFileHeader is UniqueID

            Instance Selection
                where (WorkFile = WorkFileHeader)

            Local Fields
                LWorkFileHeader is a WorkFile

            Parameter Rules
                WorkFileHeader
                    required
                    LWorkFileHeader = WorkFileHeader

            Action Rules
                Instance Rules
                    invoke Delete

                Empty Set Rules
                    invoke Purge LWorkFileHeader

                Set Rules
                    Exit Rules
                        invoke Purge LWorkFileHeader
