PfiIONVariationId is a BusinessClass
    owned by pfi
    prefix is PfiIV
    default label is "IONVariationId"

    Ontology
        symbolic key is PfiIONVariationId

    Patterns
        implements CRUD
        disable AsOfDateProcessing
        disable Auditing
        disable EffectiveDated

    Persistent Fields

        VariationId is Numeric size 22
            default label is "NextVariationId"
        Type        is Numeric size 1
            States
                Sequence   value is 1
                Timestamp  value is 2

    Field Rules

        VariationId
            required
            
    Actions

        Create is a Create Action

        Update is an Update Action

        Delete is a Delete Action
