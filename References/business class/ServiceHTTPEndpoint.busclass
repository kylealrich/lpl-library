ServiceHTTPEndpoint is a BusinessClass
    owned by security
    prefix is SHEP
    stored in environment
	contains environment data
	disable data area copy
		preserve target data

    Ontology
    	symbolic key is ServiceHTTPEndpoint

    Patterns
        implements CRUD
        implements SecurityCache
        disable AuditIndex

    Sets
        SVCSet
            indexed
            no duplicates
            Sort Order
                HTTPEndpoint
                Service
