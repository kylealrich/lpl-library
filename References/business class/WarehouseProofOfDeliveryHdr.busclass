WarehouseProofOfDeliveryHdr is a BusinessClass
	owned by wh
	prefix is WPDH
	
	Ontology
		symbolic key is WarehouseProofOfDeliveryHdr
	
	Patterns
		implements StaticJava
        disable AuditIndex
	
	Persistent Fields
		ReportDate 		is TimeStamp

	Field Rules
		ReportDate 	
			required

	Relations
		WarehouseProofOfDeliveryWHSRel is a WarehouseProofOfDeliveryWHS set
	
	Derived Fields
		DerivedCurrentDate is a DerivedField
			type is TimeStamp
			return current timestamp
			
	Actions
        Create is a Create Action
        	restricted
        	
        Update is an Update Action
        	restricted
        	
		Delete is a Delete Action
			restricted
			
		UploadIDM is an Instance Action
			restricted
			Parameters
				PrmIDMPrinter			is an IDMPrinter

			Action Rules
				for each WarehouseProofOfDeliveryWHSRel
					invoke GenerateIDMDocument each
						invoked.PrmIDMPrinter				=	PrmIDMPrinter








					invoke Delete
