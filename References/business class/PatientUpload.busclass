PatientUpload is a BusinessTask
    owned by ic

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                PatientUploadReport

    Parameters
        Company                 is an InventoryCompany
        FileName                is Alpha size 30
        UpdateFlag              is AlphaUpper size 1
            States
                EditAndUpdate value is "Y"
                    default label is "Edit and Update"
                EditOnly      value is "N"

    Parameter Rules
        Company
            required
        UpdateFlag
        	initial value is "Y"
            default to "Y"

    Actions
        PatientUpload is a Set Action
            allow unlimited concurrency
