AssetTaxAuthority is a BusinessClass
    owned by am
    prefix is ATA
    classic name is AMTAXAUTH

    Ontology
        symbolic key is AssetTaxAuthority
            classic set name is ATASET1
            classic name is AUTHORITY
            classic name for AssetTaxAuthorityType is AUTH-TYPE

    Patterns
        implements DynamicCreation
        implements StaticJava
        disable AuditIndex

