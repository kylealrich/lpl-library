StandardComment is a BusinessClass
    owned by te
    prefix is SDC
    classic name is STDCOMMENT

    Ontology
        symbolic key is StandardComment
            classic set name is SDCSET1
            classic name is COMMENT-CD

    Patterns
        implements StaticJava
        disable AuditIndex

    Persistent Fields

        CommentType is an OrderType
        CommentText is AlphaUpper size 40

        
	Field Rules
		
		CommentType
			cannot be changed
