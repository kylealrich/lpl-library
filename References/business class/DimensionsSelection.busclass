DimensionsSelection is a BusinessClass
    owned by GeneralLedger
    prefix is DmSlc

    Ontology
        symbolic key is DimensionsSelection

    Persistent Fields
        DimensionColumns
        Description
        Priority	   	        			is Numeric 6 
			disable Auditing
		Active                  			is Boolean
		DimensionsCount						is Numeric 2

    Field Rules
		Active
			initial value is true
			default to true

	Relations
		GeneralLedgerCodeRel
			one-to-many relation to GeneralLedgerCode
			Field Mapping uses ByDimensionsSelection
				related.FinanceEnterpriseGroup	= FinanceEnterpriseGroup
				related.SelectedDimensions		= DimensionsSelection
				related.AllDimensionCode		= DimensionColumns.DerivedDimensionCode
				related.EnhancedCompare			= true
				related.FinanceCodeBlock.Ledger	= DimensionColumns.CodeBlock.Ledger

    Sets
		ByDimensionsCount
			Sort Order
				FinanceEnterpriseGroup
				DimensionsCount
				DimensionsSelection

    Actions
        Create is a Create Action
			restricted
        
        Update is an Update Action
            restricted

        Delete is a Delete Action
			restricted
