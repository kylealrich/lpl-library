PatientChargeItemConversion is a BusinessTask
    owned by ic

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                PatientChargeItemConversionReport

    Parameters
        InputFile               is like WorkFileName
        UpdateOpt               is AlphaUpper size 1
            States
                EditOnly      value is "N"
                EditAndUpdate value is "Y"
                    default label is "Edit and Update"

    Parameter Rules
        InputFile
			initial value is "PatientChargeItemInput"
			default to "PatientChargeItemInput"
       
        UpdateOpt
        	initial value is "N"
            default to "N"
            required

    Actions
        PatientChargeItemConversion is a Set Action
            allow unlimited concurrency
