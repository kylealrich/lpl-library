BookRegulation is a BusinessClass
	owned by am
	prefix is BkReg

    Ontology
        symbolic key is BookRegulation

    Patterns
        disable AuditIndex
		disable Auditing 
       	disable EffectiveDated
		   	
	Persistent Fields
		Description
		
	Field Rules
		Description	
			required

	Derived Fields
		ADSMessageField is a MessageField
			"ADS"
		GDSMessageField is a MessageField
			"GDS"

	Relations
		ADSBookRegulationRel
			one-to-one relation to BookRegulation
			Field Mapping uses symbolic key
				related.FinanceEnterpriseGroup	= FinanceEnterpriseGroup
				related.BookRegulation			= "ADS"

		GDSBookRegulationRel
			one-to-one relation to BookRegulation
			Field Mapping uses symbolic key
				related.FinanceEnterpriseGroup	= FinanceEnterpriseGroup
				related.BookRegulation			= "GDS"				

	Actions
		Create is an Action
			Action Rules
				if (BookRegulation not = "ADS"
				or BookRegulation not = "GDS")
					if (ADSBookRegulationRel not exists
					or GDSBookRegulationRel not exists)
						confirmation required
							"<ADSMessageField>and<GDSMessageField>BookRegulationsAreRecommendedForTaxReporting"

		Update is an Action

		Delete is an Action
			Entrance Rules
				if (AssetGuidelineClassRegulation set exists)
					confirmation required
						"AllRelatedAssetGuidelineClassRegulationsWillBeDeleted;SelectOKToContinue"				
