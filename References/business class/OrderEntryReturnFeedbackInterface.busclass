OrderEntryReturnFeedbackInterface is a BusinessTask
    owned by oe

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                OrderEntryReturnFeedbackInterfaceReport

    Parameters
        Company                 is a BillingCompany
        Location                is an InventoryLocation
        ReleaseCode             is AlphaUpper size 1
            States
                DoNotRelease value is "N"
                Release      value is "R"

    Parameter Rules
        Company
            required
        Location
            required
        ReleaseCode
        	initial value is "N"
            default to "N"

    Actions
        OrderEntryReturnFeedbackInterface is a Set Action
            allow unlimited concurrency
