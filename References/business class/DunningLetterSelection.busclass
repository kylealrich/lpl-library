DunningLetterSelection is a BusinessTask
	owned by ar

	Patterns
		implements ManualTransaction
		implements StaticJava
			Reports
				DunningLetterSelectionReport
				DunningLetterSelectionErrorReport

	Parameters
		Company				is a ReceivableCompany
		CompanyGroup		is a GeneralLedgerCompanyGroup
		IncludeCredits		is AlphaUpper size 1
			States
				DoNotIncludeCreditMemo	value is "N"
					default label is "Do not include credit memo"
				IncludeCreditMemos		value is "Y"
					default label is "Include credit memos"
		Reselect			is AlphaUpper size 1
			States
				NoPurgeSelectOnly	value is "N"
					default label is "No purge (select only)"
				PurgeAndReselect	value is "Y"
					default label is "Purge and reselect"
		DunningDate			is Date

	Parameter Rules
		CompanyGroup
			constraint (Company not entered)
				"BothCompanyAndCompanyGroupCannotBeEntered"
			constraint (CompanyGroup.GeneralLedgerCompanyGroupMembersRel exists)
				"CompanyGroup<CompanyGroup>ContainsNoCompanies"
		IncludeCredits
			initial value is "N"
			default to "N"
		Reselect
			initial value is "N"
			default to "N"
			required

	Actions
		DunningLetterSelection is a Set Action

