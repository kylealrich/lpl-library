CashLedgerUser is a BusinessClass
    owned by cb
    prefix is CBU
    classic name is CBUSER

    Ontology
        symbolic key is CashLedgerUser
            classic set name is CBUSET1
            classic name is USER-ID
            classic name for CashLedgerUserClass is USER-CLASS

    Patterns
        implements StaticJava
        disable AuditIndex

    Persistent Fields

        InquireAccess is Boolean
        UpdateAccess  is Boolean

    Field Rules

        InquireAccess
            default to true
            
		UpdateAccess
			if (!InquireAccess)
				if (UpdateAccess)
					constraint (false)
						"Inquiry_\AccessMust_=_\YesIf_\Update_\Access_=_\Yes"		  
        
	Actions
		Create is a Create Action
			Action Rules

		Update is an Update Action
			Action Rules

		Delete is a Delete Action
			Action Rules
		
