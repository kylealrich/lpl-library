DomainAuthorStatus is a BusinessClass
    default label is "DomainAuthorizationStatus"
    owned by security
    prefix is DASTS
    stored in environment

    Ontology
        symbolic key is DomainAuthorStatus
	
	Patterns
        implements CRUD
        implements SecurityCache
        disable AuditIndex
        
    Persistent Fields
        Active					is Boolean
        
    Actions
        Create is an Action 	
		Update is an Action
		Delete is an Action
