PfiHealthStatusNotification is a BusinessClass
    default label is "HealthStatusNotification"
    owned by pfi
    prefix is HSN

    Ontology
        symbolic key is PfiHealthStatusNotification

    Patterns
        disable AsOfDateProcessing
        disable Auditing
        disable EffectiveDated
        disable UniqueID

    Persistent Fields
        Time is Numeric size 6
        Threshold is Numeric size 6
        Email is Alpha size 1000
        Select is Boolean
            default label is "Enabled"
           
                       
    Field Rules
        Time
            default to 0
            initial value is 0
            if(Threshold = 0)
                required
        Threshold
            default to 0
            initial value is 0
            if(Time = 0)
                required
    
        Select
            default to true
                
    Actions
        Create is a Create Action
       
        Update is an Update Action
       
        Delete is a Delete Action
