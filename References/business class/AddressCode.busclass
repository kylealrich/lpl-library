AddressCode is a BusinessClass
    owned by GeneralLedger
    prefix is GLADD
    representative text is "<AddressCode>-<Description>"

    Ontology
        symbolic key is AddressCode

    Patterns
        implements StaticJava
        disable AuditIndex

    Persistent Fields

        Description
        PostalAddress			is a PostalAddressV2	
        	holds pii
		PersonResponsible		is a Employee
		TelephoneNumber			is a Telephone 
			holds pii
		FaxNumber 
			holds pii
		EmailAddress            is an EmailAddressMulti 
			holds pii

    Conditions

	Actions
        Create is a Create Action

        Update is an Update Action

        Delete is a Delete Action
