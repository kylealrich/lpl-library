GeneralLedgerCalendarDate is a BusinessClass
	owned by GeneralLedger
	
	prefix is GLCD
	
	Ontology
		symbolic key is GeneralLedgerCalendarDate
		
	
	Patterns
		disable AuditIndex
 		disable EffectiveDated
		disable AsOfDateProcessing 		
	
	Persistent Fields
		Year											is a GeneralLedgerCalendarYear 
		YearToDateCalendarPeriod						is like GeneralLedgerCalendarPeriod
		QuarterToDateCalendarPeriod						is like GeneralLedgerCalendarPeriod
		MonthToDateCalendarPeriod						is like GeneralLedgerCalendarPeriod
		WeekToDateCalendarPeriod						is like GeneralLedgerCalendarPeriod
	
	Transient Fields

	Local Fields

	Derived Fields

	Conditions
	
	Relations

	Sets

	Field Rules
	
	Rule Blocks

    Actions
		Create is a Create Action
			default label is untranslatable
			restricted

		Update is an Update Action
			default label is untranslatable
			restricted

		Delete is a Delete Action
			default label is untranslatable
			restricted

		Purge is a Purge Action
			default label is untranslatable
			restricted
	
		PurgeData is a Set Action
			default label is untranslatable
			restricted
			Parameters
				PrmFinanceEnterpriseGroup		is a FinanceEnterpriseGroup
					default label is "FinanceEnterpriseGroup"
				PrmCalendar						is a GeneralLedgerCalendar
					default label is "Calendar"

			Instance Selection
				where (FinanceEnterpriseGroup                  = PrmFinanceEnterpriseGroup
				and    (PrmCalendar not entered or PrmCalendar = GeneralLedgerCalendar))

			Action Rules
				Instance Rules
					invoke Purge
	
