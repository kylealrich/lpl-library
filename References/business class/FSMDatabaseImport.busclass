FSMDatabaseImport is a BusinessClass
	owned by framework
	prefix is DBI

	Ontology
		symbolic key is FSMDatabaseImport

	Patterns
		disable Auditing
		disable EffectiveDated
		disable AsOfDateProcessing
 		implements DynamicCreation

	Persistent Fields

	Conditions
		FSMDatabaseImportControlExists
			when (FSMDatabaseImportControl exists)

	Derived Fields
		LastRunDate is a DerivedField
			type is TimeStamp
			return first FSMDatabaseImportHistoryRel.RunDate

	Relations
		FSMDatabaseImportControlRel
			one-to-one relation to FSMDatabaseImportControl
			Field Mapping uses symbolic key
				related.FSMDatabaseImportControl = FSMDatabaseImport

		FSMDatabaseImportHistoryRel
			one-to-many relation to FSMDatabaseImportHistory
			Field Mapping uses ByRunDate
				related.FSMDatabaseImport = FSMDatabaseImport

	Actions
		Create is a Create Action

		Update is an Update Action

		Delete is a Delete Action
