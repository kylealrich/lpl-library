HTTPEndpointGroup is a BusinessClass
    owned by security
    prefix is HEPG
    stored in environment
	contains environment data
	disable data area copy
		preserve target data

	Ontology
		symbolic key is HTTPEndpointGroup

    Patterns
        implements CRUD
        implements SecurityCache
        disable AuditIndex

    Persistent Fields
        Description	is a LSDescription

