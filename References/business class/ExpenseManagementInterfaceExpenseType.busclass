ExpenseManagementInterfaceExpenseType is a BusinessClass
    owned by ap
    prefix is XMT
    sql name is XMIExpenseType
    classic name is CTXMEXPTYP

    Ontology
        symbolic key is ExpenseManagementInterfaceExpenseType
		    sql name is XMIExpenseType
            classic set name is XMTSET1
            classic name for ExpenseManagementInterfaceExpenseType is EXPTYPE

    Patterns
        implements StaticJava
        disable AuditIndex

    Persistent Fields
		Name
		Description
		HROrganization
        HRProcessLevel	is AlphaUpper size 5
        JobCode			is AlphaUpper size 9
        PayCode			is AlphaUpper size 4
        FinanceStructure is a FinanceCodeBlock
        
    Relations
		AccountingEntitySecurityGroupMemberRel
			one-to-one relation to AccountingEntityGroupMember
			Field Mapping uses part of key
				related.FinanceEnterpriseGroup	= FinanceEnterpriseGroup
				related.AccountingEntityGroup   = actor.context.AccountingEntitySecurityGroup.FinanceDimensionStructure
				related.AccountingEntity        = AccountingEntity
								
	Field Rules

	Conditions
		SecurityGroupAllowsAccess
			when (actor.context.AccountingEntitySecurityGroup = blank
			or   (AccountingEntitySecurityGroupMemberRel exists))
    	
