NRACResponseRecallCode is a BusinessClass
    owned by recall
    store using com.lawson.rdtech.framework.repository.XMLDataSourceStorage

    Ontology
        symbolic key is NRACResponseRecallCode

    Patterns
        disable Auditing
        disable EffectiveDated
        disable UniqueID
        disable StaticTranslations

	Persistent Fields
		TextContent is an XMLElement
			sql name is "text()"		
		Type is an XMLElement
			sql name is "Type"
		ExpDate is an XMLElement
			sql name is "ExpDate"



	Field Rules
	Actions
		Create is a Create Action
			restricted
		Update is an Update Action
			restricted
		Delete is a Delete Action
			restricted
		Purge is a Purge Action
			restricted
