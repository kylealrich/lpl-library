IONInboxReader is a BusinessTask
    owned by la
    
    Patterns
        implements ManualTransaction
        
	Actions
		Sweep is an Instance Action
			Local Fields
		    	SkipReset is Boolean
			
			Action Rules
				SkipReset = config(ion).IonInboxResetSkip
				if (not SkipReset)
					invoke ResetPending IONInbox in foreground
