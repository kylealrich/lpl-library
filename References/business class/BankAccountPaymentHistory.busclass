BankAccountPaymentHistory is a BusinessTask
    owned by ap

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                BankAccountPaymentHistoryReport

    Parameters
		VendorGroup
        PayGroup                
        CashCode
        FrCheckDate             is Date
        ToCheckDate             is Date
        FrCheckNum              is AlphaRight size 10
        ToCheckNum              is AlphaRight size 10
        PaymentCode             is a BankTransactionCode

    Parameter Rules
    	VendorGroup
    		required
        PayGroup
            required
        ToCheckDate
        	initial value is current corporate date + 1 year    
        	default to current corporate date + 1 year

    Actions
        BankAccountPaymentHistory is a Set Action
            allow unlimited concurrency
