WithholdingScaleDetail is a BusinessClass
	owned by ap
	prefix is WSD

	Ontology
		symbolic key is WithholdingScaleDetail

	Patterns

	Persistent Fields
		FromAmount 									is an InternationalAmount 
		AmountToWithhold							is an InternationalAmount
		PlusPercentage								is a ChrgRate

	Transient Fields

	Derived Fields

	Local Fields

	Context Fields

	Rule Blocks

	Relations

	Conditions

	Sets
		ByFromAmount
			Sort Order
				VendorGroup
				WithholdingScale
				FromAmount

	Field Rules
		AmountToWithhold
			constraint (AmountToWithhold > 0)
				"AmountToWithholdMustBePositiveAmount"
			constraint (AmountToWithhold < FromAmount)
				"AmountToWithholdCannotBeGreaterThanFromAmount"

	Actions

		Create is a Create Action

		Update is an Update Action

		Delete is a Delete Action
