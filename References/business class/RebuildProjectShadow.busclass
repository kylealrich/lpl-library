RebuildProjectShadow is a BusinessTask
    owned by Projects

    Parameters
        FinanceEnterpriseGroup
       	ProjectStructure

    Parameter Rules
        FinanceEnterpriseGroup
        	required
        ProjectStructure 

    Actions

        ScheduleRebuildProjectShadow is an Instance Action

            Action Rules
            
            	if (ProjectStructure entered)
					invoke BuildShadowFile ProjectStructure in background
				else
					invoke BuildShadowFile FinanceEnterpriseGroup.ProjectStructure set in background

