CamCategory is a BusinessClass
	owned by cam
	default label is "Category"
	
	prefix is CamCa
	
	Ontology
		symbolic key is CamCategory
		
	Patterns

	Persistent Fields
		Description
		SSNRequired	is a YesOrNo
			default label is "TaxIDRequired"
		TaxIDType	is a TaxIdentificationType

	Field Rules
		Description
			required
		SSNRequired
			required
		TaxIDType
			if (SSNRequired.Yes)
				required
