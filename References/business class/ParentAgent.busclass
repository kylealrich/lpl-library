ParentAgent is a BusinessClass
    owned by person
    prefix is PRNT

    Ontology
        symbolic key is ParentAgent
        
	Patterns
        implements CRUD
        disable AsOfDateProcessing
        implements SecurityCache        

    Persistent Fields
        Actor

    Sets
        ActorSet
            no duplicates
            Sort Order
                Actor
            Instance Selection
                where (ActorEntered)

    Relations
        ChildRel
            one-to-many relation to Agent
            Field Mapping uses ParentSet
                related.ParentAgent = ParentAgent

    Conditions

        ActorEntered
            when (Actor entered)

    Actions
        Create is a Create Action

        Delete is a Delete Action

        Update is an Update Action

        DeleteMe is a Delete Action
            Parameters
                Me is a ParentAgent
