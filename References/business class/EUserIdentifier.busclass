EUserIdentifier is a BusinessClass
    owned by security
    prefix is EXUID
    stored in environment

    Ontology
        symbolic key is EUserIdentifier

    Patterns
    	implements CreateStamp
        disable AuditIndex
        disable AsOfDateProcessing
        disable EffectiveDated
        disable Auditing

    Persistent Fields
        IdentifierId        is Alpha 50
        IdentifierType      is Alpha 10
        IdentifierValue     is Alpha 50
        IsPrimary           is Boolean

    Actions
        Create is an Action			
			
		Update is an Action
			restricted
		
		Delete is an Action

		Purge is a Purge Action
			


		

		
