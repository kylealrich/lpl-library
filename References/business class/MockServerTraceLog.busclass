MockServerTraceLog is a BusinessClass
	owned by ws
	prefix is MSTL
	contains environment data

	Ontology
		symbolic key is MockServerTraceLog

	Patterns
		implements CRUD

	Persistent Fields
		LandmarkWSGUID             			    is Alpha size 100
			default label is "LogGUID"
		RelatedMockEndpoint						is Alpha size 40
			default label is "MockEndpoint"
        TraceLog                        		is Text

	Actions
		Create is a Create Action
		Update is an Update Action
		Delete is a Delete Action
