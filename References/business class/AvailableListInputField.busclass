AvailableListInputField is a BusinessClass
    owned by wsbuilder
    store using com.lawson.rdtech.framework.repository.BusclassFieldStorage

    Ontology
        symbolic key is AvailableListInputField

	Patterns
    	disable UniqueID
	
    Persistent Fields
        Required                is Boolean
        
    Actions
        Create is a Create Action
        Update is an Update Action
        Delete is a Delete Action
        
        AttachInputFieldsToListOperation is an Instance Action
			Parameters
				WebService
				ListOperation
				Module
				Busclass
				List

			Action Rules
				invoke Create ListInputField
					invoked.WebService              = WebService
                    invoked.ListOperation           = ListOperation
                    invoked.Module                  = Module
                    invoked.Busclass                = Busclass
            		invoked.List                    = List
            		invoked.InputField              = AvailableListInputField
                    invoked.Required                = true
