TypeField is a BusinessClass
    owned by repository
    store using com.lawson.rdtech.framework.repository.TypeFieldStorage

    Ontology
        symbolic key is TypeField

    Patterns
    	implements ReadOnly
    	disable UniqueID

    Persistent Fields
    	IsKeyField		is Boolean
    		default label is "KeyField"
    	IsConfiguration is Boolean
    		default label is "Configuration"

	Derived Fields
		BusinessLogicLPL is a NativeField
		    type is LPL

		UiLPL is a NativeField
		    type is LPL
			default label is "UI_LPL"
