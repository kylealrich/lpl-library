ProfileLogIntermediateEntry is a BusinessClass
    owned by la
    prefix is PLIE
    classic name is PROFILELIE
	contains environment data
	disable data area copy
		preserve target data
    
    Ontology
    	symbolic key is ProfileLogIntermediateEntry
    
    Patterns
    	implements CRUD
		disable Auditing
		disable EffectiveDated

    Persistent Fields
		LogData is BinaryDocument

	Actions
		Create is a Create Action
		Delete is a Delete Action
		Update is an Update Action
