ClassicTransactionExecutor is a BusinessTask
    owned by la
    
    Patterns
        implements ManualTransaction
       
    Parameters
		RecordID		is a LawsonClassicMessageGroup
		FirstOnly 		is Boolean
		ThruDate		is TimeStamp
		        
	Actions
		ExecuteClassicTransaction is an Instance Action
			
		ResetFailed is an Instance Action
			Action Rules
				constraint (RecordID entered)
					"KeyRequiredForResetActions"
		
		PurgeClassicTransaction is an Instance Action
			Action Rules
				if (not RecordID entered)
					constraint (ThruDate entered)
						"ThruDateRequired"
			
