TaxTransactionConversion is a BusinessTask
    owned by tx

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                TaxTransactionConversionReport

    Parameters
        EditOnly                is AlphaUpper size 1
            States
                Yes value is "Y"
                No  value is "N"

    Parameter Rules
        EditOnly
        	initial value is "Y"
            default to "Y"

    Actions
        TaxTransactionConversion is a Set Action
            allow unlimited concurrency
