ShipToFacility is a BusinessClass
    owned by ic
    prefix is STF

    Ontology
        symbolic key is ShipToFacility
            classic set name is STFSET1

    Patterns
        implements StaticJava
        disable AuditIndex

    Persistent Fields

        Name

    Field Rules

        Name
            required

    Attach Rules
        if (parentcontext.name != "ShipToFacilityDetail")
            constraint (ShipToFacilityDetail set exists)
                "ShipTo_FacilityHasNoCompanyLocationDetails"

  	Actions
  	
        Create is a Create Action

        Update is an Update Action
	
        Delete is a Delete Action
