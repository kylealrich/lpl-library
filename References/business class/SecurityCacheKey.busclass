SecurityCacheKey is a BusinessClass
    owned by security
    prefix is SCKY
    stored in environment


    Ontology
        part of SecurityCacheAnalyser
    		relative key is SecurityCacheKey

	Patterns
		implements CRUD
        disable AsOfDateProcessing
        disable EffectiveDated
        disable Auditing

	Persistent Fields
		NumberOfObjects	is Numeric size 6
		IsPreloaded is Boolean
			default label is "Preloaded"
		
	Field Rules
		IsPreloaded
			default to false
			
	Actions	

        Create is an Action
 		Update is an Action
		Delete is an Action
		Purge is a Purge Action
		PurgeAll is a Set Action
			Action Rules
				Instance Rules
					invoke Purge
