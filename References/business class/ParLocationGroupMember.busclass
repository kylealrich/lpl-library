ParLocationGroupMember is a BusinessClass
    owned by ic
    prefix is PLGM

    Ontology
        symbolic key is ParLocationGroupMember

    Patterns
        implements StaticJava

    Persistent Fields
        Priority  				is Numeric size 2
        Active                  is Boolean

	Sets
    	ByPriority
			Sort Order
				Company
				ParLocationGroup
                Priority
				InventoryLocation

	Relations
		ParLocationGroupMemberRel
			one-to-many relation to ParLocationGroupMember
			Field Mapping uses ByPriority
				related.Company	                = Company 
				related.ParLocationGroup        = ParLocationGroup
                related.Priority				= Priority
            Instance Selection
				where (related.InventoryLocation	!= InventoryLocation)

	Field Rules
		Priority
			required
			constraint (ParLocationGroupMemberRel not exists)
				"DuplicatePriority"

		Active
			initial value is true	

	Actions		
        Create is a Create Action
        Update is an Update Action
        Delete is a Delete Action
            
