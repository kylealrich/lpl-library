IC130Ic130work is a BusinessClass
    owned by ic
    prefix is W2806

    Patterns
        disable StaticTranslations
        implements WorkFile

    Persistent Fields
        WorkFile                is UniqueID
        WorkFileSeq             is Numeric size 12
        Company                 is like InventoryCompany 
        PostingDate             is Date 
        DocType                 is like DocumentType 
        AcctUnit                is like AccountingUnit
        Account                 is like GeneralLedgerChartAccount 
        SubAccount              is like SubAcct 
        DetailFl                is Boolean 
        Item                    is like Item
        Reference               
        Activity                is like Project 
        BaseAmount              is like InternationalAmount 
        TranAmount              is like InternationalAmount 
        CurrencyCd              is like Currency	
        CurrencyNd              is like CurrencyNumberOfDecimals
        BaseNd                  is like BaseNumberOfDecimals 
        FromToCmpy              is like Company 
        UpdateDate              is Date 
        Location                is like InventoryLocation 
        TransDate               is Date 
        Document                is like InventoryTransaction
        ShipmentNbr             is like WarehouseShipment 
        LineNbr                 is like LineNumber
        ComponentSeq            is like InventorySequence 
        MdwrSeq                 is Numeric size 8
        UnitsPostingRequested   is Boolean 
        Quantity                
        SystemCd                is like TransactionSystemCode 
        AnalysisFld             is a IC130Ic130workAnalysisFldArray
        AtnObjId                is like ObjId 
		AccountDescription      
		IctMatchDtlKey          is like MatchDetailKey 
    Sets

        InsertionOrderSet
            indexed
            no duplicates
            primary
            Sort Order
                WorkFile
                WorkFileSeq

        IC130Ic130workIC130Sort1
            indexed
            duplicates
            Sort Order
                WorkFile
                Company
                FromToCmpy
                PostingDate
                DocType
                AcctUnit
                Account
                SubAccount
                Activity

        IC130Ic130workIC130Sort2
            indexed
            duplicates
            Sort Order
                WorkFile
                Company
                Location
                FromToCmpy
                AcctUnit
                Account
                SubAccount
                PostingDate

    Actions

        Create is a Create Action

            Field Rules
                WorkFileSeq
                    autosequence

        Delete is a Delete Action

        DeleteAll is a Set Action
            restricted

            Parameters
                WorkFileHeader is UniqueID

            Instance Selection
                where (WorkFile = WorkFileHeader)

            Local Fields
                LWorkFileHeader is a WorkFile

            Parameter Rules
                WorkFileHeader
                    required
                    LWorkFileHeader = WorkFileHeader

            Action Rules
                Instance Rules
                    invoke Delete

                Empty Set Rules
                    invoke Purge LWorkFileHeader

                Set Rules
                    Exit Rules
                        invoke Purge LWorkFileHeader
