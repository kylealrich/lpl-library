ServiceSSODomain is a BusinessClass
    owned by security
    prefix is SSD
    stored in environment
	contains environment data
	disable data area copy
		preserve target data

    Ontology
    	symbolic key is ServiceSSODomain

    Patterns
        implements CRUD
        disable AuditIndex

    Sets
        SSDSet
            indexed
            no duplicates
            Sort Order
                Service
				SSODomain
