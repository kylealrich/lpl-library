PurchasingDocumentIntegrityCheck is a BusinessTask
    owned by po

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                PurchasingDocumentIntegrityCheckReport
                Pointeg

    Parameters
        Company                 is a PurchasingCompany 

    Parameter Rules
        Company
            required

    Actions
        PurchasingDocumentIntegrityCheck is a Set Action
            allow unlimited concurrency
