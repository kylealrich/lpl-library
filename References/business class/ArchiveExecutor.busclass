ArchiveExecutor is a BusinessTask
    owned by la
    
    Patterns
        implements ManualTransaction
        
    Parameters
   		ArchiveMode is Numeric size 1   
   			default label is untranslatable
   			States
   				Sweep 			value is 0
   					default label is untranslatable
   				ByRequest 		value is 1
   					default label is untranslatable
   				ByActionGroup 	value is 2
   					default label is untranslatable
   		
   		AsyncID is UniqueID
			default label is untranslatable
   			
	Actions
		Archive is an Instance Action
			run in background
			restricted
		
