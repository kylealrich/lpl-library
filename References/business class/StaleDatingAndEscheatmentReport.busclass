StaleDatingAndEscheatmentReport is a BusinessTask
    owned by cb

    Patterns
        implements ManualTransaction
        implements StaticJava
            Reports
                StaleDatingAndEscheatmentReportReport

    Parameters
		CashManagementGroup    
        CashCode
        CcgCashCodeGrp          is a CashCodeGroup

        BankInstCode            is a BankTransactionCode
        ProcLevel               is AlphaUpper size 5 
        RunOption               is AlphaRight size 1
            States
                StaleDatedRecords	value is "1"
                EscheatmentRecords	value is "2"
        SortOption              is AlphaRight size 1
            States
                CashCode     		value is "1"
                ProcessLevel 		value is "2"
        PrintAddress            is AlphaUpper size 1
            States
                No  value is "N"
                Yes value is "Y"
        FromDate				is Date
        ToDate 			        is Date
        
    Parameter Rules
        RunOption
            required
        SortOption
            initial value is "1"
            default to "1"
            required
        PrintAddress
            initial value is "N"
            default to "N"
            required

    Actions
        StaleDatingAndEscheatmentReport is a Set Action
            allow unlimited concurrency
