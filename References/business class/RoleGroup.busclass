RoleGroup is a BusinessClass
    owned by security
    prefix is RGRP
    stored in environment

	Ontology
		symbolic key is RoleGroup

	Patterns
		disable AuditIndex

	Persistent Fields
		Description is a LSDescription
		Used 		is Boolean
	
	Attach Rules
    	Used = true 
    		
	Actions

        Create is an Action
        Update is an Update Action
        Delete is an Action
        	valid when (!Used)

	Relations
		RoleGroupMembers is a RoleGroupMember set
