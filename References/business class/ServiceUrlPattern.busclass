ServiceUrlPattern is a BusinessClass
    owned by security
    prefix is SUP
    stored in environment
	contains environment data
	disable data area copy
		preserve target data

    Ontology
        part of Service
            relative key is ServiceUrlPattern is Numeric size 6

    Patterns
        implements CRUD
        disable AuditIndex

    Persistent Fields
        Value   is Alpha size 200
