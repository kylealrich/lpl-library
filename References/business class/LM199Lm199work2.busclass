LM199Lm199work2 is a BusinessClass
    owned by lm
    prefix is W8811

    Patterns
        disable StaticTranslations
        implements WorkFile

    Persistent Fields
        WorkFile                is UniqueID
        WorkFileSeq             is Numeric size 12
        InvCompany              is like Company
        InvLease                is like Lease 
        LseDescription          is like Description    
        InvPmtVendor            is like Vendor   
        InvInvoice              is like Invoice
        LseLeaseClass           is like LeaseClassification
        InvDueDate              is Date    
        InvTranAmount           is like InternationalAmount
        InvTranNd               is like NumberOfDecimals
        InvStatus               is Numeric size 1

    Sets

        InsertionOrderSet
            indexed
            no duplicates
            primary
            Sort Order
                WorkFile
                WorkFileSeq

        LM199Lm199work2LM199Sort1
            indexed
            duplicates
            Sort Order
                WorkFile
                InvCompany
                InvLease

    Actions

        Create is a Create Action

            Field Rules
                WorkFileSeq
                    autosequence

        Delete is a Delete Action

        DeleteAll is a Set Action
            restricted

            Parameters
                WorkFileHeader is UniqueID

            Instance Selection
                where (WorkFile = WorkFileHeader)

            Local Fields
                LWorkFileHeader is a WorkFile

            Parameter Rules
                WorkFileHeader
                    required
                    LWorkFileHeader = WorkFileHeader

            Action Rules
                Instance Rules
                    invoke Delete

                Empty Set Rules
                    invoke Purge LWorkFileHeader

                Set Rules
                    Exit Rules
                        invoke Purge LWorkFileHeader
