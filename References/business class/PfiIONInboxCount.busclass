PfiIONInboxCount is a BusinessTask
    owned by pfi
    default label is "IPA_IONInboxCount"

    Patterns
        implements ManualTransaction

    Parameters
        ThruDate     is Date
        IncludeError is Boolean

    Parameter Rules
        ThruDate     required
							
    Actions

        CountProcessed is an Instance Action
        	default label is "CountProcessed"
