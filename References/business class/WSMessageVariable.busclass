WSMessageVariable is a BusinessClass
    owned by la
    prefix is WSMV

    Ontology

		part of WSMessage
    		relative key is VariableName is Alpha size 30

	Patterns
    	implements CRUD

	Persistent Fields
        ClassicName		  		is Alpha size 22
        Value					is Text
