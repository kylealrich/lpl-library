IDMIONCredentials is a BusinessClass 
	owned by idm
	prefix is IDMCN
	
	Ontology
		symbolic key is IDMIONCredentials
	
	Patterns
		implements Encrypted 
		
	Persistent Fields
		PrivateKey						is Alpha size 3500 
			restricted
		Credentials						is Alpha size 1500
			restricted
		PrivateKeyFileName				is DocumentTitle size 100
		CredentialsFileName				is DocumentTitle size 100
		UseIDTranslation				is Boolean
						
 	Actions		
 		CreateFromIDMConfiguration is a Create Action
			restricted
			
		UpdateFromIDMConfiguration is an Update Action
			restricted
